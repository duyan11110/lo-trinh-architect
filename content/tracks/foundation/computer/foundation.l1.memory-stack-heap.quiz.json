{
  "lesson": "foundation.l1.memory-stack-heap",
  "stage": 0,
  "questions": [
    {
      "id": "foundation.l1.memory-stack-heap.q1",
      "type": "order",
      "bloom": "remember",
      "difficulty": 2,
      "skills": [
        "foundation.os.memory"
      ],
      "section_ref": 4,
      "question": {
        "vi": "Trong bài mẫu, `Run` được gọi, tạo một `Order` rồi trả về. Sắp xếp năm thời điểm sau theo đúng thứ tự xảy ra.",
        "en": "In the sample, `Run` is called, creates one `Order`, then returns. Put these five moments in the order they happen."
      },
      "options": [
        {
          "id": "a",
          "vi": "`Run` trả về, cả khối nhớ của nó rời đi một lần cùng bốn ô biến trong đó",
          "en": "`Run` returns, and its whole block of memory goes at once with the four slots in it"
        },
        {
          "id": "b",
          "vi": "Một khối nhớ cho `Run` được thêm lên stack, có chỗ cho từng biến cục bộ của nó",
          "en": "A block of memory for `Run` is added to the stack, with room for each of its local variables"
        },
        {
          "id": "c",
          "vi": "garbage collector thấy không còn gì với tới được đối tượng đó và thu hồi bộ nhớ của nó",
          "en": "The garbage collector finds nothing reaching that object any more and takes its memory back"
        },
        {
          "id": "d",
          "vi": "`new Order` đặt một đối tượng lên heap, và khối nhớ của `Run` nhận được đường tới nó",
          "en": "`new Order` puts an object on the heap, and the block for `Run` gets a way to reach it"
        },
        {
          "id": "e",
          "vi": "Cái mà `first` đang giữ được chép sang `second`, nên hai ô cùng với tới một đối tượng",
          "en": "What `first` holds is copied into `second`, so the two slots reach one object"
        }
      ],
      "answer": [
        "b",
        "d",
        "e",
        "a",
        "c"
      ],
      "explanation": {
        "vi": {
          "correct": "Khối nhớ trên stack có trước, vì các biến cục bộ phải có chỗ đứng trước khi được gán. `new Order` đặt đối tượng lên heap rồi mới có gì để chép sang `second`. Khi `Run` trả về, chỉ khối nhớ trên stack rời đi; đối tượng trên heap ở lại cho tới lượt dọn sau đó của garbage collector."
        },
        "en": {
          "correct": "The block on the stack comes first, because the local variables need somewhere to sit before anything is assigned to them. `new Order` puts the object on the heap before there is anything to copy into `second`. When `Run` returns, only the stack block goes; the heap object stays until a later pass of the garbage collector."
        }
      }
    },
    {
      "id": "foundation.l1.memory-stack-heap.q2",
      "type": "single",
      "bloom": "understand",
      "difficulty": 2,
      "skills": [
        "foundation.os.memory"
      ],
      "section_ref": 3,
      "question": {
        "vi": "Trong bài mẫu, `first` và `second` cùng đến từ một `new Order`. Mỗi ô trong hai ô đó thực sự giữ cái gì?",
        "en": "In the sample, `first` and `second` both come from one `new Order`. What does each of those two slots actually hold?"
      },
      "options": [
        {
          "id": "a",
          "vi": "Một bản của đơn hàng cho mỗi ô, nên khối nhớ rốt cuộc giữ hai đơn hàng",
          "en": "A copy of the order, one per slot, so the frame ends up holding two orders",
          "misconception": "Assigning an object to a new variable copies the object."
        },
        {
          "id": "b",
          "vi": "Đường tới một đối tượng trên heap, nên cả hai ô đều dẫn tới cùng một đơn hàng",
          "en": "A way to reach one object on the heap, so both slots lead to the same order"
        },
        {
          "id": "c",
          "vi": "Đơn hàng ở trong `first`, còn `second` giữ đường tới chính đơn hàng đó",
          "en": "The order in `first`, and a way to reach that same order in `second`"
        },
        {
          "id": "d",
          "vi": "Một con số đếm còn bao nhiêu biến với tới được đơn hàng, do runtime giữ",
          "en": "A count of how many variables still reach the order, kept by the runtime"
        }
      ],
      "answer": [
        "b"
      ],
      "explanation": {
        "vi": {
          "correct": "`Order` khai báo bằng `class`, nên biến kiểu đó không giữ đối tượng mà giữ đường tới đối tượng nằm trên heap. Phép gán chép đúng thứ trong ô, tức là đường đó, nên hai ô dẫn về một đối tượng duy nhất.",
          "a": "Dễ nghĩ vậy vì phép gán ở nơi khác đúng là tạo bản độc lập — với `Money` trong cùng bài mẫu thì đúng thật; nhưng với kiểu khai báo bằng `class`, thứ được chép là đường tới đối tượng, không phải đối tượng.",
          "c": "Nghe hợp lý nếu nghĩ `first` là bản gốc còn `second` chỉ là lối tắt; nhưng hai ô hoàn toàn ngang nhau, `first` cũng chỉ giữ một đường tới đối tượng như `second`.",
          "d": "Đoán rằng có một con số đếm cũng hợp lý, vì phải có gì đó quyết định khi nào một đối tượng hết được dùng; nhưng bài không nói ô biến giữ con số nào cả — ô biến của kiểu khai báo bằng `class` giữ đường tới đối tượng, còn việc quyết định chương trình còn với tới được gì là việc của garbage collector."
        },
        "en": {
          "correct": "`Order` is declared with `class`, so a variable of that type does not hold the object; it holds a way to reach an object on the heap. Assignment copies what the slot holds, which is that way, so both slots lead to one object.",
          "a": "Reasonable, because assignment elsewhere really does make an independent copy — it does for `Money` in the same sample; but for a type declared with `class` what gets copied is the way to reach the object, not the object.",
          "c": "This feels right if you read `first` as the original and `second` as a shortcut to it; but the two slots are equals, and `first` holds only a way to reach the object exactly as `second` does.",
          "d": "Guessing at a count is fair, since something must decide when an object is finished with; but nothing in this lesson says a slot holds a count — a slot of a type declared with `class` holds a way to reach the object, and deciding what the program can still reach is the garbage collector's business."
        }
      }
    },
    {
      "id": "foundation.l1.memory-stack-heap.q3",
      "type": "truefalse",
      "bloom": "understand",
      "difficulty": 2,
      "skills": [
        "foundation.os.memory"
      ],
      "section_ref": 4,
      "question": {
        "vi": "Khẳng định: \"Sau khi `second.TotalVnd = 0;` chạy, trên heap có hai đối tượng `Order`, một giữ 1250000 và một giữ 0.\"",
        "en": "Statement: \"After `second.TotalVnd = 0;` runs, the heap holds two `Order` objects, one with 1250000 and one with 0.\""
      },
      "options": [
        {
          "id": "a",
          "vi": "Đúng, vì `var second = first;` tạo ra một đơn hàng thứ hai, và dòng ngay sau đó chỉ sửa bản mới đó",
          "en": "True, because `var second = first;` makes a second order, and the next line changes only that new copy",
          "polarity": "true",
          "misconception": "Assigning an object to a new variable copies the object."
        },
        {
          "id": "b",
          "vi": "Đúng, vì mỗi biến gọi tên một đơn hàng đều cần đối tượng riêng trên heap để giữ tổng tiền của mình",
          "en": "True, because every variable that names an order needs its own heap object to hold that order's total",
          "polarity": "true"
        },
        {
          "id": "c",
          "vi": "Sai, vì `var second = first;` chép đường tới đối tượng, nên cả hai ô cùng với tới đúng một đối tượng",
          "en": "False, because `var second = first;` copies the way to reach the object, so both slots reach one object",
          "polarity": "false"
        },
        {
          "id": "d",
          "vi": "Sai, vì garbage collector đã thu hồi đối tượng cũ ngay khi tổng tiền của nó đổi, chỉ còn lại một",
          "en": "False, because the garbage collector took the old object back as soon as its total changed, leaving one",
          "polarity": "false"
        }
      ],
      "answer": [
        "c"
      ],
      "explanation": {
        "vi": {
          "correct": "Chỉ có một lần `new Order` chạy, nên chỉ có một đối tượng được đặt lên heap. Phép gán chỉ thêm một ô nữa cùng với tới đối tượng đó, nên ghi qua `second` là ghi qua `first`.",
          "a": "Đây là cách đọc tự nhiên nhất của dấu `=` và cũng là lý do dòng in ra làm nhiều người bất ngờ; nhưng `=` chép thứ trong ô biến, mà với kiểu `class` thứ đó là đường tới đối tượng.",
          "b": "Câu này đúng cho đối tượng được tạo bằng `new`, nên nghe rất hợp lý; nhưng ở đây chỉ có một lần `new`, còn `second` không tạo gì cả — nó chỉ nhận đường tới đối tượng đã có.",
          "d": "Kết luận \"chỉ còn một\" tình cờ đúng, nhưng lý do thì không: garbage collector chỉ thu hồi đối tượng khi không còn gì với tới nó, chứ không phản ứng với việc một trường bị đổi giá trị."
        },
        "en": {
          "correct": "`new Order` runs once, so exactly one object is put on the heap. The assignment only adds a second slot that reaches that same object, so writing through `second` is writing through `first`.",
          "a": "This is the most natural reading of `=`, and it is why the printed line surprises people; but `=` copies what the slot holds, and for a type declared with `class` that is a way to reach the object.",
          "b": "The claim is true of objects made with `new`, which is why it sounds right; but `new` runs once here, and `second` creates nothing — it receives the way to reach the object that already exists.",
          "d": "The conclusion \"one is left\" happens to be right, but the reason is not: the garbage collector takes an object back when nothing reaches it, not in response to one of its fields changing."
        }
      }
    },
    {
      "id": "foundation.l1.memory-stack-heap.q4",
      "type": "single",
      "bloom": "understand",
      "difficulty": 2,
      "skills": [
        "foundation.os.memory"
      ],
      "section_ref": 6,
      "question": {
        "vi": "Bạn giữ một biến duy nhất còn với tới một `Order`, rồi gán nó `null` và nhìn bộ nhớ mà một công cụ hệ thống báo cho process của bạn. Ngay sau dòng đó, bạn chắc chắn được điều gì?",
        "en": "One variable is the last thing still reaching an `Order`. You set it to `null`, then watch the memory a system tool reports for your process. What can you be sure of right after that line?"
      },
      "options": [
        {
          "id": "a",
          "vi": "Không còn gì với tới đơn hàng đó nữa, nhưng bộ nhớ của nó có thể còn bị chiếm thêm một lúc",
          "en": "Nothing reaches that order any more, but its memory can still be in use for a while"
        },
        {
          "id": "b",
          "vi": "Bộ nhớ của đơn hàng đã được trả lại ngay tại dòng đó, nên con số bạn nhìn tụt xuống ngay",
          "en": "The order's memory came back at that line, so the number you watch drops right away",
          "misconception": "Setting a variable to null frees the memory immediately."
        },
        {
          "id": "c",
          "vi": "Đơn hàng vẫn còn được với tới, vì xóa một biến không hủy được phép gán đã chạy trước đó",
          "en": "The order is still reached, because clearing a variable does not undo the assignment before it"
        },
        {
          "id": "d",
          "vi": "Dòng đó đã yêu cầu garbage collector chạy, nên bộ nhớ quay về ở lượt dọn ngay kế tiếp",
          "en": "That line asked the garbage collector to run, so the memory returns on its very next pass"
        }
      ],
      "answer": [
        "a"
      ],
      "explanation": {
        "vi": {
          "correct": "Gán `null` chỉ bỏ đi một đường tới đối tượng, và ở đây là đường cuối cùng. Đối tượng thành thứ chương trình không còn với tới được, nhưng bộ nhớ chỉ quay về khi một lượt dọn sau đó của garbage collector phát hiện ra điều ấy — bạn không nói được là ở dòng nào.",
          "b": "Rất dễ tin vì đó đúng là điều bạn muốn xảy ra, và ở một số ngôn ngữ khác lập trình viên tự giải phóng bộ nhớ; nhưng trong .NET không có câu lệnh nào trả lại bộ nhớ của một đối tượng, việc đó do garbage collector làm theo lịch của nó.",
          "c": "Đúng là phép gán trước đó đã chạy và không undo được, nên câu này nghe có lý; nhưng nó nói về ô biến chứ không về đối tượng, mà ô biến giờ không giữ đường tới đâu nữa.",
          "d": "Nghe hợp lý vì có vẻ phải có gì đó khởi động garbage collector; nhưng không dòng nào trong bài yêu cầu nó chạy, và nó tự quyết định thời điểm."
        },
        "en": {
          "correct": "Setting `null` removes one way to reach the object, and here it was the last one. The program can no longer reach the object, but its memory returns only when a later pass of the garbage collector notices — you cannot name the line.",
          "b": "Easy to believe, because it is what you want to happen, and in some other languages the programmer does free memory; but no statement in .NET returns one object's memory, and the garbage collector works on its own schedule.",
          "c": "The earlier assignment really did run and cannot be undone, which is why this sounds right; but it describes the slot rather than the object, and the slot now holds no way to reach anything.",
          "d": "Plausible, since something ought to start the garbage collector; but no line in this lesson asks it to run, and it decides its own timing."
        }
      }
    },
    {
      "id": "foundation.l1.memory-stack-heap.q5",
      "type": "multi",
      "bloom": "apply",
      "difficulty": 3,
      "skills": [
        "foundation.os.memory"
      ],
      "section_ref": 5,
      "question": {
        "vi": "Bạn chạy `dotnet run --project samples/DonHang.Samples -- stack-heap` trong repo ví dụ. Chọn tất cả những phát biểu đúng về lần chạy đó.",
        "en": "You run `dotnet run --project samples/DonHang.Samples -- stack-heap` in the example repository. Select all that apply to that run."
      },
      "options": [
        {
          "id": "a",
          "vi": "Dòng đầu báo `first.TotalVnd` là `0`, vì ghi qua `second` chính là ghi qua `first`",
          "en": "The first line reports `first.TotalVnd` as `0`, because writing through `second` is writing through `first`"
        },
        {
          "id": "b",
          "vi": "Dòng thứ hai báo `oneAmount.AmountVnd` vẫn là `1250000`, vì `anotherAmount` là một số tiền riêng",
          "en": "The second line reports `oneAmount.AmountVnd` still `1250000`, because `anotherAmount` is a separate amount"
        },
        {
          "id": "c",
          "vi": "Cả hai dòng đều báo `1250000`, vì phép gán ở cả hai nửa đều chép ra thứ độc lập với bản gốc",
          "en": "Both lines report `1250000`, because assignment in both halves copies something independent of the original",
          "misconception": "Assigning an object to a new variable copies the object."
        },
        {
          "id": "d",
          "vi": "Sau khi `var second = first;` chạy, trên heap có một đối tượng `Order` và hai ô trong khối nhớ cùng với tới nó",
          "en": "After `var second = first;` has run, one `Order` object sits on the heap and two slots in the frame reach it"
        },
        {
          "id": "e",
          "vi": "Dòng đầu báo `0` vì `new Order` không lưu được `1_250_000`: dấu gạch dưới làm giá trị không đọc được",
          "en": "The first line reports `0` because `new Order` never stored `1_250_000`: the underscores spoil the value"
        }
      ],
      "answer": [
        "a",
        "b",
        "d"
      ],
      "explanation": {
        "vi": {
          "correct": "`Order` khai báo bằng `class` nên `second` chỉ là ô thứ hai với tới một đối tượng; `Money` khai báo bằng `struct` nên `anotherAmount` là số tiền thứ hai, độc lập. Cùng một dãy ba bước cho hai kết quả khác nhau, và khác biệt quyết định ở đây là từ khóa khai báo mỗi kiểu.",
          "c": "Đây là suy luận đúng nếu mọi phép gán đều chép ra bản độc lập, và với `Money` thì đúng như vậy — nên nửa sau của câu này không sai; nhưng với `Order`, thứ được chép là đường tới đối tượng, nên dòng đầu in `0`.",
          "e": "Dấu gạch dưới trông lạ nên nghi ngờ nó là phản xạ hợp lý; nhưng `1_250_000` và `1250000` là cùng một giá trị, còn dòng đầu in `0` là vì `second.TotalVnd = 0;`."
        },
        "en": {
          "correct": "`Order` is declared with `class`, so `second` is only a second slot reaching one object; `Money` is declared with `struct`, so `anotherAmount` is a second, independent amount. The same three steps give two different results, and the difference that decides it is the keyword each type is declared with.",
          "c": "This follows correctly if every assignment copies something independent, and for `Money` it does — so half the reasoning holds; but for `Order` what gets copied is the way to reach the object, so the first line prints `0`.",
          "e": "The underscores look unusual, so suspecting them is a fair reflex; but `1_250_000` and `1250000` are the same value, and the first line prints `0` because of `second.TotalVnd = 0;`."
        }
      }
    },
    {
      "id": "foundation.l1.memory-stack-heap.q6",
      "type": "single",
      "bloom": "apply",
      "difficulty": 3,
      "skills": [
        "foundation.os.memory"
      ],
      "section_ref": 5,
      "question": {
        "vi": "Bạn đổi `private struct Money` thành `private sealed class Money`, giữ nguyên mọi dòng khác, rồi chạy lại bài mẫu. Giờ cả hai dòng in ra đều báo `0`. Phát biểu nào giải thích đúng?",
        "en": "You change `private struct Money` to `private sealed class Money`, leave every other line as it is, and run the sample again. Both printed lines now report `0`. Which statement explains why?"
      },
      "options": [
        {
          "id": "a",
          "vi": "`anotherAmount` giờ nhận cùng một đường tới một số tiền chứ không phải bản chép, nên đặt nó về `0` là đặt luôn thứ `oneAmount` với tới",
          "en": "`anotherAmount` now receives the same way to reach one amount instead of a copy, so setting it to `0` sets what `oneAmount` reaches"
        },
        {
          "id": "b",
          "vi": "`oneAmount` và `anotherAmount` giờ là cùng một biến, nên trong khối nhớ chỉ còn một ô để in ra",
          "en": "`oneAmount` and `anotherAmount` are now one and the same variable, so only one slot is left in the frame to print",
          "misconception": "After `second` changed the order, `first` and `second` are the same variable."
        },
        {
          "id": "c",
          "vi": "`sealed` khiến .NET runtime giữ chung một số tiền duy nhất cho mọi biến thuộc kiểu đó",
          "en": "`sealed` makes the .NET runtime keep a single shared amount for every variable of that type"
        },
        {
          "id": "d",
          "vi": "Biến của kiểu khai báo bằng `class` không tự giữ được con số, nên đọc số tiền ra thì được `0`",
          "en": "A variable of a type declared with `class` cannot hold a number itself, so reading the amount gives `0`"
        }
      ],
      "answer": [
        "a"
      ],
      "explanation": {
        "vi": {
          "correct": "Từ khóa khai báo kiểu quyết định biến giữ gì. Đổi `struct` thành `class` làm `new Money` đặt một đối tượng lên heap, nên `anotherAmount` nhận đường tới chính đối tượng mà `oneAmount` với tới, và `anotherAmount.AmountVnd = 0;` đổi thứ cả hai cùng đọc.",
          "b": "Hai cái tên lúc nào cũng báo một con số thì trông đúng là một biến, nên cách đọc này rất tự nhiên; nhưng khối nhớ vẫn có hai ô, mỗi ô giữ đường riêng của mình tới đối tượng — đặt một số tiền khác vào một ô thì ô kia vẫn giữ đường cũ.",
          "c": "`sealed` là từ có thật trong dòng bạn vừa sửa nên nghi nó là hợp lý; nhưng bài đã nói `sealed` không liên quan tới chỗ đối tượng nằm, và `Order` mang từ khóa đó từ đầu trong khi nửa đơn hàng không hề đổi. Chính từ khóa `class` mới đưa số tiền lên heap.",
          "d": "Đúng là biến của kiểu khai báo bằng `class` không tự giữ dữ liệu, nên câu này xuất phát từ ý đúng; nhưng nó giữ đường tới đối tượng đang giữ dữ liệu, và `oneAmount.AmountVnd` đọc qua đường đó. Số `0` là do lệnh ghi qua `anotherAmount`."
        },
        "en": {
          "correct": "The keyword a type is declared with decides what its variables hold. Turning `struct` into `class` makes `new Money` put an object on the heap, so `anotherAmount` receives the way to reach the very object `oneAmount` reaches, and `anotherAmount.AmountVnd = 0;` changes what both read.",
          "b": "Two names that always report the same number do look like one variable, which is why this reads well; but the frame still holds two slots, each with its own way to reach the object — put a different amount into one and the other keeps the old one.",
          "c": "`sealed` is a real word in the line you edited, so suspecting it is fair; but the lesson says `sealed` has nothing to do with where an object lives, and `Order` carried it all along while its half of the sample did not change. The `class` keyword is what moved the amount to the heap.",
          "d": "A variable of a type declared with `class` really does not hold the data itself, so this starts from the right idea; but it holds the way to reach the object that does, and `oneAmount.AmountVnd` reads through it. The `0` comes from the write through `anotherAmount`."
        }
      }
    }
  ]
}
