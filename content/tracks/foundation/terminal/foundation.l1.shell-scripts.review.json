{
  "id": "foundation.l1.shell-scripts",
  "technical": {
    "reviewed_at": "2026-09-09T14:30:00+07:00",
    "reviewer": "claude-opus-5",
    "claim_verdicts": [
      {
        "n": 1,
        "verdict": "verified",
        "evidence": "Execution this session (bash 5.2.26). Built an executable file whose first line is `#!/usr/bin/env myreader`, plus a program `myreader` printing its first argument. Running the file directly printed `myreader was started; it was handed the file: /tmp/.../subject` and `first line of that file: #!/usr/bin/env myreader` — those two characters plus a path started that program and handed it this file. \"Reads the lines in order\": the real script, run here, printed its four lines in file order (claim 21).",
        "source": "execution (bash 5.2.26, host); refs/dotnet-docs/docs/csharp/language-reference/preprocessor-directives.md"
      },
      {
        "n": 2,
        "verdict": "verified",
        "evidence": "Execution this session. With the folder holding `myreader` absent from PATH, `env myreader <file>` printed `env: 'myreader': No such file or directory`; after `export PATH=\"$d/bin:$PATH\"` the same command ran it, and `command -v myreader` printed `/tmp/.../bin/myreader` while `command -v env` printed `/usr/bin/env`. So `env` locates the named program through PATH rather than the file naming a folder for it. See the section 4 issue: the body's clause \"so the file names no folder itself\" overstates this — the file does name `/usr/bin`.",
        "source": "execution (bash 5.2.26 + GNU coreutils env, host)"
      },
      {
        "n": 3,
        "verdict": "verified",
        "evidence": "Execution this session. A script beginning `set -euo pipefail` printed `start`, then `false` on its second-to-last line ended the run: the following `echo \"MUST NOT PRINT\"` produced nothing and the script's status was 1. docs.github.com corroborates the flag: the default shell for `run` is `bash --noprofile --norc -eo pipefail {0}`.",
        "source": "execution (bash 5.2.26, host); https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax (defaults.run.shell table)"
      },
      {
        "n": 4,
        "verdict": "verified",
        "evidence": "Execution this session, closing round 3's `unverified`. After `set -u`, `echo \"$never_set_name\"` wrote `t2.sh: line 14: never_set_name: unbound variable` to standard error and the script ended with status 1; the line after it never ran. The same run showed `-u` leaves `${1:-/tmp/backups}` (printed `/tmp/backups`) and an empty `\"$@\"` alone, which is what line 5 of the lesson's script needs.",
        "source": "execution (bash 5.2.26, host)"
      },
      {
        "n": 5,
        "verdict": "verified",
        "evidence": "Execution this session: `false | true` left status 0 with `set +o pipefail` and status 1 with `set -o pipefail`; `true | false | true` left 1 with pipefail, so it is any command in the chain, not only the rightmost. docs.github.com states the same rule for its default shell: \"the return status of a pipeline is the status of the last command to exit with a non-zero status, or zero if no command exited with a non-zero status\".",
        "source": "execution (bash 5.2.26, host); https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax"
      },
      {
        "n": 6,
        "verdict": "verified",
        "evidence": "Execution this session against the real script (line 5 neutralised, `pg_dump` stubbed): with no argument it printed `wrote /tmp/backups/donhang-schema.sql`, with `/tmp/mine` it printed `wrote /tmp/mine/donhang-schema.sql`. Separately, `set -- \"\"` then `echo \"${1:-/tmp/backups}\"` printed `/tmp/backups`, so an empty first word also falls back. refs/docker-docs (Dockerfile reference, Environment replacement) names `${variable:-word}` a standard bash modifier with the same set-and-non-empty rule.",
        "source": "execution (bash 5.2.26, host) against examples/don-hang scripts/terminal/backup-db.sh at stage-0; refs/docker-docs Dockerfile reference"
      },
      {
        "n": 7,
        "verdict": "verified",
        "evidence": "Execution this session. A script invoked as `args.sh \"/tmp/my backups\" second` reported `dollar-one = [/tmp/my backups]`, `dollar-two = [second]`, `dollar-hash = 2`. With the quotes dropped, `mkdir -p $p` where `p=\"my backups\"` created two folders, `my` and `backups`, while `mkdir -p \"$p\"` created one folder named `my backups`. Splitting happens at the spaces; double quotes stop it, both on the command line and around `\"$1\"`.",
        "source": "execution (bash 5.2.26 + GNU mkdir, host)"
      },
      {
        "n": 8,
        "verdict": "verified",
        "evidence": "Execution this session: `true` left 0, `false` left 1, a script whose last command is `(exit 5)` left 5, a script ending `exit 7` left 7 to its caller, `mkdir -p` left 0 both on a new and on an existing folder, `[ -f <missing> ]` left 1. Every command observed left a number; only 0 followed a command that did what it was asked. docs.github.com treats a non-zero `run` step as a failed step.",
        "source": "execution (bash 5.2.26, host); https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax"
      },
      {
        "n": 9,
        "verdict": "rephrased",
        "evidence": "Execution this session shows \"most recently executed foreground command\" is inexact on the very page that teaches `pipefail`: with pipefail on, `false | true` leaves `$?` = 1 although the command that finished last, `true`, exits 0 (without pipefail the same chain leaves 0). The lesson's own runs are unaffected — after `(exit 9)` `$?` read 9 and after the next `echo` it read 0, and lines 17-18 of the script behave as claimed.",
        "source": "execution (bash 5.2.26, host)",
        "suggested_text": "`$?` expands to the exit status of the most recently executed foreground pipeline — a single command, or a chain built with `|` judged as one — so it reports on that one thing only, and the next command replaces it."
      },
      {
        "n": 10,
        "verdict": "verified",
        "evidence": "Execution this session, closing round 3's `unverified` on the `set -e` clause. Under `set -e`: `false || echo X` printed X and the script carried on; `false && echo unreachable` printed nothing and the script carried on to its next line and exited 0; but `false || false` ended the script with status 1 and the `echo` below it never ran. So a failure inside such a list is ignored except in its last command, which is judged.",
        "source": "execution (bash 5.2.26, host)"
      },
      {
        "n": 11,
        "verdict": "verified",
        "evidence": "Execution this session, closing round 3's `unverified`. A script whose last command is `(exit 5)` — after a successful `echo hello` — left status 5 to its caller; a script stopped by `set -e` at a `false` left 1; the lesson's own script, run here, left 0. The discriminating run round 3 lacked (a failing last command) was made.",
        "source": "execution (bash 5.2.26, host)"
      },
      {
        "n": 12,
        "verdict": "verified",
        "evidence": "Execution this session, closing round 3's blocker. A script invoked as `bash args.sh \"/tmp/my backups\" second` printed `dollar-zero = [/tmp/.../args.sh]`, `dollar-one = [/tmp/my backups]`, `dollar-two = [second]`, `dollar-hash = 2`, and iterating `\"$@\"` yielded exactly those two values in that order. All four parts are settled by execution, so no bash docs root is needed. Note: the sentence sits in section 3's Core concepts bullet, while the ledger files it under section 4.",
        "source": "execution (bash 5.2.26, host)"
      },
      {
        "n": 13,
        "verdict": "verified",
        "evidence": "Execution this session: `[ -f regular.txt ]` on a file just created ran the then-branch (0); `[ -f <a directory> ]` left 1; `[ -f /nope/nothing ]` left 1. So 0 requires both existence and a regular file, exactly as the claim words it. The lesson body drops the \"regular file\" half — raised as a minor on section 5.",
        "source": "execution (bash 5.2.26, host)"
      },
      {
        "n": 14,
        "verdict": "verified",
        "evidence": "Repo at stage-0: docker-compose.yml defines service `lab` (build ./lab, `./:/repo:ro`), started by scripts/up.sh. scripts/capture-output.sh runs `bash scripts/terminal/backup-db.sh` on the host, and outputs/stage-0/scripts/terminal/backup-db.txt holds the four expected lines — so line 5's test failed on the host (exec fired) and succeeded in the box (no second re-exec; `pg_dump --host db` only resolves on the compose network). `/.dockerenv` itself is undocumented: refs/docker-docs has one 2014 release note. Docker Desktop was down, so no live check.",
        "source": "examples/don-hang at stage-0 (docker-compose.yml, scripts/up.sh, scripts/capture-output.sh, scripts/lab-run.sh, outputs/stage-0/scripts/terminal/backup-db.txt); refs/docker-docs"
      },
      {
        "n": 15,
        "verdict": "verified",
        "evidence": "Execution this session, closing round 3's `unverified`. An outer script printed `outer pid: 7738`, then ran `[ -f /.dockerenv ] || exec \"$(dirname \"$0\")/inner.sh\" \"$0\" \"$@\"` with arguments `/tmp/mine` and `two words`; the inner script printed `inner pid: 7738` — the same process, not a child — received `count=3` with `two words` intact as one argument, and its `exit 7` became the outer script's status. The `echo` written after the exec line never printed.",
        "source": "execution (bash 5.2.26, host)"
      },
      {
        "n": 16,
        "verdict": "verified",
        "evidence": "Execution this session ran the repository's own lines 11-12 (the `pg_dump` call with the trailing `\\`, `pg_dump` stubbed) as one command, writing to `\"$file\"` and leaving one status. Separately, `ls /nowhere > out.txt 2> err.txt` left out.txt at 0 bytes and put `ls: cannot access '/nowhere': No such file or directory` in err.txt, so `>` moves standard output only and complaints go elsewhere.",
        "source": "execution (bash 5.2.26 + GNU ls, host) against examples/don-hang scripts/terminal/backup-db.sh at stage-0"
      },
      {
        "n": 17,
        "verdict": "verified",
        "evidence": "Execution this session of the repository's own line 15: the run printed `tables in the backup: 6`, so `grep -c 'CREATE TABLE' \"$file\"` ran first and its standard output was substituted into the sentence. Line 5's `$(dirname \"$0\")` was exercised in the exec test: `dirname` ran first and its output became part of the path that started the next script.",
        "source": "execution (bash 5.2.26 + GNU grep/dirname, host) against examples/don-hang scripts/terminal/backup-db.sh at stage-0"
      },
      {
        "n": 18,
        "verdict": "verified",
        "evidence": "PostgreSQL 17 pg_dump reference, fetched this session: `--schema-only` — \"Dump only the object definitions (schema), not data.\"; `--no-owner` — \"Do not output commands to set ownership of objects to match the original database.\"; `--no-privileges` — \"Prevent dumping of access privileges (grant/revoke commands).\"; `--host` — \"Specifies the host name of the machine on which the server is running.\"; `--username` — \"User name to connect as.\"; `--dbname` — \"Specifies the name of the database to connect to.\" All six halves hold; postgresql is pinned at 17.",
        "source": "https://www.postgresql.org/docs/17/app-pgdump.html"
      },
      {
        "n": 19,
        "verdict": "verified",
        "evidence": "examples/don-hang/db/schema.sql at stage-0 has exactly six `CREATE TABLE` lines (products, customers, orders, order_items, payments, notifications — lines 4, 11, 18, 26, 34, 42) and `grep -c 'CREATE TABLE' db/schema.sql` printed 6 here. docker-compose.yml mounts schema.sql then seed.sql into /docker-entrypoint-initdb.d and seed.sql only inserts rows. outputs/stage-0/scripts/terminal/backup-db.txt line 2 reads `tables in the backup: 6`, and the run made this session printed the same.",
        "source": "examples/don-hang at stage-0 (db/schema.sql, db/seed.sql, docker-compose.yml, outputs/stage-0/scripts/terminal/backup-db.txt); execution (host)"
      },
      {
        "n": 20,
        "verdict": "verified",
        "evidence": "Execution this session ran the real script three times (no argument, `/tmp/mine`, `\"/tmp/my backups\"`); each run ended with a blank line and then `the exit code of the last command was 0`, so round 3's caveat that \"after every run\" rested on a single recorded run is closed. The bare `echo` on line 17 is what prints that blank line, and `$?` on line 18 reads its status, not `pg_dump`'s.",
        "source": "execution (bash 5.2.26, host) against examples/don-hang scripts/terminal/backup-db.sh at stage-0"
      },
      {
        "n": 21,
        "verdict": "verified",
        "evidence": "Execution this session, closing round 3's `unverified`. The repository's own scripts/terminal/backup-db.sh was copied with line 5 replaced by `true` (diff confirms that is the only difference) and a stub `pg_dump` emitting db/schema.sql placed first on PATH. `bash backup-db.sh /tmp/mine` printed `wrote /tmp/mine/donhang-schema.sql` then `tables in the backup: 6`; the no-argument run printed `wrote /tmp/backups/donhang-schema.sql` and the same count, reproducing outputs/stage-0/scripts/terminal/backup-db.txt line for line.",
        "source": "execution (bash 5.2.26, host) against examples/don-hang scripts/terminal/backup-db.sh and db/schema.sql at stage-0"
      },
      {
        "n": 22,
        "verdict": "verified",
        "evidence": "Mechanism half by execution: a script's status reached its caller unchanged (7 from an `exit 7` under `exec`, 5 from a last command `(exit 5)`, 1 from a `set -e` stop) while the words it printed were carried on a separate stream. CI half from docs.github.com, which lists bash's default `run` command as `bash --noprofile --norc -eo pipefail {0}` — a build step is a bash script. The closing clause (\"which is why reading such a step starts with reading a script\") is teaching framing, not a documented fact; kind is `opinion`.",
        "source": "execution (bash 5.2.26, host); https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax"
      },
      {
        "n": 23,
        "verdict": "verified",
        "evidence": "Execution this session: `dirname /repo/scripts/terminal/backup-db.sh` printed `/repo/scripts/terminal`, and appending `/../lab-run.sh` gives `/repo/scripts/terminal/../lab-run.sh`, which is scripts/lab-run.sh in the repository. `dirname backup-db.sh` printed `.` — exactly the case the claim's condition excludes. Section 5 states it without that condition, which still holds for the invocation Try it prescribes.",
        "source": "execution (bash 5.2.26 + GNU dirname, host); examples/don-hang scripts/lab-run.sh at stage-0"
      },
      {
        "n": 24,
        "verdict": "verified",
        "evidence": "Execution this session: `mkdir -p \"$d/a b/c\"` created both missing levels and printed nothing; run a second time on the now-existing folder it printed nothing again and left status 0. The lesson's script also ran `mkdir -p` on an existing `/tmp/backups` in the second and third runs without a word of output or a non-zero status.",
        "source": "execution (bash 5.2.26 + GNU mkdir, host)"
      }
    ],
    "issues": [
      {
        "severity": "major",
        "section": 4,
        "kind": "accuracy",
        "text": "The sentence contradicts the clause two before it. Section 4 first says `#!` is \"followed by a path\" and hands the file to \"the program at that path\" — that path is `/usr/bin/env`, which does name a folder, `/usr/bin`. Verified here: `command -v env` prints `/usr/bin/env`, and `env` failed with `env: 'myreader': No such file or directory` until PATH held the program's folder. What is true is that the file names no folder for `bash`; as written it says the file names no folder at all, which is false.",
        "quote": "whose job is to start `bash` wherever this machine keeps it, so the file names no folder itself",
        "fix": "End the sentence: \"…whose job is to start `bash` wherever this machine keeps it, so the file itself names no folder for `bash`.\" Claim 2 in .meta.json has the same looseness (\"instead of the file naming one fixed folder\") — make it \"one fixed folder for `bash`\"."
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "accuracy",
        "text": "The body drops the condition its own ledger entry carries. Claim 13 reads \"exits 0 when path exists and is a regular file\"; section 5 says only \"when the file exists\". Execution here: `[ -f <a directory> ]` leaves 1 even though the directory exists. True of `/.dockerenv`, which is a regular file, so the lesson's reasoning is unharmed, but a reader taking the sentence as the rule for `-f` will get a directory wrong.",
        "quote": "it ends with `0` when the file exists and with a number other than `0` when it does not",
        "fix": "Add three words: \"it ends with `0` when that file is there and is an ordinary file, and with a number other than `0` when it is not\"."
      },
      {
        "severity": "minor",
        "section": 3,
        "kind": "accuracy",
        "text": "`$?` is defined as the code of the last command, on a page that also teaches `set -o pipefail`. Execution here: with pipefail on, `false | true` leaves `$?` = 1 although the command that finished just before was `true`, which exits 0. The lesson never reads `$?` after a chain, so nothing in the walkthrough is wrong, but the two rules sit four paragraphs apart and a reader can combine them into a false one.",
        "quote": "`$?` holds the code of whichever command finished just before it, and of that one only",
        "fix": "Say \"command or chain\" in the Core concepts bullet and in the section 5 repetition: \"`$?` holds the code of whatever finished just before it — one command, or a chain judged as one — and of that only\". Take claim 9's suggested_text into .meta.json."
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "accuracy",
        "text": "\"It is eighteen lines long\" is a concrete number about the source file and there is no entry for it in .meta.json, so it is an unlisted claim. It happens to be true — `git show stage-0:scripts/terminal/backup-db.sh` is 18 lines and the block header says `lines=1-18` — but the ledger is what keeps the number honest when the example repository moves.",
        "quote": "It is eighteen lines long, so here it is whole.",
        "fix": "Add a claim to .meta.json, kind `number`, section 5: \"scripts/terminal/backup-db.sh at stage-0 is 18 lines long, so the lesson quotes it whole.\" No body change."
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "scope",
        "text": "Outline item 3 promises `$1`, `$2` and quoting. The lesson also establishes `exec`, `$0`, `$@`, `dirname`, `$( … )`, `${1:-word}`, `mkdir -p` and the trailing `\\` — each with its own explanation, not a passing mention. Every one is forced by the pinned example_files (line 5 of backup-db.sh, which foundation.l1.pipes-and-filters explicitly deferred here: \"What `||` does belongs to [[foundation.l1.shell-scripts]]\"), and depth_notes forbids only if/for/function. So the outline is out of date, not the lesson over-teaching.",
        "quote": "The rest of the line names what does the starting: `dirname \"$0\"` prints the folder this script sits in",
        "fix": "In content/tracks/foundation/track.yaml, add an outline item to foundation.l1.shell-scripts: \"Line 5 of the pinned script is read in full: `||` and `&&` react to an exit code, and `exec`, `$0`, `$@`, `dirname` and `$( … )` are explained only as far as that line needs.\""
      },
      {
        "severity": "minor",
        "section": 0,
        "kind": "version",
        "text": "Everything this lesson teaches is bash behaviour — `set -euo pipefail`, `${1:-word}`, `$?`, `$@` — and content/versions.yaml pins no bash or coreutils key, so `versions_used: [docker, postgresql]` names neither the shell nor the tools whose output is quoted. Nothing here proved version-dependent (checked on bash 5.2.26), but `set -o pipefail` and `${1:-word}` are not POSIX sh, and a reader running the script with `sh` gets different behaviour. Same finding as foundation.l1.pipes-and-filters.",
        "quote": "versions_used: [docker, postgresql]",
        "fix": "Owner-level: add a key to content/versions.yaml for the lab box's bash and core utilities (check with `docker compose exec lab bash --version` and `ls --version`), then list it in this lesson's `versions_used` and give claims 3-13, 15-17, 20, 23-24 that `version_key`. No body change."
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "accuracy",
        "text": ".meta.json `open_questions` still opens with \"BLOCKER (technical round 2, section 4) … Claim 12 … remains `unverified`\", and `tools/validate` prints that whole paragraph as an M06 warning. It is now closed: claim 12 is `verified` by execution this round, as are 4, 10, 11, 15 and 21, which the same paragraph lists as open. Leaving it in means the next reader (and the gate) sees a blocker that no longer exists. Also, claim 12's `section` is 4 while the sentence is section 3's Core concepts bullet.",
        "quote": "BLOCKER (technical round 2, section 4) still cannot be closed from this file",
        "fix": "Replace .meta.json `open_questions` entries 1-3 with one line: \"Claims 4, 10, 11, 12, 15 and 21 were settled by execution in the technical review of 2026-09-09 (bash 5.2.26); no bash docs root is needed.\" Set claim 12's `section` to 3."
      }
    ],
    "scope": {
      "outline_covered": true,
      "beyond_scope": [
        "`||` and `&&` as operators that read an exit code, and `exec`, `$0`, `$@`, `dirname`, `$( … )` — the whole of line 5 of the pinned script, deferred here in so many words by foundation.l1.pipes-and-filters; outline item 3 covers only `$1`, `$2` and quoting.",
        "`${1:-word}`, `mkdir -p` and the trailing `\\` line continuation — one clause each, forced by the pinned example file; depth_notes forbids only if/for/function detail, and none of those appears."
      ]
    },
    "verdict": "fix-required"
  },
  "junior": {
    "reviewed_at": "2026-09-09T18:40:00+07:00",
    "reviewer": "claude-opus-5 (junior reader simulation, prompt 05; known_vocab = process, stack, heap, garbage-collector, environment-variable, thread, async-await, shell, pipe + five-line summaries of foundation.l1.pipes-and-filters and foundation.l1.terminal-basics)",
    "issues": [
      {
        "severity": "major",
        "section": 4,
        "kind": "gap",
        "text": "By this rule I predicted the opposite of what Try it says. Step 1 runs the script with no word after its name, so `$1` was never set; line 7 then uses `$1`, which is \"using a name you never set\", so `-u` should end the run there. But Expected result says the run prints `wrote /tmp/backups/...`. Nothing tells me that `${1:-…}` is not one of those uses, and I cannot decide from the text which of the two is right.",
        "quote": "`-u` makes using a name you never set a failure instead of an empty value.",
        "fix": "Add one sentence at the end of the `${1:-/tmp/backups}` paragraph: \"Asking for a value with a fallback like this is not the kind of use `-u` stops — `-u` ends the run only when a name with no value is used with no fallback.\""
      },
      {
        "severity": "major",
        "section": 4,
        "kind": "diagram",
        "text": "The second box sits on the straight line from A to C, so the picture tells me the file always starts again. Two things then do not fit: the prose says it happens only \"when the run is not there already\", and quiz q1 asks me to order the steps for a run started inside the lab box, where by the prose this box does not happen at all. The picture uses a diamond for the exit code question but not for this one, so I read it as a step, not a question.",
        "quote": "B --> B2[\"not inside the lab box yet, so the file starts again from its first line in there\"]",
        "fix": "Make B2 a diamond like E: `B --> B2{\"Already inside the lab box?\"}`, with `B2 -->|\"yes\"| C` and `B2 -->|\"no\"| B3[\"the file starts again from its first line in there; everything below happens in that second run\"]`, and `B3 --> C`."
      },
      {
        "severity": "minor",
        "section": 4,
        "kind": "diagram",
        "text": "In the picture only `pg_dump` gets its code looked at, so I finished section 4 thinking `-e` watches that one command. The prose next to it says `-e` \"stops the script at the first command that ends in failure\", any of them, and quiz q4 and q6 need the wider rule.",
        "quote": "D --> E{\"Did it end with 0?\"}",
        "fix": "Label the arrow into E so the picture states the general rule once: change the node text to \"Did it end with 0? (bash asks this after every command)\"."
      },
      {
        "severity": "minor",
        "section": 4,
        "kind": "gap",
        "text": "I was looking straight at `#!/usr/bin/env bash`, which plainly contains a folder, `/usr/bin`. The sentence says the file names no folder, so I stopped to work out whether I was misreading the line or the sentence. The intended point — that no folder for `bash` is written down — took me a second pass.",
        "quote": "so the file names no folder itself",
        "fix": "Add the missing two words: \"so the file names no folder for `bash` itself\"."
      },
      {
        "severity": "minor",
        "section": 4,
        "kind": "style",
        "text": "\"is judged\" has no one doing the judging, and this is the sentence I need for quiz q6, where one option says `set -e` stops the script at line 5. I worked it out from the clause before it, but only after re-reading.",
        "quote": "`-e` does not stop the script for a failure on the left of either; only the last command of such a line is judged.",
        "fix": "Name the actor: \"`-e` does not stop the script for a failure on the left of either; the only command on such a line that `-e` judges is the last one.\""
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "gap",
        "text": "Line 5 did five things in the paragraph I just read — a test, `||`, `exec`, `\"$0\"`/`\"$@\"`, `dirname` inside `$( … )` — so \"what line 5 did\" could be any of them. I guessed it means the `$( … )` brackets, because those are named just before, but I had to guess.",
        "quote": "Line 15, the `echo` holding `$( … )`, does what line 5 did: the `6` is what `grep -c` printed.",
        "fix": "Say which part: \"Line 15, the `echo` holding `$( … )`, uses those same brackets as line 5 did: the `6` is what `grep -c` printed.\""
      },
      {
        "severity": "minor",
        "section": 2,
        "kind": "term",
        "text": "\"lab box\" carries a lot here — line 5 exists for it, `/.dockerenv` is only in it, `exec` moves the run into it, and Try it says my files are written inside it — but the only thing I was ever told is that the same commands answer the same way there. I could follow every sentence, yet I never learned what kind of place it is or why my `/tmp` and its `/tmp` are different.",
        "quote": "In the lab box — the one `scripts/up.sh` starts for you — you type the `pg_dump` line",
        "fix": "Extend the dash clause with what a reader needs here: \"— the one `scripts/up.sh` starts for you, a separate place with its own files and its own `/tmp` —\"."
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "term",
        "text": "\"a build or deployment step\" is new to me, and it is the only support summary line 5 has. From the sentence I gather some program elsewhere runs scripts and reads their number, which is enough to accept the point, but not enough to picture what that program is or when I would meet one.",
        "quote": "your shell here, the program that runs a build or deployment step elsewhere",
        "fix": "Add four words of gloss: \"…the program that runs a build or deployment step elsewhere — a script someone else's machine runs for you\"."
      },
      {
        "severity": "minor",
        "section": 7,
        "kind": "gap",
        "text": "I typed a file's path as if it were a command, which is new: until now the first word of a line named a program. It works here, and the lesson never says why, so I do not know whether any file of commands I write would run the same way. Nothing in the lesson lets me find out.",
        "quote": "run `scripts/terminal/backup-db.sh`. On the next line run `echo $?`.",
        "fix": "Add one clause to step 1: \"run `scripts/terminal/backup-db.sh` — the file in the repository is already marked as one you may run, so its path is enough\"."
      },
      {
        "severity": "minor",
        "section": "quiz q2",
        "kind": "quiz",
        "text": "I answered before thinking. The same point is made four times before the quiz — in Core concepts, in the paragraph on the last two lines, in the third misconception, and in Try it's Expected result (\"That last `0` came from the empty `echo` above it, not from `pg_dump`\") — and option c repeats it in almost the same words. Nothing was tested but my memory of a sentence I had read minutes earlier.",
        "quote": "False, because that number reports on the command that finished immediately before it, which here printed the blank line",
        "fix": "Move the test one step away from the sentence: reword the statement so it is about a script where line 17 is `grep -c 'CREATE TABLE' \"$file\"` instead of a bare `echo`, and let option c say the number then reports on `grep`, still not on `pg_dump`."
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "length",
        "text": "One paragraph explains the `-f` test, `||`, `exec`, `\"$0\"`, `\"$@\"`, `dirname` and `$( … )` — seven new things, each in one clause, in six sentences. I read it three times and still had to come back to it while doing quiz q6.",
        "quote": "Line 5, the one beginning `[ -f /.dockerenv ]`, is what the previous lesson left for here.",
        "fix": "Split it in two: end the first paragraph after \"…in place of the run you started, not beside it.\", and start a new one with \"The rest of the line names what does the starting: …\"."
      }
    ],
    "readability": {
      "unknown_terms": [
        "lab box — used throughout and load-bearing for line 5; the only thing I was told is that commands answer the same way there",
        "pg_dump — new, but section 2 tells me in the same sentence what it does",
        "`/.dockerenv`, `lab-run.sh`, `scripts/up.sh` — file names I take on trust; the lesson says what each is for",
        "`exec`, `env`, `dirname`, `$0`, `$( … )`, `${1:-word}`, `mkdir -p`, the trailing `\\` — all new, all explained in place",
        "build step, deployment step — never explained (section 5, summary line 5, Connections)",
        "`CREATE TABLE`, \"the shape of the database\", `--host db` — passed over, but the lesson only needs a file and a count from them"
      ],
      "skipped_steps": [
        "Why the no-argument run in Try it does not die on `set -u` at line 7, when line 7 uses `$1` and I gave no `$1`.",
        "Whether the file starting again inside the lab box is a step or a question — the diagram shows a step, the prose says \"when the run is not there already\".",
        "Which of line 5's five parts \"does what line 5 did\" points back to, in the sentence about line 15.",
        "Why typing a file's path runs it, when until now the first word of a line named a program."
      ],
      "situation_connects": true,
      "try_it_feasible": true,
      "summary_matches": true
    },
    "verdict": "fix-required"
  }
}
