{
  "id": "foundation.l1.terminal-basics",
  "generated_by": "claude-opus-5",
  "generated_at": "2026-09-08T01:20:00+07:00",
  "claims": [
    {
      "n": 1,
      "text": "A shell reads a command line, splits it into words, treats the first word as the name of what to run, and passes the remaining words on as arguments; words holding no character the shell acts on (such as `*` or `>`) arrive at the program unchanged.",
      "kind": "fact",
      "source_hint": "Bash Reference Manual, 'Shell Operation' and 'Simple Commands'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 2,
      "text": "Before searching for a program file, the shell checks whether the first word names something it carries out itself (a shell built-in); in bash `cd` is a built-in, while `ls`, `cat`, `mkdir`, `cp`, `mv`, `rm`, `wc` and `head` are program files.",
      "kind": "behavior",
      "source_hint": "Bash Reference Manual, 'Command Search and Execution'; 'Shell Builtin Commands'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 3,
      "text": "`cd` is a shell built-in and changes the working directory of the shell itself; a separate program could change only its own working directory, which would be gone when it exits.",
      "kind": "behavior",
      "source_hint": "Bash Reference Manual, 'Bourne Shell Builtins' (cd); POSIX rationale for cd as a built-in",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 4,
      "text": "When the first word is not a built-in, the shell searches for an executable file of that name in a list of directories it keeps, starts it, and waits for it to finish before reading the next line.",
      "kind": "behavior",
      "source_hint": "Bash Reference Manual, 'Command Search and Execution'; 'Job Control' (foreground)",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 5,
      "text": "`pwd`, `ls`, `cd`, `cat`, `mkdir`, `cp`, `mv` and `rm` are standard on Linux and macOS machines and inside a bash terminal on Windows; they are separate programs or shell built-ins, not features of a particular terminal application.",
      "kind": "fact",
      "source_hint": "POSIX.1 Shell and Utilities, chapter 'Utilities' (ls, cat, mkdir, cp, mv, rm, pwd, cd); GNU coreutils manual",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 6,
      "text": "In bash, pressing Tab completes a partially typed name; if the text so far names nothing that can be completed, nothing is filled in.",
      "kind": "behavior",
      "source_hint": "Bash Reference Manual, 'Readline Interaction' / 'Commands For Completion' (complete, TAB)",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 7,
      "text": "Bash keeps a history of the lines entered in the session, and the Up arrow recalls previously entered lines.",
      "kind": "behavior",
      "source_hint": "Bash Reference Manual, 'Bash History Facilities'; Readline 'Commands For History' (previous-history)",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 8,
      "text": "`ls -1` prints one entry per line; when more than one directory is given as an operand, `ls` prints each with its name as a heading and separates the groups with a blank line.",
      "kind": "behavior",
      "source_hint": "POSIX.1 utility 'ls', options -1 and operand handling; GNU coreutils manual, 'ls invocation'",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 9,
      "text": "`wc -l db/schema.sql` prints the line count followed by the file name it was given; at tag stage-0 the captured output is `48 db/schema.sql`.",
      "kind": "number",
      "source_hint": "examples/don-hang outputs/stage-0/scripts/terminal/tour.txt; POSIX utility 'wc' output format",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 10,
      "text": "`head -2 <file>` prints the first two lines of that file and nothing else.",
      "kind": "behavior",
      "source_hint": "POSIX.1 utility 'head', option -n / obsolescent -number",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 11,
      "text": "`echo` with no arguments prints an empty line; the blank lines that separate the blocks of the captured output come from those calls, while the blank lines inside the `ls` listing are printed by `ls` between one directory's group and the next.",
      "kind": "behavior",
      "source_hint": "Bash Reference Manual, 'Bash Builtins' (echo); outputs/stage-0/scripts/terminal/tour.txt",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 12,
      "text": "`..` names the directory one level up, so `cd db/queries` followed by `cd ../..` returns the run to where it started, which the two `pwd` lines of the output show.",
      "kind": "behavior",
      "source_hint": "POSIX.1 Base Definitions, 'Pathname Resolution' (dot-dot); outputs/stage-0/scripts/terminal/tour.txt",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 13,
      "text": "`pwd` prints the working directory of the run that calls it.",
      "kind": "behavior",
      "source_hint": "POSIX.1 utility 'pwd'; Bash Reference Manual, 'Bourne Shell Builtins' (pwd)",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 14,
      "text": "In the example repository at tag stage-0, most lesson scripts under `scripts/` — including `scripts/terminal/tour.sh` — carry a line near the top that starts the same file again inside the lab box, so their output is the same on Windows, macOS, Linux and CI.",
      "kind": "fact",
      "source_hint": "examples/don-hang scripts/terminal/tour.sh line 5; scripts/lab-run.sh",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 15,
      "text": "The lab box that `scripts/up.sh` starts runs on the reader's own machine and has paths of its own; the example repository is made available inside it at `/repo`, which is why `tour.sh` prints `/repo` and not a path on the reader's machine.",
      "kind": "fact",
      "source_hint": "examples/don-hang docker-compose.yml, service `lab` (volume mount of the repository at /repo)",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 16,
      "text": "Installing Git on Windows adds a terminal window called Git Bash, which runs bash, so a reader on Windows has a shell of the kind this lesson describes without installing anything else.",
      "kind": "fact",
      "version_key": "git",
      "source_hint": "Git documentation / Git for Windows installation notes (Git Bash)",
      "needs_verification": true,
      "section": 7
    },
    {
      "n": 17,
      "text": "Opinion, stated with its condition: when you need to repeat what you did or hand it to someone else exactly, a typed line is easier to keep and send than a sequence of clicks.",
      "kind": "opinion",
      "needs_verification": false,
      "section": 6
    },
    {
      "n": 18,
      "text": "Opinion, stated with its condition: when a name refuses to complete on Tab, the most common cause is that the run is standing somewhere the name does not exist; for `cd` it can also mean the matching name is a file rather than a folder.",
      "kind": "opinion",
      "needs_verification": false,
      "section": 6
    },
    {
      "n": 19,
      "text": "Some words are changed by the shell before the program sees them: a word containing `*` is replaced with the file names it matches, and `>` is kept by the shell to send the output to a file instead of reaching the program as an argument.",
      "kind": "behavior",
      "source_hint": "Bash Reference Manual, 'Filename Expansion' and 'Redirections'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 20,
      "text": "When the first word of a command line contains a `/`, the shell does not search its list of directories: it runs exactly the file that path names, which is how `scripts/terminal/tour.sh` runs.",
      "kind": "behavior",
      "source_hint": "Bash Reference Manual, 'Command Search and Execution' (first step: a slash in the command name)",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 21,
      "text": "At tag stage-0, `scripts/up.sh` prints the line `The lab is up.` once the lab is started, so the reader can tell the lab box is ready before running a lesson script.",
      "kind": "fact",
      "source_hint": "examples/don-hang scripts/up.sh at stage-0 (echo lines after docker compose up --wait)",
      "needs_verification": true,
      "section": 7
    }
  ],
  "coverage": {
    "outline_items_covered": [1, 2, 3, 4],
    "outline_items_missing": [],
    "beyond_scope": [
      "Section 5 explains `wc -l` and `head -2` at option level because both appear in the code block; foundation.l1.pipes-and-filters reserves wc, head and the other filters, so that lesson should treat them as already met, not as new."
    ]
  },
  "self_check": {
    "one_concept": true,
    "vocab_all_defined": true,
    "code_from_repo": true,
    "no_external_urls": true,
    "word_count": 1478,
    "notes": "Review applied. Technical: built-in definition drops its false cause; `*` and `>` split (claim 19); blank lines split between `echo` and `ls` (claim 11); summary line 3 narrowed. Junior: bash named as the shell Try it opens, path-as-first-word rule added (claim 20), lab box put on the reader's own laptop (claim 15), `pwd`/`echo` parenthetical dropped. Block extended to lines 4-23 so the lab-box line shows. Cut the section 4 tail on later machines to stay under 300 words."
  },
  "open_questions": [
    "Technical major (section 4, kind version) asks for bash and coreutils keys in content/versions.yaml so claims 1-8, 10-13 and 19-21 get a version_key and claims 5, 6, 7, 18 stop being unverified. content/versions.yaml is a source of truth CLAUDE.md reserves for the owner, so `versions_used` still lists only `git`. Owner's call; same gap recorded for foundation.l1.files-and-permissions.",
    "Two review issues fall inside foundation.l1.terminal-basics.quiz.json, which /lesson does not write: technical major on q4 (option c and the correct-explanation clause still say every blank line comes from `echo`, which section 5 no longer says) and junior minor on q4 (options a-c repeat section 5 word for word). Run /quiz foundation.l1.terminal-basics apply-review, or the major stays open and the lesson cannot reach approved.",
    "Junior minor (section 7) asks to move the Git Bash sentence into 'Before you start'. That section takes exactly one bullet per prerequisite and nothing else (prompt 02 hard constraints), so the sentence stays in step 1, marked as one-time setup outside the three minutes. Step 2 now names the line `The lab is up.` as the signal the lab box is ready (claim 21).",
    "Technical minor (section 5, kind scope) on `wc -l` and `head -2` needs no lesson change: both are in the pinned block, and coverage.beyond_scope already records the overlap for foundation.l1.pipes-and-filters."
  ]
}
