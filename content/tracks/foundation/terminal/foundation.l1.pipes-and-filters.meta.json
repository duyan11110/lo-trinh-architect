{
  "id": "foundation.l1.pipes-and-filters",
  "generated_by": "claude-opus-5",
  "generated_at": "2026-09-08T09:40:00+07:00",
  "claims": [
    {
      "n": 1,
      "text": "The shell gives every command it starts three standard streams already connected: standard input, standard output and standard error.",
      "kind": "fact",
      "source_hint": "POSIX.1 Base Definitions, 'Standard I/O Streams'; Bash Reference Manual, 'Redirections'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 2,
      "text": "When a command is run from an interactive shell with no redirection, standard output and standard error are both attached to the terminal, so ordinary results and failure messages appear together on screen.",
      "kind": "behavior",
      "source_hint": "Bash Reference Manual, 'Redirections' (default file descriptors 0, 1, 2 of a simple command)",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 3,
      "text": "`|` between two commands connects the standard output of the left command to the standard input of the right command; no temporary file is involved.",
      "kind": "syntax",
      "source_hint": "Bash Reference Manual, 'Pipelines'; POSIX.1 Shell Command Language, 'Pipelines'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 4,
      "text": "The commands of a pipeline are started at the same time, each in its own process, and run concurrently; the right-hand command reads lines while the left-hand one is still writing them, so output can appear before the left command has finished.",
      "kind": "behavior",
      "source_hint": "Bash Reference Manual, 'Pipelines' (each command in a separate subshell, executed concurrently); POSIX.1 Shell Command Language, 'Pipelines'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 5,
      "text": "A program reading standard input does not have to be told where the lines come from: `grep`, `head` and `wc` read a file, a terminal and a pipe the same way.",
      "kind": "fact",
      "source_hint": "POSIX.1 Base Definitions, 'Standard I/O Streams'; grep/head utility descriptions (operand omitted means standard input)",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 6,
      "text": "`grep PATTERN FILE` writes the lines of FILE that contain PATTERN to standard output and drops the others.",
      "kind": "behavior",
      "source_hint": "POSIX.1 utility 'grep', description and stdout section; GNU Grep manual, 'Invoking grep'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 7,
      "text": "`cut -d' ' -f4-` cuts each input line at every single space and writes the fourth field through the last; on the log lines of the example repository this drops the date, the time and the word `ERROR`, leaving the message.",
      "kind": "syntax",
      "source_hint": "POSIX.1 utility 'cut', options -d and -f (range 'n-'); GNU coreutils manual, 'cut invocation'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 8,
      "text": "`uniq -c` collapses only lines that are adjacent and equal, and prefixes each written line with the number of occurrences; this is why `sort` has to run before it for a count over a whole file.",
      "kind": "behavior",
      "source_hint": "POSIX.1 utility 'uniq', option -c and 'repeated lines in the input shall not be detected if they are not adjacent'; GNU coreutils, 'uniq invocation'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 9,
      "text": "`sort -n` compares lines by the numeric value at the start of the line and `-r` reverses the order, so `sort -rn` puts the largest count first.",
      "kind": "syntax",
      "source_hint": "POSIX.1 utility 'sort', options -n and -r; GNU coreutils manual, 'sort invocation'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 10,
      "text": "`> file` sends standard output to that file and truncates it to zero length first; `>> file` sends standard output to the file and adds after what is already there.",
      "kind": "syntax",
      "source_hint": "Bash Reference Manual, 'Redirecting Output' and 'Appending Redirected Output'; POSIX.1 Shell Command Language, 'Redirection'",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 11,
      "text": "`>` redirects standard output only, so a failure message written to standard error still reaches the terminal; `2> file` is the redirection that captures standard error, the `2` being the descriptor number standard error carries in both `2>` and `>&2`.",
      "kind": "syntax",
      "source_hint": "Bash Reference Manual, 'Redirections' (file descriptor 1 is the default for >; 2> names descriptor 2)",
      "needs_verification": true,
      "section": 4
    },
    {
      "n": 12,
      "text": "`>&2` in `echo \"...\" >&2` makes that echo write to standard error instead of standard output.",
      "kind": "syntax",
      "source_hint": "Bash Reference Manual, 'Duplicating File Descriptors' (>&word)",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 13,
      "text": "`grep -c` writes the number of matching lines, not the number of matches within them; in the captured output of `scripts/terminal/pipes.sh` at stage-0 a six-line file gives `3`.",
      "kind": "behavior",
      "source_hint": "POSIX.1 utility 'grep', option -c ('write only a count of selected lines'); outputs/stage-0/scripts/terminal/pipes.txt",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 14,
      "text": "In the captured output of `scripts/terminal/pipes.sh` at stage-0, `ls /nowhere` writes `ls: cannot access '/nowhere': No such file or directory` to standard error, `2> errors.txt` puts it in that file, and `cat errors.txt` prints it back.",
      "kind": "behavior",
      "source_hint": "examples/don-hang outputs/stage-0/scripts/terminal/pipes.txt; POSIX.1 utility 'ls' (diagnostic messages on standard error)",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 15,
      "text": "`wc -l` writes the number of lines of its input, and writes the file name beside the count when the file arrives as an operand; `wc -l < file` gives the file through standard input instead, so only the count appears. At stage-0 `lab/app.log` gives `37`.",
      "kind": "number",
      "source_hint": "POSIX.1 utility 'wc' (output format with and without a file operand); outputs/stage-0/scripts/terminal/find-errors-in-log.txt",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 16,
      "text": "At stage-0, `lab/app.log` holds 37 lines, of which 6 contain `ERROR` and 5 contain `WARN`.",
      "kind": "number",
      "source_hint": "examples/don-hang lab/app.log at stage-0; outputs/stage-0/scripts/terminal/find-errors-in-log.txt",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 17,
      "text": "`grep -n` prefixes each written line with its line number in the input file; at stage-0 the first three `ERROR` lines of `lab/app.log` are lines 6, 8 and 16.",
      "kind": "number",
      "source_hint": "POSIX.1 utility 'grep', option -n; outputs/stage-0/scripts/terminal/find-errors-in-log.txt",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 18,
      "text": "`head -3` writes the first three lines of its input and `tail -1` the last one.",
      "kind": "syntax",
      "source_hint": "POSIX.1 utilities 'head' and 'tail', option -n / obsolescent -number",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 19,
      "text": "At stage-0 the counting chain over `lab/app.log` reports the payment timeout message 3 times, the failed notification email 2 times and the database deadlock once.",
      "kind": "number",
      "source_hint": "examples/don-hang outputs/stage-0/scripts/terminal/find-errors-in-log.txt",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 20,
      "text": "At stage-0, `grep WARN lab/app.log | cut -d' ' -f4- | sort | uniq -c | sort -rn` prints four lines: the message about the payment gateway being slow with a count of 2, then three messages with a count of 1 each.",
      "kind": "number",
      "source_hint": "examples/don-hang lab/app.log at stage-0 (run the chain against it); outputs/stage-0/scripts/terminal/find-errors-in-log.txt for the ERROR equivalent",
      "needs_verification": true,
      "section": 7
    },
    {
      "n": 21,
      "text": "`grep PATTERN missing.log` with no such file writes a diagnostic naming the file to standard error and creates nothing on standard output, so `> found.txt` leaves an empty file.",
      "kind": "behavior",
      "source_hint": "POSIX.1 utility 'grep', 'Standard Error' and exit status 2; Bash Reference Manual, 'Redirecting Output' (the file is created and truncated before the command runs)",
      "needs_verification": true,
      "section": 7
    },
    {
      "n": 22,
      "text": "In the example repository at stage-0, line 5 of `scripts/terminal/find-errors-in-log.sh` (the second line of the quoted block) tests whether the run is already inside the lab box and, if not, starts the same file again in there, where the repository is at `/repo`.",
      "kind": "fact",
      "source_hint": "examples/don-hang scripts/terminal/find-errors-in-log.sh line 5, scripts/lab-run.sh, docker-compose.yml service `lab`",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 23,
      "text": "In `scripts/terminal/pipes.sh` at stage-0, `cd /tmp` moves the run to a scratch folder and `printf 'a\\nb\\na\\nc\\na\\nb\\n' > letters.txt` writes a six-line file there holding three `a`, two `b` and one `c`; `printf` spells the line ending as `\\n` where `echo` adds it.",
      "kind": "syntax",
      "source_hint": "examples/don-hang scripts/terminal/pipes.sh lines 7-8 at stage-0; POSIX.1 utilities 'printf' and 'echo'",
      "needs_verification": true,
      "section": 5
    },
    {
      "n": 24,
      "text": "When a running system misbehaves and its log is reachable, searching that log with `grep` and `tail` is usually where you start, before installing or opening anything else.",
      "kind": "opinion",
      "source_hint": "no document expected; condition stated in the sentence (outline item 4 of the lesson entry in track.yaml)",
      "needs_verification": false,
      "section": 5
    },
    {
      "n": 25,
      "text": "An assignment `name=value` gives a value a name in the shell, and `$name` in quotes is replaced by that value wherever it is written.",
      "kind": "syntax",
      "source_hint": "POSIX.1 Shell Command Language, 'Variable Assignment' and 'Parameter Expansion'; Bash Reference Manual, 'Shell Parameters'",
      "needs_verification": true,
      "section": 5
    }
  ],
  "coverage": {
    "outline_items_covered": [1, 2, 3, 4],
    "outline_items_missing": [],
    "beyond_scope": [
      "Section 5 names `||` once to say the reader will meet it in foundation.l1.shell-scripts; exit codes are not explained here.",
      "`cut` is named in Core concepts and used in the counting chain because the example script uses it; the lesson explains only `-d` and `-f4-`, not fields in general.",
      "Section 5 says in one clause what the lab-box line does and that `log=...` names a path; both are foundation.l1.shell-scripts material and are pointed there."
    ]
  },
  "self_check": {
    "one_concept": true,
    "vocab_all_defined": true,
    "code_from_repo": true,
    "no_external_urls": true,
    "word_count": 1570,
    "notes": "Round 2 of apply-review: 23 of 24 claims verified, none wrong; the unverified one is claim 24 (opinion) and its hedge is now in the body too. Applied 6 in-lesson minors (technical, the 7th deferred) and 1 major + 8 in-lesson minors (junior). Claim 11 extended with the descriptor number, claim 15 with what wc counts and prints, claim 24 reworded to match the body; claim 25 added for name=value (technical ledger gap). 1439 to 1570 words; section 4 at 283 (150-300)."
  },
  "open_questions": [
    "versions_used is empty: the lesson depends on bash and the standard utilities (grep, sort, uniq, cut, head, tail, wc), which content/versions.yaml does not pin, so claims 1-11, 13, 15, 17, 18, 21 and 25 carry no version_key. Same gap already recorded for foundation.l1.terminal-basics and foundation.l1.files-and-permissions; adding bash/coreutils keys is the owner's call. Technical's section 5 `kind: version` minor asks for the same pin, since the quoted `ls` diagnostic is the lab box's GNU coreutils wording; the lesson now says the script runs inside the lab box, which is the part `/lesson` can fix.",
    "Four review issues land in `foundation.l1.pipes-and-filters.quiz.json`, which `/lesson` does not write: a technical blocker and a junior major on q4 option b (its premise — that letters.txt holds more than three `a` characters — is false: `printf 'a\\nb\\na\\nc\\na\\nb\\n'` gives exactly three, so both readings of `grep -c` give 3), junior minors on q3 (option a repeats the Try it expected result) and q5 (option a repeats section 5's sentence rearranged). Run `/quiz foundation.l1.pipes-and-filters apply-review`, otherwise the blocker survives and the lesson cannot reach `approved` (R03). Section 5 now carries the clause that makes the distinction visible ('a line reading `aaa` would still add only 1'), which is the half of that pair `/lesson` owns.",
    "Technical's section 3 `kind: scope` minor asks to add `cut` to outline item 2 of foundation.l1.pipes-and-filters in content/tracks/foundation/track.yaml, because the outline's own example_files pin find-errors-in-log.sh whose chain cannot be read without it. The reviewer says explicitly not to remove `cut` from the lesson, so nothing changed here; editing track.yaml is a source-of-truth change reserved for the owner (CLAUDE.md rule 2), same family as the versions.yaml request above."
  ]
}
