{
  "id": "foundation.l1.ip-and-ports",
  "generated_by": "claude-opus-5",
  "generated_at": "2026-09-08T00:20:00+07:00",
  "claims": [
    {
      "n": 1,
      "text": "An IP address names a machine's place on a network and one machine can have more than one; the older, still common form writes it as four numbers from 0 to 255 separated by dots.",
      "kind": "fact",
      "section": 3,
      "source_hint": "RFC 791 section 2.3 (addressing) and RFC 1122 section 3.2.1 — 32-bit IPv4 addresses, dotted-decimal notation",
      "needs_verification": true
    },
    {
      "n": 2,
      "text": "A port number runs from 0 to 65535.",
      "kind": "number",
      "section": 3,
      "source_hint": "RFC 9293 section 3.1 — the 16-bit Source Port and Destination Port fields of the TCP header",
      "needs_verification": true
    },
    {
      "n": 3,
      "text": "A loopback address is one every machine has for itself and data addressed to it never leaves that machine; in the four-number form it is `127.0.0.1`.",
      "kind": "fact",
      "section": 3,
      "source_hint": "RFC 1122 section 3.2.1.3 (g) — 127.0.0.0/8 is the internal host loopback address, must never appear outside a host",
      "needs_verification": true
    },
    {
      "n": 4,
      "text": "The name `localhost` stands for the loopback address, and therefore means the machine running the command rather than any fixed machine.",
      "kind": "fact",
      "section": 3,
      "source_hint": "RFC 6761 section 6.3 — the special-use domain name localhost resolves to the loopback address",
      "needs_verification": true
    },
    {
      "n": 5,
      "text": "The private ranges are those starting `10.`, `192.168.`, and `172.16` through `172.31`; they are meant for use inside one network and machines on the public internet cannot reach them. The lab's `172.28.0.x` numbers are inside such a range.",
      "kind": "fact",
      "section": 3,
      "source_hint": "RFC 1918 section 3 — private address space 10/8, 172.16/12, 192.168/16, not routed on the public internet",
      "needs_verification": true
    },
    {
      "n": 6,
      "text": "At tag stage-0 the lab box has the fixed address `172.28.0.12` and the database machine `172.28.0.11`, set in `docker-compose.yml` on a network whose block is `172.28.0.0/24`.",
      "kind": "behavior",
      "version_key": "docker",
      "section": 4,
      "source_hint": "examples/don-hang docker-compose.yml at stage-0 (networks.donhang.ipam, services lab/db ipv4_address); Compose file reference, networks key",
      "needs_verification": true
    },
    {
      "n": 7,
      "text": "A process claims a port by asking the operating system for it and then listening on it; the operating system gives the port to the first process that asks and refuses every later program asking for that same number on that same address while the first still holds it.",
      "kind": "behavior",
      "section": 4,
      "source_hint": "POSIX bind() — EADDRINUSE: the specified address is already in use; listen() on a bound socket",
      "needs_verification": true
    },
    {
      "n": 8,
      "text": "One process can hold several ports at the same time: at stage-0 Caddy, the program serving the Đơn Hàng site, holds both 8080 and 8443.",
      "kind": "behavior",
      "version_key": "docker",
      "section": 4,
      "source_hint": "examples/don-hang Caddyfile at stage-0 (the :8080 and donhang.local:8443 sites) and outputs/stage-0/scripts/network/who-listens.txt",
      "needs_verification": true
    },
    {
      "n": 9,
      "text": "The port a process held can be claimed by another program once that process is gone; the number is held, not owned.",
      "kind": "behavior",
      "section": 4,
      "source_hint": "POSIX close()/bind() — the address bound to a socket is released when the socket is closed",
      "needs_verification": true
    },
    {
      "n": 10,
      "text": "At stage-0 the SSH server of the lab box listens on 2222, Caddy on 8080 and 8443, and nothing on that box listens on 9999.",
      "kind": "behavior",
      "version_key": "docker",
      "section": 5,
      "source_hint": "outputs/stage-0/scripts/network/who-listens.txt in the example repository; docker-compose.yml service lab (LISTEN_PORT 2222) and web",
      "needs_verification": true
    },
    {
      "n": 11,
      "text": "`nc -z` reports only whether something is there on the given machine and port.",
      "kind": "behavior",
      "section": 5,
      "source_hint": "nc(1) man page — the -z flag, zero-I/O mode, used for scanning listeners; outputs/stage-0/scripts/network/who-listens.txt (each target prints open or closed)",
      "needs_verification": true
    },
    {
      "n": 12,
      "text": "`db:5432` is open while `db` is not the lab box: at stage-0 the database runs on another machine of the lab network, `172.28.0.11`. The listing printed by the script is narrowed by `grep` to 2222, 8080 and 8443, so it says nothing about 5432 either way.",
      "kind": "behavior",
      "version_key": "docker",
      "section": 5,
      "source_hint": "examples/don-hang scripts/network/who-listens.sh line 19 (the grep filter) and docker-compose.yml services lab/db at stage-0",
      "needs_verification": true
    },
    {
      "n": 13,
      "text": "In the listening list, `0.0.0.0` in front of a port means every IPv4 address the machine has, not one particular address.",
      "kind": "fact",
      "section": 5,
      "source_hint": "RFC 1122 section 3.2.1.3 (a) — 0.0.0.0 as the unspecified/wildcard address; netstat(8) Local Address column",
      "needs_verification": true
    },
    {
      "n": 14,
      "text": "The listening entries written with colons are IPv6, a second family of addresses beside the four-number form.",
      "kind": "fact",
      "section": 5,
      "source_hint": "RFC 4291 sections 2.2 and 2.5.2 — IPv6 text representation and the unspecified address ::",
      "needs_verification": true
    },
    {
      "n": 15,
      "text": "In `two-servers.sh` two `nc` runs hold 9001 and 9002 at the same time, and a third asking for 2222 is refused with `nc: Address in use` and exit code 1.",
      "kind": "behavior",
      "section": 5,
      "source_hint": "outputs/stage-0/scripts/network/two-servers.txt in the example repository, captured by scripts/capture-output.sh",
      "needs_verification": true
    },
    {
      "n": 16,
      "text": "The text of a refusal such as `Address in use` is chosen by the program that prints it, not by the operating system, so another program may write `address already in use` for the same condition.",
      "kind": "fact",
      "section": 5,
      "source_hint": "POSIX bind() EADDRINUSE — the standard names the error condition, not the message text a program prints for it",
      "needs_verification": true
    },
    {
      "n": 17,
      "text": "A request for a port on a machine where no process is listening is turned away at once rather than left unanswered, which is why `nc -z -w 3` reports `localhost:9999 is closed` without waiting out its three seconds.",
      "kind": "behavior",
      "section": 5,
      "source_hint": "RFC 9293 section 3.5.2 — if the connection does not exist (CLOSED), a reset is sent in response to any incoming segment; outputs/stage-0/scripts/network/who-listens.txt",
      "needs_verification": true
    },
    {
      "n": 18,
      "text": "Inside the lab box nothing listens on 5432, so `localhost:5432` finds nothing there, while `db:5432` reaches the database machine `172.28.0.11`.",
      "kind": "behavior",
      "version_key": "docker",
      "section": 6,
      "source_hint": "examples/don-hang docker-compose.yml at stage-0 — db is a separate service with its own address; the lab box shares its network namespace only with the web service (network_mode: service:lab)",
      "needs_verification": true
    },
    {
      "n": 19,
      "text": "`db` is a name the lab gives the database machine, and inside the lab network it stands for that machine's address.",
      "kind": "behavior",
      "version_key": "docker",
      "section": 5,
      "source_hint": "Compose file reference, networks key — a service is reachable by its service name on the networks it joins; examples/don-hang docker-compose.yml at stage-0",
      "needs_verification": true
    }
  ],
  "coverage": {
    "outline_items_covered": [1, 2, 3, 4],
    "outline_items_missing": [],
    "beyond_scope": []
  },
  "self_check": {
    "one_concept": true,
    "vocab_all_defined": true,
    "code_from_repo": true,
    "no_external_urls": true,
    "word_count": 1401,
    "notes": "apply-review round 1: 2 technical major (the 5432 argument rested on a grep filter; localhost:5432 now claim 18), 8 technical minor, 8 junior major (Caddy and SSH named, sockets said as ports, netstat explained, routed replaced, five numbers dropped, db as a name), 4 junior minor. Verdicts 1, 7, 12, 13, 17 took suggested_text; 11 narrowed to its verified half; 16 rewritten as principle. Section 4 prose 291 words after trimming three sentences the review did not name."
  },
  "open_questions": [
    "The three .quiz.json issues (technical minor q1 stem, junior major q4 option a citing stage-0, junior minor q2 repeating a Beginners-often-think bullet) were applied by /quiz foundation.l1.ip-and-ports apply-review; q3 option a and q7 option a were reworded in the same pass to match the lesson's new \"turned away at once\" and \"no way to reach\" wording.",
    "Junior minor asked for one clause naming `tcp` at the start of every netstat line. `tcp` is a glossary term introduced in foundation.l1.tcp-vs-udp, so it is outside known_vocab ∪ vocab and cannot appear in prose; it stays in the output block only. Same shape as the `shell` case in foundation.l1.env-and-config.",
    "Claim 9 (a port can be claimed again once the process is gone) was unverified: no script in the repository releases and re-claims a port. Kept as written because the rule to rewrite at principle level applies to number/syntax claims, and outline item 2 needs the point; a reviewer with a socket reference should check whether it needs a wait condition.",
    "Claims 2, 3, 4, 5, 13, 14, 17 point at RFCs not in refs/rfc/ (only 9110/9111/9112/6265 were fetched); round 1 verified them by WebFetch on rfc-editor.org."
  ]
}
