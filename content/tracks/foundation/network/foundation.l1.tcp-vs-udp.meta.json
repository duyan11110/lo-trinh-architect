{
  "id": "foundation.l1.tcp-vs-udp",
  "generated_by": "claude-opus-5",
  "generated_at": "2026-09-08T21:40:00+07:00",
  "claims": [
    {
      "n": 1,
      "text": "TCP moves bytes between two ports only after both ends have set up a connection; while that connection lasts, every byte arrives once, in the order it was sent.",
      "kind": "behavior",
      "section": 3,
      "source_hint": "RFC 9293 section 2.2 — TCP provides a reliable, in-order, byte-stream service; section 3.5.2 for a connection ending under bytes still in flight",
      "needs_verification": true
    },
    {
      "n": 2,
      "text": "A connection exists as state remembered at both ends, not as something the network holds, and it lasts until each side has said it is finished.",
      "kind": "behavior",
      "section": 3,
      "source_hint": "RFC 9293 section 3.3.1 (Transmission Control Block is per-connection state at each end); section 3.6 (CLOSE is simplex); RFC 791 section 1.4 (IP has no connections)",
      "needs_verification": true
    },
    {
      "n": 3,
      "text": "Setting up a TCP connection takes three short messages: one side offers, the other accepts and offers back, the first accepts in turn.",
      "kind": "behavior",
      "section": 3,
      "source_hint": "RFC 9293 section 3.5 — the three-way handshake (SYN, SYN-ACK, ACK)",
      "needs_verification": true
    },
    {
      "n": 4,
      "text": "The network carries each packet on its own and may lose it, deliver a copy of it, or deliver it out of the order it was sent.",
      "kind": "behavior",
      "section": 3,
      "source_hint": "RFC 791 section 1.4 — each datagram is an independent entity, no acknowledgments, no retransmissions; RFC 768 for duplicates",
      "needs_verification": true
    },
    {
      "n": 5,
      "text": "UDP sends each packet on its own with no connection set up beforehand and no promise that the packet arrives, arrives once, or arrives in turn.",
      "kind": "behavior",
      "section": 3,
      "source_hint": "RFC 768 — the protocol is transaction oriented, delivery and duplicate protection are not guaranteed",
      "needs_verification": true
    },
    {
      "n": 6,
      "text": "Your data waits for the other side's answer to the first handshake message only: the third message can travel out together with the first data, so opening a connection costs one trip out and back.",
      "kind": "behavior",
      "section": 4,
      "source_hint": "RFC 9293 section 3.5, Figure 6 — the initiator reaches ESTABLISHED on the peer's SYN-ACK, and the figure's third segment carries <DATA>",
      "needs_verification": true
    },
    {
      "n": 7,
      "text": "Each side of a connection remembers four numbers — its own address and port, and the other side's address and port — and that set of four is what names the connection.",
      "kind": "fact",
      "section": 4,
      "source_hint": "RFC 9293 section 3.4.1 (a connection is defined by a pair of sockets) and 3.3.1 (the TCB holds local and remote addresses and ports)",
      "needs_verification": true
    },
    {
      "n": 8,
      "text": "One listening port serves many connections at the same time, because each asking side brings a port number of its own and no two connections then carry the same four numbers.",
      "kind": "behavior",
      "section": 4,
      "source_hint": "RFC 9293 section 3.4.1; RFC 9112 section 9 — servers maintain thousands of concurrent connections (refs/rfc/rfc9112.txt lines 1296-1304)",
      "needs_verification": true
    },
    {
      "n": 9,
      "text": "TCP numbers every lump of bytes it sends; the receiving side puts them back in order and says which ones it has, and the sending side sends again anything it does not hear about.",
      "kind": "behavior",
      "section": 4,
      "source_hint": "RFC 9293 section 2.2 (loss detected via sequence numbers), 3.8 (the sender tracks the oldest unacknowledged number and retransmits)",
      "needs_verification": true
    },
    {
      "n": 10,
      "text": "UDP numbers nothing and sends nothing again, and the sending side is never told that a packet was lost.",
      "kind": "behavior",
      "section": 4,
      "source_hint": "RFC 768 — the UDP header carries no sequence number and the protocol defines no acknowledgement or retransmission; RFC 791 section 1.4",
      "needs_verification": true
    },
    {
      "n": 11,
      "text": "`nc -z` asks for a connection without sending data of its own, prints a line when it gets one, and says nothing when it does not — the failure shows only in its exit code.",
      "kind": "behavior",
      "section": 5,
      "source_hint": "outputs/stage-0/scripts/network/tcp-connect.txt at tag stage-0: an nc line appears only for the two successes; attempts 2 and 3 print only the script's own echo",
      "needs_verification": true
    },
    {
      "n": 13,
      "text": "In the lab, `nc -z -w 3 localhost 8080` reaches Caddy and prints `Connection to localhost (::1) 8080 port [tcp/http-alt] succeeded!`, in which `[tcp/http-alt]` is `nc`'s own name for port 8080.",
      "kind": "behavior",
      "section": 5,
      "version_key": "caddy",
      "source_hint": "Run `scripts/network/tcp-connect.sh` at tag stage-0; compare with `outputs/stage-0/scripts/network/tcp-connect.txt` lines 1-3",
      "needs_verification": true
    },
    {
      "n": 14,
      "text": "`nc -z -w 3 localhost 9999` gets a reset rather than a connection, because the machine is up but holds nothing on that port; a reset is an answer, so the attempt ends without waiting for any limit, and the script's `$?` prints exit code 1.",
      "kind": "behavior",
      "section": 5,
      "source_hint": "RFC 9293 section 3.5.2 — a segment for a connection that does not exist is answered with a reset; outputs/stage-0/scripts/network/tcp-connect.txt lines 5-6 and who-listens.txt",
      "needs_verification": true
    },
    {
      "n": 15,
      "text": "`nc -z -w 3 203.0.113.1 80` gets no reply of any kind, so it ends only when the limit set by `-w 3` runs out, with the same exit code 1 as the refusal; that limit is what stops the attempt waiting for ever.",
      "kind": "behavior",
      "section": 5,
      "source_hint": "outputs/stage-0/scripts/network/tcp-connect.txt lines 8-9 — the third attempt ends and the script goes on; the transcript records no timing, so no unit is claimed",
      "needs_verification": true
    },
    {
      "n": 16,
      "text": "`203.0.113.1` lies in a range set aside for writing examples rather than for real machines.",
      "kind": "fact",
      "section": 5,
      "source_hint": "RFC 5737 section 3 — 203.0.113.0/24 (TEST-NET-3) is reserved for use in documentation and should not appear on the public Internet",
      "needs_verification": true
    },
    {
      "n": 17,
      "text": "Every command finishes with a number, 0 when it worked and something else when it did not, and `$?` prints it; joined with `&&`, the second command runs only when the first gave 0, and joined with `||`, only when it did not.",
      "kind": "syntax",
      "section": 5,
      "source_hint": "GNU Bash manual sections 3.2.3 (lists of commands) and 3.4.2 (`$?` holds the exit status of the most recent pipeline); outputs/stage-0/scripts/network/tcp-connect.txt",
      "needs_verification": true
    },
    {
      "n": 18,
      "text": "`::1` is how a machine names itself in the newer, longer form of address, so a connection that reaches it never leaves the machine.",
      "kind": "fact",
      "section": 5,
      "source_hint": "RFC 4291 section 2.5.3 — 0:0:0:0:0:0:0:1, written `::1`, is the loopback address a node uses to send to itself",
      "needs_verification": true
    },
    {
      "n": 19,
      "text": "HTTP as you meet it here travels over a TCP connection, so a page opened at `localhost:8080` needed the three handshake messages before anything else moved.",
      "kind": "fact",
      "section": 5,
      "version_key": "http",
      "source_hint": "refs/rfc/rfc9112.txt section 9 (lines 1282-1294) — the `http` URI scheme indicates a default connection of TCP over IP",
      "needs_verification": true
    },
    {
      "n": 20,
      "text": "Encryption is added on top of an established TCP connection rather than replacing the handshake that opened it.",
      "kind": "fact",
      "section": 5,
      "source_hint": "RFC 8446 section 1 — TLS asks of the transport only a reliable, in-order stream, which the TCP connection must already provide",
      "needs_verification": true
    },
    {
      "n": 21,
      "text": "A refusal is an answer saying nothing holds that port, so it tells you the machine is up; a timeout is the absence of any answer, so the address may be wrong or something in between is dropping packets.",
      "kind": "behavior",
      "section": 6,
      "source_hint": "RFC 9293 section 3.5.2 (a request for a port with no listener is answered with a reset); refs/kubernetes-website .../kubeadm/high-availability.md:149-155",
      "needs_verification": true
    },
    {
      "n": 22,
      "text": "A name lookup goes out over UDP and asks a second time if nothing comes back, instead of setting up a connection to carry one short question.",
      "kind": "behavior",
      "section": 6,
      "source_hint": "RFC 1035 sections 4.2 and 4.2.1 — UDP on port 53 is the recommended method for standard queries, and lost queries require a retransmission strategy",
      "needs_verification": true
    },
    {
      "n": 23,
      "text": "Leaving out the handshake and the sending-again is worth it when setting up a connection costs more than the question is worth and the asker can simply ask again, which is why a name lookup goes over UDP.",
      "kind": "opinion",
      "section": 6,
      "source_hint": "RFC 768 (applications wanting ordered reliable delivery should use TCP) and RFC 1035 section 4.2.1 (UDP recommended for standard queries, with retransmission)",
      "needs_verification": false
    },
    {
      "n": 24,
      "text": "Closing a TCP connection is per direction: one side says it is finished, and the connection is closed only once the other side has said the same.",
      "kind": "behavior",
      "section": 4,
      "source_hint": "RFC 9293 section 3.6 — each side sends its own FIN and has it acknowledged; the peer stays half-closed until its own application closes",
      "needs_verification": true
    },
    {
      "n": 26,
      "text": "The side that opens a connection normally leaves its own port number to the operating system, which hands the program a free one each time; a program can ask for a particular one, but few do.",
      "kind": "behavior",
      "section": 4,
      "source_hint": "RFC 6056 sections 1 and 2 — the client's port is normally left unspecified and chosen by the networking stack, though some applications bind a specific one; 51000 is in 49152-65535",
      "needs_verification": true
    },
    {
      "n": 27,
      "text": "The second line of the quoted block re-runs `scripts/network/tcp-connect.sh` inside the lab box, so the reader runs the script the same way on any machine.",
      "kind": "behavior",
      "section": 5,
      "version_key": "docker",
      "source_hint": "examples/don-hang at stage-0: scripts/network/tcp-connect.sh line 5 and scripts/lab-run.sh — the guard on `/.dockerenv` re-execs the script through `docker compose exec lab`",
      "needs_verification": true
    }
  ],
  "coverage": {
    "outline_items_covered": [1, 2, 3, 4],
    "outline_items_missing": [],
    "beyond_scope": [
      "Says `203.0.113.1` sits in a range set aside for examples, so the third attempt's silence is not read as a broken lab.",
      "Names `::1` as the newer, longer form of a machine's own address and `[tcp/http-alt]` as `nc`'s name for 8080, to explain a line the captured output prints.",
      "Says in one sentence that every command finishes with a number, that `$?` prints it, and what `&&` and `||` do with it, so the transcript's `exit code 1` and both failure lines can be read (junior review, section 5).",
      "Says the script's second line re-runs it inside the lab box, so the reader does not stop on a line the lesson never teaches (junior review, section 5).",
      "Says the operating system normally hands the asking side a free port number, because the four numbers cannot be explained without saying where the reader's own number comes from (section 4)."
    ]
  },
  "self_check": {
    "one_concept": true,
    "vocab_all_defined": true,
    "code_from_repo": true,
    "no_external_urls": true,
    "word_count": 1594,
    "notes": "Round 2 of apply-review: 1 major, 7 minor (technical), 5 major, 9 minor (junior); no blocker, no `wrong` verdict. Claims 2, 14, 18, 26 took `suggested_text`; message 3 carrying first data merged into claim 6, `||` into 17, `[tcp/http-alt]` into 13; claim 25 (8443) went with the sentence junior called a dead end and claim 27 (the lab-box line) took its slot, holding 25. Seven sentences the review did not ask about were cut as the fixes went in."
  },
  "open_questions": [
    "`/quiz foundation.l1.tcp-vs-udp apply-review` ran and closed all four items of the round: q1 option a and its correct explanation now say the connection lasts until both ends have said they are finished (key stays [a]); q3 explanation b now says a refusal points at the port rather than the address; q6 option b now names a consequence (the exit codes alone cannot say which failure was refused) instead of repeating the lesson's sentence, and its correct explanation follows; q6 option a now sets the finished handshake beside `nc` naming the port instead of making it the cause. The technical review's q2 fix stayed unapplied on purpose: the body no longer mentions 8443, so `Caddy keeps the one port` is no longer contradicted. No claim or body sentence changed.",
    "Junior minor asks for every `Core concepts` entry to be bolded, not only `tcp`. Prompt 02 reserves bold for `vocab` terms and the quoted belief in section 6, so the fix is refused on purpose — same handling as recorded for `foundation.l1.files-and-permissions`. Unchanged from round 1.",
    "Technical minor (`kind: version`) asks the owner to add `standards: tcp/udp/ip` to `content/versions.yaml`, mirror RFC 9293/768/791 into `refs/rfc/`, set `version_key` on claims 1-10 and 21, and pin the lab box's tool set. CLAUDE.md reserves the source-of-truth files for the owner — same as the `standards: dns` request on `foundation.l1.dns`.",
    "The Connections gap was fixed by adding `[[foundation.l1.dns]]`, but the review also asks for `foundation.l1.dns` in this lesson's `related` in `track.yaml`. `related` is outline data, so the owner or `/outline` makes that change; the lesson's frontmatter still copies `related: []` from the outline.",
    "Claims 11 and 15 stay `unverified` (`kind: behavior`, so not a gate under D1): nothing in `refs/` documents `nc(1)`, `content/versions.yaml` pins no netcat, and the reviewer could not start the lab. Both would become verifiable if the owner pins the lab box's tool set and mirrors a netcat manual."
  ],
  "repo_changes_needed": [
    {
      "lesson": "foundation.l1.tcp-vs-udp",
      "need": "Line 19 of the script prints `one server port, several connections at the same time:`, but the two `nc` runs after it are chained with `&&` and therefore happen one after the other, so the transcript never shows what the heading claims. Reword the echo to `one server port, one connection after another:` and recapture the output. Until the owner accepts it, the prose says the heading names the point being made, not what the two chained runs do, and outline item 2 rests on the two browser tabs of the situation rather than on the transcript.",
      "suggested_path": "scripts/network/tcp-connect.sh (+ outputs/stage-0/scripts/network/tcp-connect.txt)"
    }
  ]
}
