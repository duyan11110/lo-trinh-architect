{
  "id": "backend.l1.middleware-pipeline",
  "technical": {
    "reviewed_at": "2026-09-24T08:40:00+07:00",
    "reviewer": "claude-opus-5[1m]",
    "claim_verdicts": [
      {
        "n": 1,
        "verdict": "verified",
        "evidence": "Middleware order (moniker >= aspnetcore-8.0): \"The order that middleware appears in the app's `Program` file defines the order in which middleware are invoked on a request with the reverse order for the response.\" Same page: \"Each middleware in the request pipeline is responsible for invoking the next middleware in the pipeline or short-circuiting the pipeline.\"",
        "source": "refs/aspnetcore-docs/aspnetcore/fundamentals/middleware/index.md:553,23 (learn.microsoft.com/aspnet/core/fundamentals/middleware/)"
      },
      {
        "n": 2,
        "verdict": "verified",
        "evidence": "Intro list: each middleware \"Can perform work before and after the next middleware in the pipeline.\" And: \"Chain multiple request delegates together with Use. The `next` parameter represents the next delegate in the pipeline. You can typically perform actions both before and after the `next` delegate.\" The worked sample prints \"can write to the response (1)\", \"(2)\", \"doesn't write (2)\", \"doesn't write (1)\".",
        "source": "refs/aspnetcore-docs/aspnetcore/fundamentals/middleware/index.md:19,73,122-125"
      },
      {
        "n": 3,
        "verdict": "verified",
        "evidence": "\"Each middleware ... is responsible for invoking the next middleware in the pipeline or short-circuiting the pipeline. When a middleware short-circuits, it's called a terminal middleware because it prevents further middleware from processing the request.\" (:23) and \"You can short-circuit the pipeline by *not* calling the *next* parameter.\" (:353). The hedge \"usually after writing a response\" is supported by :127 (static file middleware \"processing a request for a static file and short-circuiting the rest of the pipeline\").",
        "source": "refs/aspnetcore-docs/aspnetcore/fundamentals/middleware/index.md:23,353,127"
      },
      {
        "n": 4,
        "verdict": "verified",
        "evidence": "Authentication concepts: \"An authentication challenge is invoked by Authorization when an unauthenticated user requests an endpoint that requires authentication ... A JWT bearer scheme returning a 401 result with a `www-authenticate: bearer` header\"; forbid \"is called by Authorization when an authenticated user attempts to access a resource they're not permitted to access\" -> \"A JWT bearer scheme returning a 403 result\". Live run of the stage-1 lab: POST /api/v1/orders with no token -> \"HTTP/1.1 401 Unauthorized\", \"Www-Authenticate: Bearer\".",
        "source": "refs/aspnetcore-docs/aspnetcore/security/authentication/index.md:113,116,122,125; examples/don-hang @ stage-1, live run 2026-09-24"
      },
      {
        "n": 5,
        "verdict": "verified",
        "evidence": "`git -C examples/don-hang show stage-1:DonHang.Api/Program.cs`, lines 64-74: comment, UseMiddleware<ExceptionHandlingMiddleware>(), UseMiddleware<RequestLoggingMiddleware>(), UseCors(), UseAuthentication(), UseAuthorization(), MapControllers(), blank, Run(). The \"own calls only\" qualifier is right: WebApplication automatically adds UseDeveloperExceptionPage \"first when the HostingEnvironment is Development\" (the lab sets ASPNETCORE_ENVIRONMENT=Development) and UseRouting \"second\"; \"User configured middleware and endpoints are added between UseRouting and UseEndpoints.\"",
        "source": "examples/don-hang @ stage-1 (commit 83c0004); refs/aspnetcore-docs/aspnetcore/fundamentals/middleware/includes/middleware8.md"
      },
      {
        "n": 6,
        "verdict": "verified",
        "evidence": "`git -C examples/don-hang show stage-1:DonHang.Api/Middleware/RequestLoggingMiddleware.cs` lines 9-24 match the lesson's block character for character: Stopwatch.StartNew(); await next(context); stopwatch.Stop(); logger.LogInformation(\"{Method} {Path} responded {StatusCode} in {ElapsedMs}ms\", context.Request.Method, context.Request.Path, context.Response.StatusCode, stopwatch.ElapsedMilliseconds).",
        "source": "examples/don-hang @ stage-1, DonHang.Api/Middleware/RequestLoggingMiddleware.cs"
      },
      {
        "n": 7,
        "verdict": "verified",
        "evidence": "Live run: `docker compose up -d db api lab web` at stage-1 (clean tree, HEAD = stage-1 commit), then `curl -i -s -X POST http://localhost:8080/api/v1/orders` -> \"HTTP/1.1 401 Unauthorized ... Server: Kestrel ... Www-Authenticate: Bearer\". `docker compose logs api` -> \"info: DonHang.Api.Middleware.RequestLoggingMiddleware[0]\" / \"      POST /api/v1/orders responded 401 in 59ms\". Containers removed afterwards.",
        "source": "examples/don-hang @ stage-1, live run 2026-09-24"
      },
      {
        "n": 8,
        "verdict": "rephrased",
        "evidence": "Routing: \"WebApplicationBuilder configures a middleware pipeline that wraps middleware added in Program.cs with UseRouting and UseEndpoints. However, apps can change the order in which UseRouting and UseEndpoints run by calling these methods explicitly\"; \"Endpoints execute at the position of UseEndpoints\"; auth middleware \"are placed between UseRouting and UseEndpoints so that they can: See which endpoint was selected by UseRouting.\" middleware8.md: UseRouting \"is added second, if the user code didn't already call UseRouting\". True of Đơn Hàng, not unconditional as written.",
        "source": "refs/aspnetcore-docs/aspnetcore/fundamentals/routing.md:55,110-113,236; .../middleware/includes/middleware8.md",
        "suggested_text": "Unless the app calls `UseRouting` itself, ASP.NET Core wraps the middleware added in `Program.cs` with route matching at the start and endpoint execution at the end, so a request is matched to its endpoint before any middleware the app registered runs, and the matched endpoint is invoked only after every middleware has run without short-circuiting."
      }
    ],
    "issues": [
      {
        "severity": "major",
        "section": 5,
        "kind": "accuracy",
        "quote": "Moving `UseAuthorization` above `RequestLoggingMiddleware` would not just reorder two lines: a rejected request would then short-circuit before `RequestLoggingMiddleware` ever called `next`, so it would stop appearing in the log at all",
        "text": "Unlisted claim: this counterfactual is nowhere in the claims ledger, and it hides a condition. Moving UseAuthorization above RequestLoggingMiddleware also moves it above UseCors and UseAuthentication, which the docs forbid: \"CORS middleware, authentication middleware, and authorization middleware must appear in the order shown\" and \"UseCors must be placed after UseRouting and before UseAuthorization\". The lesson presents the lost log line as the whole cost of a reorder that is documented-illegal for other reasons too.",
        "fix": "Use the equivalent counterfactual that crosses nothing else: \"moving `app.UseMiddleware<RequestLoggingMiddleware>()` below `app.UseAuthorization()` would make rejected requests stop appearing in the log\". Add that as a claim in .meta.json (behavior, aspnetcore). Keep quiz q6's scenario in sync with whichever direction the prose uses; if the UseAuthorization-moves-up version is kept, add \"(the docs also require UseAuthorization to stay after UseCors and UseAuthentication)\"."
      },
      {
        "severity": "major",
        "section": "backend.l1.middleware-pipeline.q6",
        "kind": "quiz",
        "quote": "the proposal moves `UseAuthorization` earlier, not `UseAuthentication` later, so that specific problem does not arise here",
        "text": "False. The scenario moves UseAuthorization ahead of \"the other wiring calls\", which includes `app.UseAuthentication()`; relative order is all that matters, so UseAuthentication would indeed run after UseAuthorization — the docs' required order (CORS, authentication, authorization) would be broken. Option c is wrong only because nothing throws an exception, not because the ordering problem \"does not arise\". Key ('a') stays correct; the explanation teaches something untrue.",
        "fix": "Rewrite c's explanation (en+vi): \"Running `UseAuthentication` after `UseAuthorization` is exactly what this proposal would cause, and the docs require the opposite order — but nothing throws; the checkable cost the question asks about is the missing log line.\""
      },
      {
        "severity": "major",
        "section": "backend.l1.middleware-pipeline.q6",
        "kind": "quiz",
        "quote": "This would matter when the proposal also moved `app.MapControllers()` itself",
        "text": "Repeats the mental-model error: where `app.MapControllers()` sits in the file does not decide when the endpoint runs. MapControllers only registers endpoints; \"Endpoints execute at the position of UseEndpoints\", which WebApplication adds at the end of the pipeline. Moving that line would not make endpoints \"stop working\". The vi text is also stronger than the en: \"request được phép vẫn qua mọi bước kiểm tra và tới `app.MapControllers()`\" describes the request as travelling to a wiring call.",
        "fix": "Rewrite b's explanation (en+vi): \"`app.MapControllers()` only registers endpoints; the endpoints it registers execute at the end of the pipeline wherever that line sits, so an authorized request still reaches `OrdersController.Create()` exactly as before.\" Align vi with en (\"tới endpoint\", not \"tới app.MapControllers()\")."
      },
      {
        "severity": "major",
        "section": "backend.l1.middleware-pipeline.q4",
        "kind": "quiz",
        "quote": "and `MapControllers()` last, since it is the terminal step an authorized request finally reaches",
        "text": "The question asks for registration order in the file, but the explanation justifies the last position by pipeline position — the same wrong model. `app.MapControllers()` is not a pipeline step: it registers endpoints, and \"Endpoints execute at the position of UseEndpoints\" (added at the end of the pipeline by WebApplication) regardless of where the line appears. Answer order b,d,a,c matches Program.cs lines 65-69 and stays correct.",
        "fix": "Rewrite the explanation's tail (en+vi): \"...and `MapControllers()` last in the file — it registers endpoints rather than adding a pipeline step, and those endpoints run at the end of the pipeline wherever this line sits.\""
      },
      {
        "severity": "minor",
        "section": 7,
        "kind": "accuracy",
        "quote": "Whatever was registered after the short-circuiting middleware — here, the endpoint `OrdersController.Create()` that routing had already matched — never runs at all.",
        "text": "Calls the endpoint something \"registered after the short-circuiting middleware\". That is textually true only by accident here (MapControllers happens to be written after UseAuthorization) and is not the reason the endpoint does not run: endpoints execute at the end of the pipeline, at UseEndpoints, whatever the registration order.",
        "fix": "\"Everything further down the pipeline — including the endpoint, which runs only at the very end — never runs at all.\""
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "accuracy",
        "quote": "`UseAuthorization` comes last among them, right before `app.MapControllers()`, because it is the last thing allowed to say no before an allowed request's endpoint runs",
        "text": "\"right before `app.MapControllers()`\" invites the reader back into the position-of-MapControllers model. What makes UseAuthorization the last check is that it is the last middleware registered, not the neighbouring line.",
        "fix": "Drop the clause: \"`UseAuthorization` comes last among them, because it is the last middleware allowed to say no before an allowed request's endpoint runs; where `app.MapControllers()` sits in the file doesn't change that.\""
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "accuracy",
        "quote": "exactly the change Try it below checks for",
        "text": "Try it applies no change: it runs one curl against the order as it stands and reads the log. It demonstrates that the log line exists today, not what would happen after the reorder.",
        "fix": "\"— the opposite of what Try it below shows with the order as it stands.\""
      },
      {
        "severity": "minor",
        "section": 4,
        "kind": "diagram",
        "quote": "E -->|No| F[401 or 403, written here]",
        "text": "\"written here\" conflicts with the prose and with claim 4: the status is produced by the authentication scheme UseAuthorization challenges (JWT bearer returns 401 with `www-authenticate: bearer`), not written by the authorization middleware itself.",
        "fix": "Relabel the node: `F[Request stops: 401 or 403 written on its behalf]`."
      },
      {
        "severity": "minor",
        "section": 4,
        "kind": "scope",
        "quote": "M[Routing matches the endpoint] --> A[ExceptionHandlingMiddleware]",
        "text": "\"routing\" is used three times (diagram node; \"the endpoint routing already matched\"; \"that routing had already matched\") but is neither in this lesson's `vocab` nor in known_vocab before it, and is never glossed — the reader is told a step exists, before the lesson's own first middleware, with no idea what it is. It is also outside `depth_notes`, which only authorises the UseAuthorization/[Authorize] observation.",
        "fix": "Gloss it once on first use, e.g. \"routing — the built-in step that decides which endpoint a URL belongs to — has already matched the endpoint before any of Đơn Hàng's middleware runs\", and either add `routing` to this lesson's `vocab` in track.yaml or extend `depth_notes` to permit naming it (as was done for UseAuthorization)."
      },
      {
        "severity": "minor",
        "section": 4,
        "kind": "accuracy",
        "quote": "so it is still on its way back out when the short-circuit happens further down — which is why it still gets a chance to log the outcome",
        "text": "Sequence is inverted: when the short-circuit happens, RequestLoggingMiddleware has finished its way-in work and is awaiting `next`; its way-out half starts only after the short-circuit returns.",
        "fix": "\"so it is still waiting for `next` to return when the short-circuit happens further down — and that return is what lets its after-`next` half run and log the outcome.\""
      },
      {
        "severity": "minor",
        "section": "backend.l1.middleware-pipeline.q3",
        "kind": "quiz",
        "quote": "the line that must run before the method can read `context.Response.StatusCode`",
        "text": "`context.Response.StatusCode` is readable at any point (it is 200 by default); `await next(context)` is what makes the value the one the request actually got. As written the stem states something false about HttpResponse, though the answer \"next\" is unambiguous.",
        "fix": "Restate the stem: \"the line that must run before `context.Response.StatusCode` holds the status this request actually got is `await ___(context);`\"."
      }
    ],
    "scope": {
      "outline_covered": true,
      "beyond_scope": [
        "Route matching as a pipeline step that runs before the app's own middleware (diagram node \"Routing matches the endpoint\" plus two mentions of what routing \"had already matched\") — factually required to avoid the earlier wrong model, but outside the outline and depth_notes, and the term is never explained."
      ]
    },
    "verdict": "fix-required"
  }
}
