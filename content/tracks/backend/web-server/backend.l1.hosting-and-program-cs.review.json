{
  "id": "backend.l1.hosting-and-program-cs",
  "technical": {
    "reviewed_at": "2026-09-24T16:20:00+07:00",
    "reviewer": "claude-opus-5[1m]",
    "claim_verdicts": [
      {
        "n": 1,
        "verdict": "verified",
        "evidence": "Docs mirror, .NET 10 moniker block (range=\">= aspnetcore-10.0\"): \"WebApplication.CreateBuilder ... initializes a new instance of the WebApplicationBuilder class with preconfigured defaults\". The same page shows `var builder = WebApplication.CreateBuilder(args);` followed by `builder.Services.Add...` and `builder.Configuration`, i.e. services and configuration are registered on that object.",
        "source": "refs/aspnetcore-docs/aspnetcore/fundamentals/minimal-apis/includes/webapplication10.md:15"
      },
      {
        "n": 2,
        "verdict": "verified",
        "evidence": "fundamentals/middleware/index.md:55 \"The call to WebApplication.Run ... at the end of the code block runs the app and blocks the calling thread until host shutdown.\" fundamentals/servers/index.md: \"Kestrel server is the default, cross-platform HTTP server implementation\" (this app self-hosts in a Linux container; no IIS). Ran on SDK 10.0.300: after builder.Build(), builder.Services.AddSingleton<object>() throws InvalidOperationException \"The service collection cannot be modified because it is read-only\" - registration really does end at Build().",
        "source": "refs/aspnetcore-docs/aspnetcore/fundamentals/middleware/index.md:55; fundamentals/servers/index.md; local run, dotnet 10.0.300"
      },
      {
        "n": 3,
        "verdict": "verified",
        "evidence": ".NET docs mirror: \"Service registration is order-independent except when registering multiple implementations of the same type.\" Every builder.Services.Add... call in Program.cs lines 10-50 registers a distinct type (AddControllers, AddProblemDetails, AddDonHangInfrastructure -> DonHangDbContext/IOrderRepository/INotifier, OrderService, JwtTokenService, AddAuthentication/AddJwtBearer, AddAuthorization, AddCors), so the exception does not apply.",
        "source": "refs/dotnet-docs/docs/core/extensions/dependency-injection/service-registration.md:128"
      },
      {
        "n": 4,
        "verdict": "verified",
        "evidence": "fundamentals/routing.md:73 \"An endpoint is something that can be: Selected, by matching the URL and HTTP method. Executed, by running the delegate.\" mvc/controllers/routing.md:94 \"Call MapControllers to map attribute routed controllers.\" Ran a controller with [Route(\"api/v1/ping\")] + [HttpGet] and [HttpGet(\"{id:int}\")] on dotnet 10.0.300: GET /api/v1/ping -> 200, POST /api/v1/ping -> 405; one method-and-path pair per attribute-routed action.",
        "source": "refs/aspnetcore-docs/aspnetcore/fundamentals/routing.md:73; mvc/controllers/routing.md:94; local run"
      },
      {
        "n": 5,
        "verdict": "verified",
        "evidence": "Local run, dotnet 10.0.300: action body List() contains Console.WriteLine(\"list ran\"); the startup log shows only \"startup: MapControllers done\", \"Now listening on: http://127.0.0.1:5199\", \"Application started\" - no \"list ran\". After two GET /api/v1/ping the log shows \"list ran\" exactly twice. The method body runs per matching request, not when MapControllers() executes.",
        "source": "local run (scratch ASP.NET Core 10 app mirroring ProductsController)"
      },
      {
        "n": 6,
        "verdict": "verified",
        "evidence": "Ran the same app with builder.Services.AddControllers() removed, dotnet 10.0.300: \"Unhandled exception. System.InvalidOperationException: Unable to find the required services. Please add all the required services by calling 'IServiceCollection.AddControllers' ... at ControllerEndpointRouteBuilderExtensions.EnsureControllerServices(IEndpointRouteBuilder endpoints) at ...MapControllers(IEndpointRouteBuilder endpoints)\". MapControllers finds no controllers without AddControllers.",
        "source": "local run, ASP.NET Core 10.0 / SDK 10.0.300"
      },
      {
        "n": 7,
        "verdict": "verified",
        "evidence": "git -C examples/don-hang show stage-1:DonHang.Api/Program.cs: line 52 `var app = builder.Build();`, lines 54-62 the migration block (CreateScope, MigrationBaseline.ApplyIfNeeded, context.Database.Migrate()), lines 64-72 the wiring calls ending in app.MapControllers();, line 74 `app.Run();`, and the file has exactly 74 lines. Both lesson code fences (lines=10-20, lines=52-74) match the source byte-for-byte (scripted diff).",
        "source": "examples/don-hang @ stage-1 (commit 83c0004), DonHang.Api/Program.cs"
      },
      {
        "n": 8,
        "verdict": "verified",
        "evidence": "Behaviour executed locally (see claim 5): nothing at startup, one line per matching request, two requests -> two lines. Container mechanics from docs: \"When you run the command docker compose up --build, Docker Compose will build the images for each service and then start the containers\" and \"By default, docker logs shows the command's STDOUT and STDERR\". Not executed: the full stage-1 lab (scripts/up.sh needs flutter build web); no container was rebuilt here.",
        "source": "local run; refs/docker-docs/content/guides/docker-compose.md:140; refs/docker-docs/content/manuals/engine/logging/_index.md:24"
      },
      {
        "n": 9,
        "verdict": "verified",
        "evidence": "git -C examples/don-hang show stage-1:DonHang.Api/Controllers/ProductsController.cs: the class carries [ApiController] and [Route(\"api/v1/products\")]; List() carries [HttpGet]; Get(int id) carries [HttpGet(\"{id:int}\")]. The path comes from the class attribute, the HTTP method (and the id segment) from the method attribute - exactly \"the class and its methods carry [Route]/[HttpGet] attributes\".",
        "source": "examples/don-hang @ stage-1, DonHang.Api/Controllers/ProductsController.cs"
      }
    ],
    "issues": [
      {
        "severity": "major",
        "section": "backend.l1.hosting-and-program-cs.q6",
        "kind": "quiz",
        "quote": "Please move `app.UseAuthentication();` to right after `app.UseCors();` and before `app.UseMiddleware<RequestLoggingMiddleware>();`",
        "text": "The scenario's reorder request is impossible against the file the lesson just quoted. In Program.cs lines 67-72 the order is ExceptionHandling, RequestLogging, UseCors, UseAuthentication, UseAuthorization: UseAuthentication is ALREADY immediately after UseCors, and UseCors itself comes after RequestLogging, so \"right after UseCors\" and \"before RequestLoggingMiddleware\" cannot both hold. A reader who checks the code can therefore defend option d (\"The reviewer's suggested line order is itself wrong\"), which makes the key ambiguous.",
        "fix": "Rewrite the review comment in `context` as one coherent move, e.g. \"Please move `app.UseAuthentication();` above `app.UseMiddleware<RequestLoggingMiddleware>();` - the current order is a bug.\" Keep answer a; option d's explanation then holds (the scenario really says nothing about whether that move is right)."
      },
      {
        "severity": "minor",
        "section": 4,
        "kind": "accuracy",
        "quote": "Everything an ASP.NET Core app needs before it can run — a database connection, a way to prove a customer has signed in, and so on — gets registered on that one object.",
        "text": "\"Everything\" overstates it: the docs say CreateBuilder hands back the builder \"with preconfigured defaults\" - configuration sources, logging and the server itself are already in place and the app never registers them. What goes on builder.Services is what this app adds on top of those defaults.",
        "fix": "Change to: \"Whatever this app needs on top of what the framework already sets up - what it uses to reach the database, a way to prove a customer has signed in, and so on - gets registered on that one object.\""
      },
      {
        "severity": "minor",
        "section": 4,
        "kind": "accuracy",
        "quote": "app.MapControllers(), is the one that creates every **endpoint** this app has",
        "text": "Unlisted claim: \"the one that creates every endpoint this app has\" is a checkable fact about DonHang.Api (MapControllers is the only endpoint-creating call in Program.cs), not a general property of MapControllers, and no claim in the ledger carries it (claim 4 only covers what MapControllers does).",
        "fix": "Keep the sentence (true at stage-1: Program.cs lines 64-72 contain no other Map* call) and add a fact claim to .meta.json, section 4: \"In DonHang.Api/Program.cs at stage-1, app.MapControllers() is the only endpoint-creating call, so every endpoint the app has comes from it.\""
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "code",
        "quote": "register the pieces the app needs to reach the database, a standard error format (`AddProblemDetails()`), an order service, and a way to prove a customer has signed in",
        "text": "Two mismatches with the source. AddDonHangInfrastructure registers DonHangDbContext, IOrderRepository AND INotifier/LoggingNotifier - the notifier is not a piece for reaching the database. JwtTokenService's only member is IssueToken(int customerId, string email): it hands out the proof, it does not prove or check anything; the checking is AddAuthentication/AddJwtBearer on line 25, outside the quoted block.",
        "fix": "Write: \"...register the infrastructure pieces this app runs on (among them everything it uses to reach the database), a standard error format (`AddProblemDetails()`), an order service, and a way to give a signed-in customer something it can show on later requests.\""
      },
      {
        "severity": "minor",
        "section": 9,
        "kind": "accuracy",
        "quote": "`app.MapControllers()` creates one for every attribute-routed method it finds.",
        "text": "True in this app only. An action method may carry several route attributes and each one produces its own endpoint; the routing docs note \"All of the HTTP verb attributes accept a route template\" and show several templates per action. Each ProductsController method carries exactly one, so the file matches, but the summary states it as a general rule.",
        "fix": "Reword summary line 3 to \"...`app.MapControllers()` creates one for each route attribute it finds - one per method in `ProductsController`.\" (or add \"in this app\")."
      },
      {
        "severity": "minor",
        "section": "backend.l1.hosting-and-program-cs.q3",
        "kind": "quiz",
        "quote": "\"answer\": [\"MapControllers\"]",
        "text": "The fill answer set has a single spelling. Comparison is case-insensitive and trimmed (docs/01), but a learner who types the call as the lesson writes it (MapControllers()) or repeats the prefix (app.MapControllers) is marked wrong for no content reason.",
        "fix": "Set \"answer\": [\"MapControllers\", \"MapControllers()\", \"app.MapControllers\", \"app.MapControllers()\"]."
      }
    ],
    "scope": {
      "outline_covered": true,
      "beyond_scope": []
    },
    "verdict": "fix-required"
  },
  "junior": {
    "reviewed_at": "2026-09-24T18:40:00+07:00",
    "reviewer": "claude-opus-5[1m] (junior-reader simulation, round 2)",
    "issues": [
      {
        "severity": "major",
        "section": 7,
        "kind": "example",
        "quote": "add a temporary `Console.WriteLine(\"list ran\");` as the first line inside `ProductsController`'s method for `GET /api/v1/products`",
        "text": "Step 1 tells me to edit `ProductsController`'s method for `GET /api/v1/products`, but the lesson never shows that class, never says which file it is in, and never shows one of the attributes it keeps talking about. A products class plausibly has more than one GET method; if I pick the wrong one the curls print nothing and I cannot tell whether the lesson is wrong or I am. Finding the right method is guesswork on top of the three minutes.",
        "fix": "In Try it step 1, name the file (for example `DonHang.Api/Controllers/ProductsController.cs`) and add one clause saying the method to edit is the one whose attributes name GET and the path `/api/v1/products`."
      },
      {
        "severity": "major",
        "section": 2,
        "kind": "term",
        "quote": "A teammate points at a single line, `app.MapControllers();`, and says \"that line is what makes the API answer `/api/v1/products`.\"",
        "text": "\"API\" is a word I have not been given and the lesson never defines it. Everywhere else the lesson says \"the app\", so I stop to decide whether the API is the same thing as the app, something inside it, or something in front of it — the previous lesson did have a server in front answering with fixed strings.",
        "fix": "In section 2 replace \"that line is what makes the API answer `/api/v1/products`\" with \"that line is what makes the app answer `/api/v1/products`\", the wording used in the rest of the lesson."
      },
      {
        "severity": "major",
        "section": 2,
        "kind": "term",
        "quote": "You do not see a URL anywhere near it.",
        "text": "\"URL\" is not in what I know and is never defined. The very next sentence says \"a specific path\", a word the lesson uses consistently afterwards, so I have to guess whether URL and path mean the same thing here or whether I am supposed to be looking for two different things in the file.",
        "fix": "Replace \"You do not see a URL anywhere near it.\" with \"You do not see that path anywhere near it.\""
      },
      {
        "severity": "major",
        "section": 4,
        "kind": "term",
        "quote": "a database connection, a way to issue tokens, and so on — gets registered on that one object",
        "text": "\"a way to issue tokens\" — and the same phrase again in section 5, next to `JwtTokenService` — rests on \"token\", a word I have never met. I cannot tell whether a token is something the app stores, sends or checks, so the example of what gets registered teaches me nothing and I skip both sentences. It does not block the main idea, but it is the only example the lesson gives twice.",
        "fix": "Gloss the word once at first use in section 4, e.g. \"a way to issue tokens (the strings a client later sends back to prove who it is)\", or replace both mentions with a phrase built from words the lesson already uses, e.g. \"a way to prove who a caller is\"."
      },
      {
        "severity": "minor",
        "section": 7,
        "kind": "length",
        "quote": "## Try it (3 minutes)",
        "text": "Step 1 asks me to rebuild the container with `docker compose up -d --build api` and then wait for the startup log to settle before curling. A rebuild is not something that finishes while I watch it, so the \"3 minutes\" in the heading is optimistic the first time I run this.",
        "fix": "Change the heading to \"Try it (5 minutes)\", or add \"the rebuild takes a minute or two the first time\" to the end of step 1."
      },
      {
        "severity": "minor",
        "section": 5,
        "kind": "gap",
        "quote": "read only its first and last lines for now; everything between them, comments included, belongs to later lessons",
        "text": "The prose tells me to read only the first and last lines of the 23-line block, and the sentence right after the block explains a line in the middle of it. I had just been told I was not allowed to look there, so I read the block a second time to find the line being discussed.",
        "fix": "Change \"read only its first and last lines for now\" to \"read only its first and last lines plus the `app.MapControllers();` line for now\"."
      },
      {
        "severity": "minor",
        "section": 4,
        "kind": "gap",
        "quote": "When two calls register different things, as every call in this file does, the order between them makes no difference to how a later request is answered.",
        "text": "The rule always arrives with a condition — calls that \"register different things\" — and the lesson never says what the other case is. The condition comes back in Beginners often think, in quiz q2 and in summary line 2, so by the fourth time I am sure the other case matters and I still cannot picture it.",
        "fix": "Add one clause at the first use in section 4 saying the other case does not arise here, e.g. \"two calls registering the same thing is a case this file does not contain and this lesson does not cover\"."
      },
      {
        "severity": "minor",
        "section": 9,
        "kind": "style",
        "quote": "When `builder.Services.Add...` calls register different things, their order only decides what is available to the app, not how any request is handled.",
        "text": "Line 2 reads as if the order is the thing that decides what is available to the app, which is the opposite of section 4's \"the order between them makes no difference\". I read it three times before deciding it must mean the calls, not their order, decide what is available.",
        "fix": "Rewrite line 2 as: \"`builder.Services.Add...` calls that register different things only decide what is available to the app; the order among them does not decide how any request is handled.\""
      },
      {
        "severity": "minor",
        "section": 4,
        "kind": "style",
        "quote": "Between `Build()` and `Run()`, Program.cs does the rest of its startup work, including wiring up (through `app.Use...`/`app.Map...` calls) the steps every incoming request will pass through before reaching your code",
        "text": "This one sentence carries four things: what happens between `Build()` and `Run()`, what wiring means, which calls count as wiring, and a forward reference. I re-read it twice. The same \"that is the next lesson's subject\" deferral then returns in Beginners often think, in Connections and in summary line 5.",
        "fix": "Split it in two: one sentence for what happens between `Build()` and `Run()` and what the `app.Use...`/`app.Map...` calls do, one short sentence for the forward reference."
      },
      {
        "severity": "minor",
        "section": "quiz q2",
        "kind": "quiz",
        "quote": "True, because a request always uses services in the same order they were registered in Program.cs.",
        "text": "Options a and b state the same claim in two wordings, so both have to be wrong, and d invents a rule about \"the compiled order of the assembly\" that appears nowhere in the lesson. I can land on c by elimination without having understood anything about registration order.",
        "fix": "Rewrite option b so it offers a different tempting reason, e.g. \"True, because `AddSingleton` and `AddScoped` build their objects right at the registration line, in the order written.\""
      },
      {
        "severity": "minor",
        "section": "quiz q2",
        "kind": "quiz",
        "quote": "which registered service a piece of code uses depends on what that code asks for, not on registration order",
        "text": "The explanation for option b tells me which service a piece of code uses \"depends on what that code asks for\". Nothing in the lesson says code asks for services, so the sentence meant to correct me hands me a new idea I have no way to check.",
        "fix": "Reword b's explanation using only what the lesson says, e.g. \"the order of these two calls decides what is available to the app, not how any request is handled\"."
      },
      {
        "severity": "minor",
        "section": "quiz q5",
        "kind": "quiz",
        "quote": "restarts the app once, and is confused not to see it print in the startup logs",
        "text": "The question has the junior \"restart the app once\" after editing the code, but Try it made a point of rebuilding with `--build` because a plain restart does not pick the change up. In the question's own story the line would therefore not run at all, which made me hesitate over option d before picking a.",
        "fix": "Change \"restarts the app once\" to \"rebuilds and restarts the app once\" in both the en and vi question text."
      }
    ],
    "readability": {
      "unknown_terms": [
        "API (\"makes the API answer `/api/v1/products`\") — never defined; the rest of the lesson says \"the app\"",
        "URL (\"You do not see a URL anywhere near it\") — never defined; \"path\" is used everywhere else",
        "token (\"a way to issue tokens\", `JwtTokenService`) — never defined",
        "connection string (`GetConnectionString(\"Default\")`) — not defined, but guessable from \"the pieces the app needs to reach the database\"",
        "middleware, CORS, authentication, authorization, migrations, schema, `DonHangDbContext`, `CreateScope` — all inside the second code block, which the prose does tell me to skip, so they did not stop me this time",
        "the lab / `scripts/up.sh`, `docker compose`, `curl`, the `api` service name — assumed from earlier lessons; neither this lesson nor the prerequisite's summary says what they are"
      ],
      "skipped_steps": [
        "\"read only its first and last lines for now\" — the next sentence then discusses a line in the middle of the block",
        "\"When two calls register different things\" — the opposite case is never described, although the condition is repeated four times",
        "Try it step 1 — which file `ProductsController` is in, and which of its methods answers `GET /api/v1/products`, is never said or shown"
      ],
      "situation_connects": true,
      "try_it_feasible": false,
      "summary_matches": true
    },
    "verdict": "fix-required"
  }
}