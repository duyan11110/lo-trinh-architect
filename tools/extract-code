#!/usr/bin/env bash
# Wrapper for tools/bin/extract_code.dart — see tools/SPEC.md.
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec dart run "$DIR/bin/extract_code.dart" "$@"
