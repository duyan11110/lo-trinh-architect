name: lta_tools
description: Validation and build toolchain for the Lộ Trình Architect content repo.
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  yaml: ^3.1.0
  json_schema: ^5.1.0
  args: ^2.4.0
  crypto: ^3.0.0
  path: ^1.9.0
  collection: ^1.18.0

dev_dependencies:
  test: ^1.24.0
