#!/usr/bin/env bash
# Wrapper for tools/bin/merge_outline.dart — see tools/SPEC.md.
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec dart run "$DIR/bin/merge_outline.dart" "$@"
