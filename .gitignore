# Repo ví dụ "Đơn Hàng": nên là submodule (DECISIONS A1) khi có remote GitHub.
# Tạm thời là repo git độc lập trên máy vì `gh` chưa cài/đăng nhập ở phiên này (DECISIONS §3).
# KHÔNG xóa thư mục này — nó chứa commit thật, chỉ chưa có remote.
/examples/don-hang/

# Output công cụ / build
/dist/

# Bản sao tài liệu chính thức cho reviewer (D9) — vài GB, tái tạo bằng tools/fetch-refs, không commit.
/refs/
