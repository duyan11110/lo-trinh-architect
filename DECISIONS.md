# DECISIONS.md — Quyết định đã chốt và mặc định cho phiên chạy tự trị

**Claude Code: đọc file này ngay sau CLAUDE.md, trước mọi việc.** Mọi điều dưới đây đã được chủ (An) quyết ngày 06/09/2026.
Khi tài liệu khác nói khác, **file này thắng**. Không hỏi lại các điểm đã có ở đây; với điểm chưa có, chọn mặc định hợp lý,
ghi vào mục "Mặc định agent đã chọn" ở cuối file (kèm lý do một dòng) và tiếp tục — chỉ dừng khi hành động không đảo ngược
được VÀ không có mặc định nào ở đây bao phủ.

## 1. Quyết định của chủ

| # | Chủ đề | Quyết định |
|---|---|---|
| D1 | Cửa duyệt bài | **Tự động đặt `approved`** khi: review kỹ thuật `pass` và review junior `pass` (sau khi đã áp dụng sửa), không còn `blocker`/`major`, không claim `wrong`, không `unverified` với `kind ∈ {number, syntax}`. Ghi `approved_by: auto`, `reviewed_at`. Chủ duyệt lại theo lô sau; bài chủ sửa → `status: reviewed`, review kỹ thuật lại phần đổi, rồi `approved_by: owner`. |
| D2 | Database repo ví dụ | **PostgreSQL 17** xuyên suốt. Không SQL Server ở V1. |
| D3 | Model | **Opus** cho sinh bài, sinh quiz, sinh outline, gate, repo ví dụ và hai reviewer; **Sonnet** cho dịch (`/translate`, `claude -p --model sonnet`). |
| D4 | Hosting | **GitHub**: repo `duyan11110/lo-trinh-architect` (nội dung + app + tools, monorepo), repo ví dụ `duyan11110/don-hang` (submodule tại `examples/don-hang`). GitHub Pages phục vụ site `https://duyan11110.github.io/lo-trinh-architect/` với `app/` (web) và `content/` (manifest + content.json.gz). CI = GitHub Actions. APK cài tay, không Play Store. |
| D5 | Môi trường chạy | **Máy của chủ**, Claude Code CLI đã đăng nhập; có Docker Desktop, Flutter, .NET SDK, Node, git. Agent **được cài** công cụ còn thiếu (fvm, mermaid-cli, kubeconform, psql client, gh, Dart deps) bằng brew/apt/npm/dart pub. |
| D6 | Quyền git | Agent **được** `commit`, `tag stage-N` (khi manifest đủ + kiểm tra cục bộ xanh), `push` nhánh `auto/*` và tag lên `origin` của cả hai repo. **Không** push `main`; merge `auto/*` → `main` là việc của chủ (PR). |
| D7 | Định danh app | Tên "Lộ Trình Architect", `applicationId dev.lotrinharchitect.app`, package `lta_app`, `--org dev.lotrinharchitect`, platforms `android,web`, icon mặc định Flutter, Flutter **stable mới nhất** tại thời điểm chạy (pin vào `.fvmrc` + `pubspec environment` + `app/CLAUDE.md`). |
| D8 | Repo ví dụ GĐ0 | Caddy 2.10 **giả lập hành vi API** bằng `respond`/`header` (status code, cookie, cache, POST) — chấp nhận; file văn xuôi được trích (`docs/team`, `docs/craft`, `docs/git`, `docs/clean-code`) viết **tiếng Việt** và được trích nguyên văn trong cả bản EN lẫn VI (ngoại lệ có chủ đích của style guide EN). |
| D9 | Bằng chứng reviewer | **Clone `refs/`** (`dotnet/docs`, `dotnet/AspNetCore.Docs`, `kubernetes/website`, `docker/docs`, `flutter/website`, `--depth 1`) + tải RFC 9110/9111/9112/6265 vào `refs/rfc/` (`tools/fetch-refs`). Grep nguyên văn là bằng chứng ưu tiên; WebFetch là phụ. |
| D10 | Phiên bản | .NET 10 / C# 14 / ASP.NET Core 10 / EF Core 10, PostgreSQL 17, Caddy 2.10, Git 2.5x, Npgsql 9 (khóa mới `npgsql`), Flutter stable mới nhất. Agent copy `templates/versions.yaml` → `content/versions.yaml`, ghi số patch theo `dotnet --list-sdks`, `flutter --version`, `psql --version` trên máy chủ. |
| D11 | Đích lần chạy đầu | **App M0–M7 + toàn bộ Giai đoạn 0 `approved`, dịch, `published`, bài cổng gate0**, web trên Pages + APK debug. GĐ1–4 chạy theo lệnh sau. |
| D12 | Nới điều kiện | Bỏ điều kiện "chỉ chạy headless sau khi chủ tự đọc ≥ 10 bài" (docs/06 §6) cho GĐ0. `tools/build --publish` được `tools/gen --stage 0` chạy ở cuối khi mọi bài main path đã approved + có `.vi.md`. |

## 2. Mặc định bắt buộc (chủ đã ký, agent không hỏi lại)

### A. Setup
- A1 Repo ví dụ: nếu `examples/don-hang` chưa là submodule: `gh repo create duyan11110/don-hang --public` (nếu `gh` có và đã đăng nhập), rồi `git submodule add https://github.com/duyan11110/don-hang.git examples/don-hang`. Nếu không tạo được remote: làm việc trong `examples/don-hang` như repo con độc lập (không nested-commit vào repo mẹ; thêm vào `.gitignore` tạm) và ghi vào mục 3.
- A2 Hạt giống stage-0: copy nguyên văn `examples/don-hang-stage-0/*` vào repo Đơn Hàng (kể cả `outputs/`, `unstable.regex`, `STAGE.md` làm nháp).
- A3 Bài mẫu `examples/foundation.l1.http-request-response.*` là **bài chính thức**: copy vào `content/tracks/foundation/http/` (5 file), giữ `status: approved`, `approved_by: owner`; không sinh lại. Nếu output thật của `raw-request.sh` khác mẫu (Content-Length, số header), **cập nhật output và câu chữ tương ứng trong cả EN/VI** rồi tăng `content_version` không cần (chưa published).
- A4 Git identity headless: `user.name "An (Lộ Trình Architect)"`, `user.email nguyenduyan11110@gmail.com`. `TZ=Asia/Ho_Chi_Minh` cho mọi timestamp.
- A5 Tên thư mục checkout `lo-trinh-architect`; base href `/lo-trinh-architect/app/`; `remote_base_url = https://duyan11110.github.io/lo-trinh-architect/content/`.
- A6 Chủ làm **một lần** (agent nhắc ở đầu phiên nếu chưa có): bật GitHub Pages (Settings → Pages → GitHub Actions); tạo keystore + 4 secret (`KEYSTORE_B64`, `KEY_ALIAS`, `KEY_PASSWORD`, `STORE_PASSWORD`) — tới lúc đó APK ký debug; merge PR `auto/*`.

### B. Tools
- B1 Dart tools + wrapper bash như SPEC; `tools/pubspec.yaml` dùng `yaml`, `json_schema`, `args`, `crypto`, `path`, `collection`; Dart SDK theo Flutter đang cài. W → exit 0.
- B2 Mermaid: `validate` chỉ parse/đếm node bằng Dart (A.5), báo W nếu không có `mmdc`; `build` bắt buộc `mmdc` (`npx -y @mermaid-js/mermaid-cli`, Chromium qua Playwright) với `htmlLabels: false`, rồi hậu xử lý: inline CSS → thuộc tính, xóa `<style>`/`<foreignObject>`, thay màu theo sentinel. Test build: SVG không chứa `<style>` và `<foreignObject>`.
- B3 `validate`: module trong `path.yaml` chưa có `track.yaml` → W (không E); `--file` bỏ S07 (chỉ kiểm L08 khớp code với `--repo-dir`); `--stage n` chỉ kiểm giai đoạn n.
- B4 `capture-output --ref <ref>` (mặc định working tree; worktree tạm chỉ khi ref ≠ HEAD). Nguồn sự thật là `scripts/capture-output.sh` **trong repo Đơn Hàng** (CI của nó chạy được); `tools/capture-output` chỉ là wrapper.
- B5 `build --preview` (gồm bài `reviewed`, cho phép thiếu `.vi.md`) → `dist/preview/`, không tăng `content_version`; `meta.versions` = phẳng hóa mọi khóa có `version`, `standards` → chuỗi `docs`.
- B6 `tools/sync-app-content [--preview]`: cp `dist/(preview/)content.json` → `app/assets/content/content.json` + ghi `VERSION`. `tools/fetch-refs`: clone/tải D9. `tools/fetch-web-sqlite`: tải `sqlite3.wasm` + `drift_worker.js` đúng phiên bản gói, pin sha256, commit vào `app/web/`.
- B7 Agent **được sửa lỗi kỹ thuật của schema/SPEC** (ví dụ `content.schema.json` khối `path` thiếu `type`) với commit message nêu rõ; không đổi ngữ nghĩa quy tắc.
- B8 Nếu Claude Code không hỗ trợ `arguments:`/`$id`/`maxTurns` trong skill/agent: chuyển sang `$ARGUMENTS`/`$0 $1` và `argument-hint`, bỏ khóa không hỗ trợ, ghi vào mục 3.

### C. Repo ví dụ Đơn Hàng (stage-0)
- C1 Manifest bắt buộc = `tools/validate --manifest 0` (81 file). Tag `stage-0` khi: manifest đủ, `scripts/up.sh` chạy, mọi script chạy exit 0 và output đã capture, `dotnet build`/`dotnet test` của Samples xanh, CI local (`act`) hoặc GitHub Actions xanh. Không cần xác nhận của chủ (D6).
- C2 Lab box: image `lscr.io/linuxserver/openssh-server` pin tag, user `dev`, xác thực bằng khóa sinh bởi `scripts/dev-secrets.sh`, cổng 2222; `scripts/terminal/ssh-into-lab.sh` chạy được non-interactive (`ssh … 'hostname; whoami'`) và tương tác.
- C3 Caddy: HTTP `:8080` site tĩnh `www/`; HTTPS `https://donhang.local:8443` với `tls internal` (thêm `127.0.0.1 donhang.local` hướng dẫn trong STAGE.md); route giả lập: `POST /api/v1/orders` → 201 + JSON, `GET /api/v1/orders/1` → 200, `/api/v1/orders/999` → 404, `/admin` → 401 khi không cookie / 403 khi cookie `role=guest`, `/login.html` set-cookie `sid` `HttpOnly; SameSite=Lax`, `/cached.html` `Cache-Control: max-age=60` + ETag, `/slow` → 503, `/conflict` → 409, `/redirect` → 302. Danh sách này là "hành vi hệ thống" mà 8 bài HTTP được khẳng định.
- C4 Images pin: `postgres:17.6-alpine`, `caddy:2.10.0` (đổi số patch theo `docker pull` mới nhất của major đó khi bắt đầu; ghi vào mục 3). `global.json` rollForward `latestPatch`; `Directory.Packages.props` pin Npgsql 9.x mới nhất.
- C5 Script của học viên gọi `docker compose exec …` **bên trong** (ẩn); học viên chỉ chạy `scripts/<module>/<name>.sh`; Docker Desktop là điều kiện tiên quyết duy nhất (ghi ở STAGE.md và bài `terminal-basics`).
- C6 Ổn định output: `git-playground/build-history.sh` cố định `GIT_AUTHOR_DATE/GIT_COMMITTER_DATE/GIT_AUTHOR_NAME/EMAIL`; `outputs/unstable.regex` gồm Date/ETag/Last-Modified/PID/thời lượng.
- C7 Seed: 5 khách (1 không có đơn), 8 sản phẩm, 12 đơn, id cố định, tiền VND số nguyên.
- C8 Samples console dùng Npgsql 9 (khóa `npgsql` trong versions.yaml, domain `www.npgsql.org` được WebFetch).
- C9 Nếu Docker không chạy được trong phiên: commit script + output placeholder `...` và nhãn "chờ CI capture"; GitHub Actions capture chính thức; không tag cho tới khi capture xong.

### D. Pipeline nội dung
- D-1 Thứ tự sinh theo `path.yaml` (`tools/gen --stage 0`), bài chờ prereq được xếp lại sau. `generated_by` = model id. `versions_used: []` cho bài không có công nghệ.
- D-2 Gate: `type_mix` mặc định single 40 / multi 15 / truefalse 10 / order 10 / fill 10 / scenario 15 (GĐ0: fill → single). `gate0.review.json` chỉ W, không chặn build.
- D-3 Vòng lặp mỗi bài tối đa 3; `claude -p --max-turns 60`; hết vòng mà chưa approved → `status: reviewed`, ghi vào báo cáo cuối và **đi tiếp bài khác** (không dừng cả pipeline).
- D-4 Sau khi mọi bài main path GĐ0 approved: `/translate` (Sonnet) từng bài → `--parity` → `gate 0` (từng track + cross) → `build --publish` → commit `dist/` → push `auto/stage-0`.
- D-5 Chủ duyệt lô sau qua PR `auto/stage-0` → `main`.

### E. App Flutter
- E1 Flutter stable mới nhất; cài `fvm` nếu chưa có (`dart pub global activate fvm`); nếu fvm lỗi, dùng `flutter` toàn cục và ghi vào mục 3. Bỏ cờ `--web-renderer` (đã gỡ ở Flutter mới); PWA `--pwa-strategy offline-first`.
- E2 `flutter create --org dev.lotrinharchitect --project-name lta_app --platforms android,web`; label "Lộ Trình Architect"; `minSdk 26`; icon mặc định.
- E3 Font: tải Be Vietnam Pro (github.com/googlefonts/be-vietnam-pro hoặc fonts.google.com) và JetBrains Mono (github.com/JetBrains/JetBrainsMono release), commit `.ttf` + `OFL.txt`.
- E4 Icon: `Icons.*_rounded` có sẵn, không thêm gói. Golden: `alchemist`, chỉ so trên Linux CI (`platformGoldens: false`); **được tạo golden lần đầu** mỗi milestone bằng `--update-goldens`, sau đó mới áp dụng quy tắc "chỉ khi chủ yêu cầu".
- E5 Bỏ `flutter_local_notifications` khỏi V1 (setting giờ nhắc ẩn). l10n: template `app_vi.arb`, class `L`, `nullable-getter: false`.
- E6 Asset content: M0–M6 dùng `examples/content.sample.json`; M7 `tools/sync-app-content --preview` rồi bản thật khi GĐ0 published. Test `gate_sampler` dùng fixture builder trong Dart (không sửa file sample).
- E7 Integration test chạy `-d chrome` với chromedriver trên CI; `review_scheduler`/`gate_sampler` nhận `Clock`/`Random` tiêm được. "Bài x/y trong module" đếm mọi bài trong module (kể cả nhánh phụ).
- E8 CI: `content.yml` (validate → build → commit `dist/` vào nhánh `auto/*` bằng PAT `AUTO_PUSH_TOKEN` nếu có, nếu không thì upload artifact) → `app.yml` (`workflow_run`, analyze/test/build web+APK) → `actions/deploy-pages` một site gồm `app/` + `content/`. APK ký debug cho tới khi có keystore (A6).

### G. Quyền & headless
- G1 `.claude/settings.json` v1.4 đã mở: `flutter`, `fvm`, `dart`, `dotnet`, `npx/npm/node`, `docker`, `psql`, `gh`, `curl` (chỉ github.com, fonts.google.com, rfc-editor.org, sqlite.org), `mkdir/cp/chmod/mv`, `git init/clone/submodule/worktree/checkout/switch/tag`, `git push origin auto/*` và tag; **deny** `git push origin main`, `git push --force`, `rm -rf`, `WebSearch`.
- G2 Headless: `tools/gen` gọi `claude -p … --model opus` (sinh/quiz/review) và `--model sonnet` (dịch) với `--max-turns 60`; chạy tuần tự theo bài; log vào `logs/gen-<date>.log`; báo cáo cuối `logs/gen-<date>.md`.
- G3 Ngân sách: không có trần cứng ở V1 (chủ chấp nhận ≈ 300–500 lượt Opus cho GĐ0); `gen` in tổng lượt/tokens từ `--output-format json` sau mỗi module.

## 3. Mặc định agent đã chọn trong lúc chạy (agent tự ghi, một dòng mỗi mục, kèm ngày)

- (2026-09-06) A4 áp dụng ở phạm vi **local** (`git config` trong từng repo: `lo-trinh-architect` và `examples/don-hang`), không đổi `--global` — tránh ảnh hưởng cấu hình git khác của chủ trên cùng máy; vẫn đúng danh tính headless yêu cầu.
- (2026-09-06, đã gỡ) Lúc đầu tưởng `gh` chưa cài/chưa đăng nhập (không thấy trên PATH của phiên bash) → hóa ra `gh` ĐÃ cài qua winget (2.100.0) và ĐÃ đăng nhập `duyan11110` (keyring), chỉ là `C:\Program Files\GitHub CLI` chưa nằm trong PATH của phiên bash headless này (mỗi lệnh Bash khởi tạo PATH mới từ profile, không tự thấy thư mục cài thêm giữa phiên) → mọi lệnh `gh` trong phiên này cần `export PATH="/c/Program Files/GitHub CLI:$PATH"` trước. Đã xin phép chủ (bị auto-mode classifier chặn `gh repo create` vì tạo repo public là hành động hướng ngoại) rồi tạo `duyan11110/don-hang` (public), push 2 commit có sẵn, chuyển `examples/don-hang` thành submodule thật theo A1. Repo ví dụ giờ đúng D4/A1.
- (2026-09-06, đã gỡ) Docker Desktop daemon không chạy đầu phiên (`docker ps` lỗi pipe) → agent tự khởi động `Docker Desktop.exe` (D5: được cài/khởi động công cụ còn thiếu); vài phút sau `docker ps` chạy được bình thường. Không cần chờ chủ. Bước 3 hết bị chặn bởi mục này.
- (2026-09-06) `psql` client chưa cài trên máy này → `content/versions.yaml.data.postgresql.version` giữ `"17"` theo D2 nhưng CHƯA xác nhận bằng `psql --version` thật (ghi rõ trong file). Sẽ cài và xác nhận khi vào Bước 3 (cùng lúc dựng container Postgres).
- (2026-09-06) Thêm `examples/don-hang/.gitattributes` (`* text=auto eol=lf`, ép LF cho `.sh`/`.sql`/`Caddyfile`) — không có trong DECISIONS gốc; máy chủ là Windows nên checkout mặc định CRLF, script chạy trong container Linux cần LF để không lỗi `$'\r': command not found`. Lý do: tránh lỗi thực thi, không đổi hành vi được dạy.
- (2026-09-06) Cùng lý do trên, thêm `.gitattributes` ở **gốc** `lo-trinh-architect` (`* text=auto eol=lf`) sau khi Bước 2 sinh các wrapper bash `tools/*` — nếu không ép LF, checkout lại trên máy Windows khác hoặc CI Linux sẽ hỏng các script này.
- (2026-09-06) **Phát hiện quan trọng cho mọi bước sau dùng `claude -p`:** phiên `claude -p` headless bỏ qua toàn bộ `permissions.allow`/`deny` của `.claude/settings.json` với cảnh báo "this workspace has not been trusted" — vì `~/.claude.json.projects["F:/SourceCode/lo-trinh-architect"].hasTrustDialogAccepted` là `false` (khác với phiên tương tác hiện tại, được harness tin cậy qua cơ chế khác). Đã xin phép chủ (AskUserQuestion, vì sửa config toàn cục bị auto-mode classifier chặn) rồi đặt `hasTrustDialogAccepted: true` cho cả 2 khóa case (`f:/...` và `F:/...`) trong `~/.claude.json` (đã backup `~/.claude.json.bak-autopilot` trước khi sửa). Xác nhận lại bằng `claude -p "say hi"` — không còn cảnh báo. **Không có bước này, mọi headless `claude -p` (Bước 3 `/repo-stage`, Bước 5 `tools/gen`, review, dịch) chạy mà không có allow/deny rule nào từ settings.json — rủi ro thật.**
- (2026-09-06) `--max-turns` không xuất hiện trong `claude --help` của bản CLI đang cài (2.1.261) nhưng test thật (`claude -p "say hi" --max-turns 3 ...`) không báo lỗi cú pháp — coi là được hỗ trợ (có thể là cờ ẩn), giữ nguyên trong `tools/gen` như SPEC.md đã viết; nếu một lần chạy thật sau này cho thấy cờ này bị bỏ qua hoàn toàn (không giới hạn lượt), sẽ áp dụng B8 (bỏ cờ, ghi tiếp vào đây).
- (2026-09-06, Bước 3 / `/repo-stage 0`) **Kiến trúc lab của repo ví dụ.** Mọi script bài học chạy **bên trong một "lab box"** (service `lab`, image `lscr.io/linuxserver/openssh-server:version-10.3_p1-r1` + Dockerfile thêm `openssl git postgresql17-client procps`). Mỗi script có 2 dòng mở đầu `[ -f /.dockerenv ] || exec .../lab-run.sh "$0" "$@"`; nội dung dạy bắt đầu ở dòng 7 nên vẫn trích được sạch. Caddy dùng `network_mode: service:lab` để `localhost:8080` có nghĩa như nhau ở mọi nơi. Lý do: C5 yêu cầu giấu `docker compose exec` và chỉ cần Docker Desktop; chạy trong container cho output **giống hệt nhau** trên Windows/macOS/Linux/CI, và giữ `scripts/http/raw-request.sh` **nguyên 6 dòng** đúng như bài mẫu đã `approved` trích (`lines=1-6`).
- (2026-09-06) Ba ngoại lệ của quy ước trên: `scripts/http/raw-request.sh` **không có** 2 dòng mở đầu (hợp đồng với bài mẫu) — `capture-output.sh` tự đưa nó qua `lab-run.sh`; `scripts/terminal/ssh-into-lab.sh` chạy **trên máy chủ** (bài dạy SSH *vào* một máy khác, cần `ssh` trên host); `scripts/debug/run-throws-deep.sh` chạy trên máy chủ (cần .NET SDK, lab box không có).
- (2026-09-06) `outputs/unstable.regex` chuyển sang **regex Perl có `\K`** (`^Date: \K.*$`…) và `scripts/capture-output.sh` mask bằng `perl` thay vì `sed`: mẫu hạt giống `^Date: .*$` sẽ xóa cả tên header, trong khi output mẫu lại là `Date: ...`. Thêm mẫu cho `notBefore/notAfter` của chứng chỉ, pid của `sleep`, và đường dẫn `.cs` trong stack trace. Thêm cờ `--all` cho `capture-output` (SPEC chỉ định nghĩa `<script-path> [--ref]`) để CI và agent chụp toàn bộ bằng một lệnh.
- (2026-09-06) `lab-run.sh` gộp stderr vào stdout **bên trong container** (`bash -c 'exec bash "$0" "$@" 2>&1'`): docker tải hai luồng riêng nên thứ tự tới host là ngẫu nhiên — đã thấy 2 file output đổi thứ tự dòng giữa hai lần chạy. Sau khi sửa, hai lần `capture-output --all` liên tiếp cho file giống hệt nhau.
- (2026-09-06) **A3 áp dụng cho bài mẫu**: output thật của `raw-request.sh` ở stage-0 có **9 header** và `Content-Length: 350` (Caddy 2.10 thêm `Vary: Accept-Encoding`, `nc` gửi `Connection: close`), khác mẫu (7 header / 412). Đã cập nhật khối `text output=true` và hai câu nhắc con số trong **cả `.en.md` lẫn `.vi.md`**, cùng claim 9 trong `.meta.json`. Không sinh lại bài, không đổi `status`.
- (2026-09-06) Thêm `samples/DonHang.Samples.Tests` (xunit) — không có trong manifest 81 file, nhưng C1 đòi `dotnet test` xanh và không có project test nào khác. 7 test, `dotnet build --warnaserror` sạch.
- (2026-09-06) `secrets/` mount **đọc-ghi** (thay vì `:ro`) để sinh được khóa SSH bằng chính container trên máy không có `ssh-keygen` trong PATH của phiên. Khóa và `.env` nằm trong `.gitignore`.
- (2026-09-06) Không sửa được `.claude/settings.json` trong phiên này (Write bị chặn) → dùng `tools/capture-output` (đã nằm trong `Bash(tools/*)`) làm lối vào duy nhất để chạy script của repo ví dụ. `gh` không gọi được (không trên PATH, không export được vì mất khớp allow-rule) → kiểm tra CI bằng `WebFetch(domain:github.com)`.
- (2026-09-06) **Sửa lỗi thật trong `tools/gen`** (do subagent Bước 2 viết, chưa test với `claude -p` thật) trước khi chạy Bước 5: (1) thiếu `export MSYS_NO_PATHCONV=1` — mọi prompt `"/lesson $id ..."` bị Git Bash/MSYS biến thành đường dẫn Windows giống hệt lỗi đã gặp ở `/repo-stage 0` lần đầu; (2) `call_subagent()` thiếu `Agent` trong `--allowedTools` nên không thể tự gọi subagent `review-technical`/`review-junior`; thêm cả hai hàm `Bash(git -C examples/don-hang *)` (skill `lesson` khai báo quyền này). Đã kiểm bằng lệnh thật (`claude -p "/lesson foundation.l1.program-to-process generate" --max-turns 3`): không còn lỗi mangle, không `permission_denials`, `--max-turns` xác nhận có tác dụng thật (dừng đúng ở `max_turns`). Trước khi chạy `--stage 0` đầy đủ, chạy thử 1 module nhỏ (`foundation/computer`) làm pilot.
- (2026-09-07) **Pilot `foundation/computer` lộ thêm 3 lỗi thật, đã sửa cả 3 trong `tools/gen` trước khi mở rộng ra `--stage 0`:**
  1. **`claude -p` thoát khác 0 khi hết `--max-turns` (hoặc lỗi khác) và `set -euo pipefail` giết cả script `tools/gen`** — bài `foundation.l1.files-and-permissions` đang ở giữa `apply-review` (hết 60 turns, chưa xong) thì cả tiến trình chết, bỏ dở 2 bài còn lại của module (`env-and-config`, `threads-and-async-intro` chưa từng được sinh) và không bao giờ ghi `logs/gen-*.md`. Sửa: bọc mọi lệnh `"${cmd[@]}"` trong `call_claude`/`call_subagent` bằng `if ! ...; then log ...; fi` — một lượt gọi không xong không còn làm sập cả pipeline, để đúng vòng lặp round/fix-validation (đã có sẵn trong thiết kế) tự bắt lại.
  2. **Subagent tự chạy nền rồi "báo lại sau" — vô nghĩa trong phiên `-p`, không có lượt nào "sau" để báo.** `review-technical` cho bài trên chạy nền, bị ceiling mặc định 600s giết giữa chừng, cả lượt review mất trắng (không log lỗi gì, chỉ âm thầm thành bản review cũ/partial). Sửa: `export CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0` (đợi vô hạn thay vì giết ở 600s) + thêm câu vào cuối prompt của `call_subagent`: "chạy subagent này ở foreground, đợi nó trả kết quả xong hẳn rồi mới trả lời — đừng chạy nền / báo lại sau".
  3. **Không ai thực sự ghi `status`/`approved_by`/`reviewed_at` vào frontmatter `.en.md`.** Skill `lesson` cố ý "Giữ status: draft. Không tự đặt approved" (đúng thiết kế — việc này thuộc về `tools/gen` theo SPEC.md phần `gen`), nhưng bản `tools/gen` do Bước 2 sinh chỉ theo dõi `final_status` trong biến bash cho báo cáo, **chưa từng ghi ngược vào file** — nghĩa là dù review sạch tuyệt đối, bài vẫn nằm ở `status: draft` mãi mãi, và `/translate` (đòi `status: approved`) sẽ luôn từ chối. Sửa: thêm hàm `set_lesson_status()` (sed 3 dòng frontmatter) gọi ngay sau khi vòng lặp round kết thúc, trước khi gọi `/translate`.
  4. **`TZ=Asia/Ho_Chi_Minh` là no-op trên máy này** — Git Bash/MSYS `date` không có database zoneinfo IANA (`/usr/share/zoneinfo` không tồn tại) nên mọi timestamp thật ra là UTC, không phải giờ Việt Nam như A4 yêu cầu (đã âm thầm sai từ Bước 1). Sửa trong `tools/gen`: `TZ="ICT-7"` (dạng offset cố định POSIX, không cần zoneinfo, Việt Nam không có DST nên tương đương). Các nơi khác dùng `Asia/Ho_Chi_Minh` (Postgres, container Docker) không bị ảnh hưởng — chúng có zoneinfo riêng.
  5. **Hệ quả với 3 bài đã sinh trước khi sửa:** `foundation.l1.memory-stack-heap` review sạch thật (0 lỗi E khi validate) → giữ, gán tay `status: reviewed` (không có script nào làm việc này trước khi sửa). `foundation.l1.program-to-process` (claim n=19 verdict `wrong`, còn 1 blocker) và `foundation.l1.files-and-permissions` (`.review.json` cũ hơn lần sửa bài gần nhất — không còn phản ánh đúng nội dung, 2 claim mới chưa có verdict) **không** đạt trạng thái hợp lệ nào theo `tools/validate` (R03/R04/R02) → trả về `status: draft`, để `tools/gen` sinh lại từ đầu ở lần chạy sau. Chấp nhận mất chi phí đã bỏ cho 2 bài này (ưu tiên dữ liệu đúng hơn tiết kiệm).
- (2026-09-06) **Bốn lỗi chỉ CI mới lộ ra** (máy chủ Windows chạy xanh, GitHub Actions đỏ): (1) bit thực thi — Windows không lưu, `./scripts/up.sh` trả 126; đã `git update-index --chmod=+x` cho toàn bộ 39 script; (2) `docker compose exec` mang stdout/stderr về host theo hai luồng riêng nên thứ tự dòng ngẫu nhiên — `lab-run.sh` gộp luồng **bên trong** container; (3) runner Azure có `search <...>.internal.cloudapp.net`, busybox `nslookup db` chỉ hỏi FQDN rồi NXDOMAIN — thêm `dns_search: ["."]` cho service `lab` (cũng cứu học viên trong mạng công ty); (4) `netstat -tln` trả socket không theo thứ tự cố định — thêm `| sort`. Sau đó CI xanh, tag `stage-0` tạo từ commit đó (`ff911b0`).
- (2026-09-06) Chủ sửa `.github/workflows/ci.yml` thẳng trên GitHub giữa lúc agent đang lặp (commit `c3b1660`, gỡ các bước chẩn đoán). Agent **rebase lên bản của chủ và giữ nguyên bản đó**, chỉ thêm lại một bước `Say what went wrong` phát `::error::` — vì phiên này không đọc được log Actions (`gh` không gọi được, tải log cần xác thực), chỉ đọc được annotation qua `WebFetch(domain:github.com)`. Nếu chủ không thích bước này, xóa được mà không ảnh hưởng gì khác.
- (2026-09-06, `/quiz` bài đầu của `foundation/computer`) **Sửa 5 wrapper bash gọi `dart`** (`tools/validate`, `known-vocab`, `build`, `extract-code`, `merge-outline`): `DIR="$(cd … && pwd)"` cho ra đường dẫn MSYS `/f/SourceCode/…` mà `dart` (binary Windows) không mở được → mọi lệnh `tools/validate …` chạy từ Git Bash đều báo `Could not find file /f/…/bin/validate.dart`. Đổi thành `{ pwd -W 2>/dev/null || pwd; }`: Git Bash trả `F:/SourceCode/…`, Linux/macOS rơi về `pwd` như cũ. Cùng họ lỗi với path-mangling đã sửa ở `tools/gen`; nếu không sửa, `tools/gen` (Bước 5) gọi `"$DIR/validate"` sẽ hỏng ở mọi bài. Không đổi ngữ nghĩa quy tắc nào (B7).
- (2026-09-07, `/lesson foundation.l1.program-to-process fix-validation`) **Lỗ hổng thứ 6 của pilot: `review-technical` sinh `evidence` vượt `maxLength` của schema, và không chế độ nào của `/lesson` sửa được.** `technical.claim_verdicts[23].evidence` (claim n=25) dài 602 ký tự / giới hạn 600 → `validate` báo `E … R01: .review.json không qua schema`. Nhưng `/lesson fix-validation` **chỉ ghi `.en.md` + `.meta.json`** (skill bước 4), nên gọi nó cho lỗi này là vòng lặp vô ích: bài học không có gì sai, mà file hỏng thì nó không đụng tới — `tools/gen` sẽ đốt hết 3 vòng rồi bỏ bài ở `status: draft` mà không ai biết vì sao. Xử lý lần này: cắt đúng 8 ký tự trong `evidence` ("Counter-case checked:" → "Counter-case:"), giữ nguyên toàn bộ dữ kiện của reviewer (nguồn docker-docs, exit 125, counter-case `nc`); không sinh lại review vì nội dung review vẫn đúng, chỉ vượt giới hạn định dạng. **Việc cần làm trước `--stage 0`:** hoặc thêm ràng buộc độ dài vào prompt 04 / agent `review-technical` (nêu rõ `evidence` ≤ 600, `fix` ≤ 400, `text` ≤ 300), hoặc cho `tools/gen` nhận diện lỗi R01-schema-of-review mà tự gọi lại review thay vì `/lesson fix-validation`. Ưu tiên cách 1 (rẻ hơn, không mất bằng chứng đã kiểm).
- (2026-09-07, `/lesson foundation.l1.files-and-permissions apply-review`) **Ba loại issue mà `/lesson apply-review` không sửa được, đã ghi vào `open_questions` thay vì im lặng bỏ qua** — `tools/gen` nên coi chúng là "đã xử lý", không đốt vòng lặp: (1) **hai reviewer mâu thuẫn nhau** — junior đòi giải thích con số trong `chmod 600`, technical (scope major) đòi *bỏ* phần giải mã chuỗi mode vì `depth_notes` đẩy chi tiết chmod bits sang module `terminal`; chọn **scope thắng** (outline là hợp đồng, prompt 02 bước 1). (2) **issue đòi sửa file nguồn sự thật** — minor `kind: version` đòi thêm khóa `lab`/`bash`/`coreutils` vào `content/versions.yaml` pin lab image stage-0 (bash 5.3.9, coreutils 9.11, Alpine) cho 7 claim shell hiện không có `version_key`; CLAUDE.md cấm sửa `versions.yaml` khi không được yêu cầu, và đây không phải "một câu" → để chủ quyết. (3) **issue thuộc file khác** — 2 issue của technical review nhắm vào `.quiz.json` (q5 giải thích còn "the number one cause", q4 đáp án d nói quá so với output); `/lesson` chỉ ghi `.en.md` + `.meta.json` (cùng họ với lỗ hổng thứ 6 đã ghi ở trên) → **phải chạy `/quiz <id> apply-review` cho bài này**, nếu không 1 major sẽ tồn tại mãi và bài không bao giờ đạt `approved` (R03). Ngoài ra bỏ qua 1 minor junior đòi **bold** term trong "Core concepts": `vocab: []` và prompt 02 dành riêng bold cho vocab + câu tin sai ở mục 6.
- (2026-09-07, `/lesson foundation.l1.files-and-permissions apply-review` vòng 2) **Trần cứng của schema/prompt là ràng buộc thật khi apply-review, không phải chi tiết vặt.** Áp một review có 1 blocker + 9 major (2 reviewer) làm bài phình từ 1592 → ~1770 từ, vượt trần L13 (900–1600) và trần "How it works 150–300 từ"; phải cắt ~180 từ ở chỗ review không nhắc để giữ hợp lệ. Đồng thời đụng ba trần schema `meta.schema.json`: `claims` tối đa **25** (phải gộp claim 11+12 — cùng một bảng Exceptions của `File.ReadAllText` — để thêm 3 claim mới bắt buộc), `claims[].text` ≤ 300, `self_check.notes` ≤ 500. **Việc cần làm trước `--stage 0`:** ghi ba trần này vào prompt 02 (mục "The claims ledger") và nhắc trong prompt 04/05 rằng mỗi major đề xuất thêm câu sẽ ăn vào ngân sách từ — nếu không, mọi bài có review dày sẽ đốt hết 3 vòng của `tools/gen` chỉ để chống lỗi L13/M01, giống lỗ hổng thứ 6 đã ghi ở trên.
- (2026-09-07) **Hai reviewer đảo ý giữa hai vòng — xử lý theo vòng mới nhất, ghi lại lý do.** Vòng 1 technical (scope major) đòi **bỏ** phần giải mã chuỗi `-rw-r--r--` vì `depth_notes` đẩy chi tiết chmod bits sang module `terminal`; vòng 2 technical **rút** ý đó và ngược lại báo outline item 2 ("Every file **and folder** has an owner and read/write/execute permissions") bị phủ thiếu, trong khi junior giữ nguyên major đòi giải mã. Chốt: **giải mã bằng chữ** (chín ký tự = ba nhóm ba: owner / group / everyone else) — đây chính là outline item 2; **không** dạy cách mã hóa số đằng sau `600` — đó mới là "chmod bits" mà `depth_notes` hoãn. Nguyên tắc chung: outline là hợp đồng, `depth_notes` chặn *mức chi tiết*, không chặn việc đọc được output mà chính bài đã in ra.
- (2026-09-06) `.gitignore` gốc: thêm `/dist/` (output `tools/build`, không commit tới khi Bước 5 cần), `/examples/don-hang/` (xem mục fallback A1 ở trên), và `/refs/` (D9 clone `dotnet/docs` + `AspNetCore.Docs` + `kubernetes/website` + `docker/docs` + `flutter/website` --depth 1 vẫn nặng ~300–700 MB/repo — commit vào git sẽ làm phình repo nội dung; DECISIONS không nói rõ nên coi đây là cache tái tạo được bằng `tools/fetch-refs`, giống `node_modules`).
- (2026-09-07, `/lesson foundation.l1.env-and-config generate`) **Lỗi thứ 7 của pilot, trong `tools/`: `CodeSource.readFile` giải mã sai UTF-8 → mọi code block có ký tự không phải ASCII đều không bao giờ khớp được.** `tools/src/code_source.dart` chạy `git show <tag>:<file>` với `stdoutEncoding: null` rồi ghép byte bằng `String.fromCharCodes` — mỗi byte thành một code unit, nên `xin chào` trong `scripts/computer/env-demo.sh` đọc ra `xin chÃ o`. Bài này trích đúng nguyên văn 11 dòng của script + toàn bộ file output vẫn bị `E L08` (block không khớp) và `E L08b` (output không khớp), trong khi block C# thuần ASCII cạnh nó khớp bình thường. Sửa (B7, chỉ lỗi kỹ thuật, không đổi ngữ nghĩa quy tắc): `utf8.decode(bytes, allowMalformed: true)`. Sau khi sửa: bài hết lỗi E, `dart test` trong `tools/` 111/111 xanh, `tools/validate --structure-only` và `--module foundation/computer` không sinh lỗi E mới. **Ảnh hưởng rộng hơn:** repo ví dụ có nhiều file văn xuôi tiếng Việt (`docs/team`, `docs/craft`, `docs/git`, `docs/clean-code` — D8) sẽ được trích nguyên văn trong các bài sau; trước bản sửa này **mọi** bài trích chúng đều sẽ đốt hết 3 vòng `tools/gen` vì một lỗi không có thật, giống lỗ hổng thứ 6. `tools/extract-code` dùng chung hàm đọc nên cũng đã đúng (phần hiển thị mojibake trên terminal Git Bash là chuyện của code page console, không phải của dữ liệu).
- (2026-09-07, `/lesson foundation.l1.env-and-config apply-review`) Áp 4 major + 4 minor của technical và 6 major + 6 minor của junior. Xác nhận lại mẫu đã ghi ở mục files-and-permissions: **4 issue nằm trong `.quiz.json`** (q4 bước b sai với repo thật, q2 giải thích sai "lặp y hệt", q5 lệch từ ngữ, q6 chép lại câu của bài) → ghi `open_questions`, **phải chạy `/quiz foundation.l1.env-and-config apply-review`** nếu không bài không bao giờ `approved` (R03). Hai chi tiết mới đáng nhớ: (1) technical bắt được một **khẳng định sai về chính repo ví dụ** ("ba bản sao" — thực ra `.env` được `docker compose` đọc như một *file*, rồi box → script, tức **hai** bản sao) — cùng lúc giải luôn major của junior "đếm mãi không ra ba"; bài học: khi bài mô tả chuỗi truyền giá trị trong repo, phải đối chiếu `docker-compose.yml` + `scripts/dev-secrets.sh` chứ không suy diễn. (2) Minor của junior đòi gọi tên `shell` bị **quy tắc vocab chặn** (glossary term ngoài `known_vocab ∪ vocab`) → chỉ gọi tên `sh -c '…'` là "chương trình được khởi động", từ `shell` chỉ còn trong khối output. Trần L07 (150–300 từ mục 4) là ràng buộc thật lần nữa: sau khi thêm 3 câu theo review phải cắt bớt để dừng ở 295 từ.
- (2026-09-07, `/lesson foundation.l1.env-and-config apply-review` vòng 2) **`review-technical` báo nhầm vì so hash của *tag object* với hash commit — sẽ lặp lại ở mọi bài.** Một minor `kind: code` nói không xác minh được hai block ở `stage-0` vì "tag object `stage-0` là 664d38a còn nhánh đang ở ff911b0, tám commit sau". Thực tế `stage-0` là **annotated tag**: `git rev-parse stage-0` in hash của đối tượng tag, còn `git rev-parse stage-0^{commit}` = `ff911b0` = đúng commit đang checkout. `tools/extract-code --tag stage-0` trả về hai block y hệt và `validate` không báo L08. Không sửa gì, ghi vào `open_questions`. **Việc cần làm trước `--stage 0`:** thêm một câu vào prompt 04 / agent `review-technical` — "để so tag với working tree phải dùng `<tag>^{commit}`, đừng so `git rev-parse <tag>` với `git rev-parse HEAD`"; nếu không, mọi bài sẽ nhận một minor giả cùng loại, và `tools/gen` không phân biệt được minor giả với minor thật.
- (2026-09-07) **Hai reviewer có thể mâu thuẫn nhau về *thứ tự trình bày*, không chỉ về phạm vi — chọn cách thỏa mãn cả hai thay vì chọn bên.** Junior (major, mục 4) đòi văn xuôi đi đúng thứ tự các cạnh của sơ đồ Mermaid (E→A, E→B, A→C, A→D), technical (minor) đòi thêm "bản sao thứ ba" của `printenv`, còn junior (minor) lại đòi **bỏ bớt** một trong hai cách đếm ("hai hop" vs "hai bản sao") vì đọc rối. Giải: sắp lại đoạn văn mục 4 theo đúng thứ tự cạnh, giữ **một** cách đếm ("hai bản sao, mỗi bản tạo lúc khởi chạy") ở mục 4, và đẩy chi tiết bản sao thứ ba xuống mục 5 nơi `printenv` thực sự xuất hiện. Trần L07 (150–300 từ) lại chặn: sau khi sắp lại còn 301 từ, phải cắt đúng 2 từ. Trần M01 (`claims[].text` ≤ 300, `self_check.notes` ≤ 500) chặn thêm hai lần nữa — đúng như mục files-and-permissions đã cảnh báo; các trần này nên vào prompt 02 trước `--stage 0`.
- (2026-09-07, `/lesson foundation.l1.threads-and-async-intro apply-review`) Áp 4 major + 2 minor của technical và 6 major + 10 minor của junior; 2 claim `rephrased` lấy nguyên `suggested_text`, claim `opinion` bị `unverified` được viết lại kèm điều kiện thay vì bỏ (quy tắc "unverified → viết lại ở mức nguyên lý" chỉ áp cho `number`/`syntax`). Ba mẫu đã biết lặp lại đúng như dự đoán: (1) **4 issue nằm trong `.quiz.json`** → `open_questions`, phải chạy `/quiz` cho bài này; (2) **trần L07 (mục 4: 150–300 từ)** chặn thật — sau khi thêm 4 câu theo review phải cắt 12 từ ở chỗ review không nhắc, dừng ở 297; (3) **`self_check.notes` ≤ 500** chặn hai lần liền. Hai xung đột mới, giải theo hướng "thỏa mãn cả hai" thay vì chọn bên: junior (minor) đòi bold cả 4 mục "Core concepts" — prompt 02 dành bold riêng cho `vocab` nên chỉ áp nửa còn lại của fix (**dùng** từ `blocking` trong mục 4 thay vì định nghĩa rồi bỏ đó); junior (minor) đòi bỏ hình dạng "hai `await` nối nhau" khỏi mục 6, nhưng **quiz q6 (`section_ref: 6`) xoay quanh đúng hình dạng đó** và `/lesson` không sửa được quiz → giữ, đồng thời thêm triệu chứng quan sát được ngay trong bài mẫu vào cùng câu.
- (2026-09-07, `/lesson foundation.l1.threads-and-async-intro apply-review` vòng 2) Technical đã `pass` (6 minor), junior còn 4 major + 7 minor. Áp hết major + mọi minor tốn ≤ 1 câu; 2 claim `rephrased` (3, 9) lấy `suggested_text`; claim 16 (`opinion`, `unverified`) **thu hẹp kèm điều kiện** thay vì bỏ — bỏ tần suất ("common at work") khỏi mục 5 và dòng tóm tắt 5, giữ "trong chương trình có màn hình" như một ý kiến có điều kiện. Ba trần format lại chặn đúng như đã cảnh báo: **L12** (tóm tắt ≤ 25 từ/dòng — dòng 5 ra 29 từ sau khi diễn đạt lại theo cơ chế), **M01** `self_check.notes` ≤ 500 (chặn **hai lần** liền), và **R01 `.review.json` vượt `maxLength`** — lần thứ hai gặp lỗ hổng thứ 6: `junior.issues[3].text` dài 617/600 ký tự, mà `/lesson` không ghi `.review.json` nên không chế độ nào tự sửa được; cắt 30 ký tự thừa lời (giữ nguyên toàn bộ lập luận của reviewer). **Kết luận cho việc cần làm trước `--stage 0`: trần độ dài phải vào cả prompt 04 VÀ prompt 05** (trước giờ mới ghi cho 04) — `text` ≤ 300 với issue của junior cũng vượt được, không riêng `evidence` của technical. Lần này thêm claim thứ 20 (`async` modifier + kiểu trả về `Task`) vì junior không đọc được dòng đầu code block; vẫn dưới trần 25 claim.
- (2026-09-07, `/lesson fix-validation` — lời gọi vô nghĩa từ chính `tools/gen`) **Lỗi thứ 8 của pilot, trong `tools/gen`: vòng lặp `pass` của `run_module` chạy thêm 2 lần với một id RỖNG, đốt ~16 lượt Opus cho một bài không tồn tại.** `run_module` kết thúc mỗi pass bằng `queue=("${deferred[@]:-}")`. Dưới `set -u`, `"${mảng_rỗng[@]:-}"` cho ra **một phần tử rỗng** chứ không phải không phần tử — nên sau pass 1 (4 bài thật, 03:37→12:09) `queue` thành `("")`, `${#queue[@]}` = 1, và pass 2 + pass 3 mỗi lần chạy trọn `process_lesson ""`. Bằng chứng trong `logs/gen-2026-09-07.log`: `=== Bài  ===` (12:09:52), `E  S02: lesson id không tồn tại trong content tree`, rồi `claude -p /lesson  fix-validation` (CALL #61 — chính là phiên viết dòng này). Mỗi phantom tốn 2 lượt generate + 4 lượt fix-validation (validate luôn hỏng nên chạy hết `MAX_FIX_VALIDATION`) + 2 subagent review = ~8 lượt, rồi thoát sớm ở `break` vì `review_has_open_severity` trả false trên file không tồn tại. `deferred` **chưa bao giờ được gán** ở đâu trong script — cơ chế "bài chờ prereq xếp lại sau" (D-1) mới chỉ có khung, nên vòng đa-pass hiện chỉ có hại. Sửa: `queue=()` rồi `if [ "${#deferred[@]}" -gt 0 ]; then queue=("${deferred[@]}"); fi`. Cùng dạng `${...[@]:-}` còn ở `write_report_footer` (`REPORT_ROWS`) nhưng vô hại vì đã có `[ -n "$row" ]` chặn.
- (2026-09-07) **Sửa file script trong lúc chính nó đang chạy: chỉ an toàn nếu file KHÔNG dài ra.** Bản sửa trên phải áp ngay trong lúc `tools/gen` đang chạy (phiên này là tiến trình con của nó). Bash **không** đọc cả script vào bộ nhớ lúc khởi động — mục "Tự sửa sai" bên dưới nói vậy là sai. Cái đúng là: **thân hàm** được phân tích và lưu trong bộ nhớ tại thời điểm *định nghĩa*, nên `run_module` đang chạy không đổi hành vi dù file trên đĩa đổi (⇒ bản sửa này **không** cứu được lần chạy hiện tại; pass 3 vẫn sẽ phí ~8 lượt nữa). Nhưng bash vẫn giữ **offset byte** vào file: sau khi `main "$@"` (dòng cuối) trả về, nó đọc tiếp từ offset ≈ EOF cũ. Nếu file **dài ra**, bash sẽ thực thi đoạn đuôi bị đẩy xuống — có thể trúng nguyên dòng `main "$@"` và **chạy lại cả module**. Nếu file **ngắn đi hoặc bằng**, offset vượt EOF → đọc ra EOF → thoát sạch. Vì vậy bản vá được cắt gọn chú thích để `wc -c` giảm 12873 → 12814 byte. Quy tắc chung cho mọi lần sau: **vá script đang chạy thì giữ kích thước không tăng**, hoặc dừng tiến trình trước.
- (2026-09-07, `/quiz fix-validation` — CALL #64, phantom thứ hai của cùng lỗi thứ 8) Pass 2 của `run_module` (bài `""` lúc 12:09) gọi tiếp `/lesson  fix-validation` (CALL #63) rồi `/quiz  fix-validation` (CALL #64). Bản vá `queue=()` **đã nằm trên đĩa** (`tools/gen:290-292`) nhưng lần chạy này giữ thân hàm cũ trong bộ nhớ đúng như đã dự đoán → còn **một pass 3 phantom nữa** (`pass < 3`) trước khi module kết thúc; chi phí đã mất ≈ 16 lượt Opus (~1,2 USD). Chi tiết mới đáng ghi: **cả hai skill `/lesson` và `/quiz` nhận `$0 $1` nên khi id rỗng, chuỗi mode `fix-validation` trượt vào slot `<id>`** — `/quiz` sẽ "sinh quiz cho bài `fix-validation`" và một agent làm theo chữ có thể ghi ra `content/tracks/.../fix-validation.quiz.json`. Cả hai lượt đều trả `blocked` và không ghi file (`tools/validate fix-validation` → `E S02: lesson id không tồn tại`), nhưng đó là nhờ agent tự nhận ra. **Việc cần làm trước `--stage 0`:** thêm một câu vào skill `lesson` + `quiz` — "nếu `<id>` là một trong `generate|fix-validation|apply-review`, coi như thiếu id: dừng, không ghi file"; rẻ hơn nhiều so với dọn một bài rác đã lọt vào content tree.
- (2026-09-07, `/lesson fix-validation` — CALL của pass 3, phantom thứ ba và cuối của lỗi thứ 8) Đúng như dự đoán ở hai mục trên: bản vá `queue=()` (`tools/gen:290-292`) đã trên đĩa nhưng lần chạy này giữ thân hàm `run_module` cũ trong bộ nhớ → pass 3 vẫn gọi `process_lesson ""`, mode `fix-validation` trượt vào slot `<id>` (`Chế độ:` rỗng). Lượt này **dừng ngay ở bước xác minh** (`tools/validate fix-validation` → `E S02`, grep `content/tracks` không có entry nào) và trả `blocked` sau 3 lệnh, thay vì ~8 lượt như hai phantom trước — tổng thiệt hại của lỗi thứ 8 chốt ở ≈ 19 lượt Opus. **Guard đề xuất ở mục trước CHƯA áp được**: `Edit` vào `.claude/skills/lesson/SKILL.md` và `.claude/skills/quiz/SKILL.md` bị chặn quyền trong phiên headless (cùng họ với ghi chú `.claude/settings.json` ngày 2026-09-06 — allow-rule không phủ `.claude/`). Câu cần thêm làm bước 0 của **cả hai** skill: "Nếu `<id>` rỗng hoặc là một trong `generate|fix-validation|apply-review` → coi như thiếu id: dừng, không đọc prompt, không ghi file, in `{"blocked": true}`." Chủ (hoặc một phiên tương tác) áp giúp trước `--stage 0`; bản vá `queue=()` đã đủ chặn nguồn gốc, guard này chỉ là lớp hai.
- (2026-09-07, `/lesson foundation.l1.ip-and-ports apply-review`) **`suggested_text` của reviewer có thể chứa thuật ngữ ngoài `known_vocab ∪ vocab` — chép nguyên văn là tự tạo lỗi L15.** Claim 17 `rephrased` của technical đề nghị "the machine turns the request away at once"; `request` là term glossary (introduced_in `foundation.l1.http-request-response`, sau bài này) → `W L15`. Sửa: "turns it away at once", giữ nguyên ý reviewer. **Quy tắc chung khi áp `rephrased`/`fix`: đọc `suggested_text` như đề xuất về *nội dung*, không phải về *câu chữ* — luôn lọc qua vocab trước khi dán.** Cùng phiên, ba mẫu đã biết lặp lại: 3 issue nằm trong `.quiz.json` → `open_questions` + phải chạy `/quiz`; 1 minor junior đòi gọi tên `tcp` bị vocab chặn (giống `shell` ở `env-and-config`); `self_check.notes` ≤ 500 chặn một lần.
- (2026-09-07, `/lesson foundation.l1.terminal-basics apply-review`) Áp 6 major + 4 minor của technical và 4 major + 7 minor của junior. Hai chi tiết mới đáng nhớ: (1) **`suggested_text` của junior có thể đòi thứ mà hợp đồng section cấm** — junior (minor, mục 7) đòi chuyển câu "cài Git Bash" sang "Before you start", nhưng prompt 02 quy định mục này **chỉ** có đúng một bullet mỗi prereq, không có chỗ cho setup; giải: giữ câu ở bước 1 nhưng gắn nhãn "one-time setup, not part of the three minutes", ghi lý do vào `open_questions`. Cùng họ với "lọc `suggested_text` qua vocab" đã ghi ở `ip-and-ports`: **`fix` của reviewer là đề xuất nội dung, không phải lệnh, và phải qua ràng buộc cứng trước.** (2) **Nới `lines=` của code block là cách rẻ nhất để đóng một issue "gap"** — junior không thấy dòng đưa run vào lab box (bài chỉ trích `lines=7-23`); đổi thành `lines=4-23` (20 dòng, vẫn ≤ 25) đóng luôn 1 minor junior + 1 minor technical mà không tốn từ nào của trần L07/L13. Trần L07 (mục 4: 150–300 từ) lại chặn thật: sau khi thêm câu bắt buộc theo junior major (quy tắc từ đầu chứa `/`) phải cắt 9 từ ở chỗ review không nhắc (đuôi "the machines your program runs on later"); trần `self_check.notes` ≤ 500 chặn một lần nữa. 1 major của technical (q4) + 1 minor junior (q4) nằm trong `.quiz.json` → **phải chạy `/quiz foundation.l1.terminal-basics apply-review`**, nếu không bài không bao giờ `approved` (R03).
- (2026-09-07, `/lesson foundation.l1.pipes-and-filters generate`) **Từ ghép có gạch nối vẫn dính L15**: viết "the slow-response message" (mô tả một dòng output của repo) → `W L15: dùng thuật ngữ "response"`, vì A.2 chỉ đòi ranh giới không phải chữ/số hai phía, mà `-` là ranh giới hợp lệ. Sửa: diễn đạt lại bằng lời ("the message about the payment gateway being slow"). Cùng họ với `shell`/`tcp` đã ghi ở `env-and-config`/`ip-and-ports`: **khi mô tả nội dung một dòng output, đừng mượn chữ trong output làm tính từ ghép** — glossary term nào cũng dính, kể cả khi nó là một nửa của từ ghép.
- (2026-09-07) **Tự sửa sai của chính agent điều phối:** mục "Sửa lỗi thật trong tools/gen" ghi ngày 2026-09-06/07 nói đã thêm `set_lesson_status()` — thực ra **chỉ viết vào DECISIONS, chưa từng thêm vào file `tools/gen`** (chỉ patch tay frontmatter 3 bài lần trước bằng `sed` chạy trực tiếp, không phải qua hàm trong script). Phát hiện khi pilot lần 2 vẫn kẹt ở `status: draft` sau khi review xong. Đã thêm hàm thật + gọi nó trong `process_lesson`, ngay cả khi pilot lần 2 đang chạy nền (an toàn — bash đã đọc toàn bộ script vào bộ nhớ lúc khởi động, sửa file trên đĩa không ảnh hưởng tiến trình đang chạy). Các bài đã qua vòng lặp *trước* thời điểm sửa này (trong lần chạy đang tiếp diễn) sẽ vẫn kẹt `draft` như cũ — cần đối chiếu `review.json`/`tools/validate` rồi gán tay, giống đã làm với `memory-stack-heap`.
- (2026-09-08, `/lesson foundation.l1.pipes-and-filters apply-review`) Áp 1 major + 6 minor trong bài của technical, 7 major + 5 minor trong bài của junior; 3 claim `rephrased` (1, 5, 7) lấy `suggested_text`. Ba điểm mới đáng nhớ: (1) **Inline code được A.2 bỏ qua, nên trích nguyên văn một dòng output chứa glossary term là hợp lệ** — đúng chỗ generate đã dính `W L15` với "the slow-response message" (mục trên), junior lại đòi trích thẳng dòng đó; viết `` `2 PaymentClient slow response from the payment gateway` `` trong inline code: không L15, mà còn **giảm 14 từ** so với câu mô tả vòng vo. Quy tắc: mô tả nội dung output bằng lời thì dính vocab, trích nguyên văn trong inline code thì không. (2) **Nới `lines=` lại là cách rẻ nhất đóng issue "gap"** (đã ghi ở `terminal-basics`): junior không biết `letters.txt` chứa gì vì block bắt đầu ở dòng 10; đổi thành `lines=7-28` (22 dòng, ≤ 25) đưa luôn `cd /tmp` + `printf 'a\nb\na\nc\na\nb\n'` vào tầm mắt, đóng 1 minor mà không tốn từ của trần L13. (3) **Junior major "câu tóm tắt 5 không có mục nào nói" trùng đúng outline item 4** ("...is the first thing you do in production") — outline là hợp đồng nên không bỏ được; giải bằng cách **thêm một câu vào mục 5** thiết lập ý đó (grep cho dòng có lỗi, tail cho cái xảy ra cuối) rồi viết lại dòng 5 kèm điều kiện theo technical minor ("when ... you can reach its log ... usually"), ghi thành claim `opinion` thứ 24. Ngân sách: 1286 → ~1439 từ (trần 1600), mục 4 giữ 263/300 nhờ cắt câu kết của đoạn đi bộ theo sơ đồ. 4 issue nằm trong `.quiz.json` (minor q1/q5 technical, minor q1/q2 junior) → `open_questions`, **phải chạy `/quiz foundation.l1.pipes-and-filters apply-review`**. `python`/`awk` vẫn bị chặn quyền → `word_count` tính tay theo delta, `tools/validate` không báo L05/L07/L12/L13.
- (2026-09-08, `/lesson foundation.l1.dns apply-review` vòng 2) Technical còn 1 major + 5 minor (2 trong số đó thuộc file khác), junior 2 major + 8 minor (2 thuộc quiz); áp hết phần thuộc `.en.md`/`.meta.json`. Bốn điểm mới đáng nhớ: (1) **Đổi nhãn node/cạnh của khối Mermaid là cách đóng issue "sơ đồ khó hiểu" với giá 0 từ** — A.1 bỏ mọi code block khỏi đếm từ, nên nhãn dài bao nhiêu cũng không ăn vào trần L07/L13. Junior (minor) đòi thêm **một câu** giải thích hai cạnh `answer still held`/`nothing held` (≈21 từ, không có chỗ trong mục 4 đang ở 296/300); đổi nhãn thành `it still has the answer from an earlier question` / `it has no answer for this name` và đổi node đầu thành `A program that uses the machine's ordinary name lookup` (theo technical minor) đóng **cả hai** issue mà không tốn từ nào. Cùng họ với "nới `lines=`" đã ghi ở `terminal-basics`/`pipes-and-filters`: trước khi tiêu từ, xem thứ đang không được đếm có nói hộ được không. (2) **Trần L05 (mục 2: 60–120 từ) là ràng buộc thật lần đầu** — mục "Tình huống" đang ở 113 từ, junior major đòi giải thích "five doors" (+7 từ, đúng 120); phải cắt 2 từ ở chỗ review không nhắc (`ever`, `repository's`) để còn 118. Trước giờ chỉ có L07/L13/M01 chặn. (3) **Một claim `rephrased` có thể chỉ vá được nửa câu** — `suggested_text` của claim 8 chỉ nói về "có nhiều name server", trong khi câu gốc còn nửa đệ quy (hỏi tiếp xuống chuỗi); dán nguyên `suggested_text` sẽ làm nửa đệ quy mất chỗ dựa. Giải: claim 8 giữ phần authority + chỗ đổi câu trả lời, **tách nửa đệ quy thành claim 18 mới** (giữ nguyên số n của 17 claim cũ để verdict của reviewer còn truy được), rồi thêm claim 19 theo technical major. (4) **Một `major` của technical có thể sửa xong hoàn toàn trong `.meta.json`** — "câu đếm được nhưng không có claim" (`Five of its lines…`), fix ghi rõ "Leave the sentence as written" → chỉ thêm claim, 0 từ. `self_check.notes` ≤ 500 chặn **hai lần** nữa (đúng như đã cảnh báo ở `files-and-permissions`). Ba issue nằm trong `.quiz.json` (minor q6 của cả hai reviewer, minor q3 của junior) → `open_questions`, **phải chạy `/quiz foundation.l1.dns apply-review`**. Issue `kind: version` (đòi `standards: dns/dns_impl` trong `versions.yaml`) đã được chính technical **hạ từ major xuống minor** ở vòng này và ghi rõ "owner-only" — xác nhận cách xử lý ở vòng 1 là đúng. `tools/validate foundation.l1.dns`: 0 lỗi E.
- (2026-09-08, `/quiz foundation.l1.pipes-and-filters apply-review`) Áp 4 issue `kind: quiz` mà `/lesson` không đụng tới được (technical minor q1 + q5, junior minor q1 + q2). Ba điểm đáng nhớ: (1) **Một issue của junior có thể nhắm tới nhiều câu dù trường `section` chỉ ghi một** — issue q1 kết bằng "Options a, b and c of q4 repeat section 5's prose the same way"; đọc `section` như địa chỉ duy nhất là bỏ sót nửa nội dung, nên đã viết lại cả q4 a/b/c. (2) **Fix của junior có thể phá format cứng của loại câu**: junior đề nghị rút gọn q2 option c thành "False, the two programs run at the same time", nhưng prompt 03 mục 6 bắt `truefalse` option c/d phải mở đầu "False, because …" → giữ tiền tố, chỉ cắt phần đuôi (121 → 83 ký tự, ngang a/b/d). Cùng họ với "lọc `suggested_text` qua vocab" (`ip-and-ports`) và "hợp đồng section thắng `fix`" (`terminal-basics`). (3) **Sửa bài ở mục 4 kéo theo câu hỏi phải sửa dù review không nhắc**: `/lesson` đã đổi ví dụ mục 4 từ `grep ERROR "$log" | head -3` sang `grep ERROR lab/app.log | head -3` (technical minor + junior major), nên đề q2 trích `"$log"` sẽ hỏi về một dòng không còn trong bài; đổi theo. Không sửa `.review.json` (không phải file của `/quiz`; 4 issue này vẫn tính là "đã xử lý" cho `tools/gen`, verdict mới sẽ do vòng review sau ghi). `tools/validate` không báo E nào.
- (2026-09-08, `/lesson foundation.l1.pipes-and-filters apply-review` vòng 2) Technical `fix-required` với **1 blocker nằm trong quiz** + 7 minor trong bài; junior 2 major + 10 minor. 23/24 claim `verified`, không claim nào `wrong`. Ba điểm mới đáng nhớ: (1) **Blocker đầu tiên của cả pilot lại là `kind: quiz`** — q4 option b khẳng định `letters.txt` "holds more than three `a` characters", nhưng `printf 'a\nb\na\nc\na\nb\n'` cho đúng ba chữ `a`, nên `grep -c a` và `grep -o a | wc -l` đều ra 3 và phân biệt "dòng chứ không phải ký tự" **vô hình trong chính ví dụ của bài**. `/lesson` không ghi `.quiz.json` (họ lỗi thứ 6) → ghi `open_questions` và **bắt buộc chạy `/quiz foundation.l1.pipes-and-filters apply-review`**; nếu không, bài không bao giờ `approved` (R03) vì blocker tồn tại mãi. Nửa thuộc về `/lesson` đã sửa: thêm mệnh đề "a line reading `aaa` would still add only 1 to that count" vào mục 5, làm phân biệt hiện ra bằng lời mà không cần đổi repo ví dụ (fix mạnh hơn — sửa `letters.txt` — là prompt 08). (2) **Ba minor `opinion-as-fact` cùng lúc, và cái đắt nhất là câu bài mâu thuẫn với chính ledger của nó** — mục 5 viết "That pair is where you start" trong khi claim 24 và dòng tóm tắt 5 đều đã có "usually"; technical phát hiện đúng bằng cách so thân bài với `.meta.json`. Bài học: khi review vòng trước bắt thêm điều kiện vào claim, **phải rà lại câu tương ứng trong thân bài**, không chỉ ledger. Hai cái còn lại ("cover most log reading", "worth memorising") là khẳng định định lượng/khuyên nhủ chưa hề có claim → thay bằng điều bài chứng minh được. (3) **Junior major "before reaching for any tool" tự mâu thuẫn**: `grep` và `tail` cũng *là* tool, nên câu không loại trừ được gì. Đổi thành "before installing or opening anything else" ở cả mục 5 lẫn dòng tóm tắt 5 — nhưng dòng 5 khi đó thành 27 từ, vượt trần L12 (≤ 25), phải viết lại còn 23 ("…`grep` and `tail` usually start the search…"). Trần L12 chặn lần thứ hai ở bài này. Ngân sách: 1439 → ~1570 từ (trần 1600), mục 4 263 → 283 (trần 300) sau khi thêm câu phân biệt mũi tên đầu của sơ đồ không phải `|` (junior minor). Thêm claim 25 (`name=value` + `"$name"`, ledger gap technical nêu từ vòng trước) — chạm đúng trần 25 claim. Một minor `kind: scope` đòi sửa `track.yaml` (thêm `cut` vào outline item 2) → `open_questions`, cùng họ với các yêu cầu sửa `versions.yaml`: nguồn sự thật là việc của chủ. `sed`/`python`/`awk` vẫn bị chặn quyền → `word_count` tính tay theo delta; `tools/validate` không báo E nào và không báo L05/L07/L12/L13.
- (2026-09-08, `/lesson foundation.l1.http-methods apply-review`) Áp 1 major + 5 minor trong bài của technical, 3 major + 8 minor trong bài của junior (19 claim, không claim nào `wrong`; 1 `unverified` `kind: behavior` nên không chặn D1). Ba điểm mới đáng nhớ: (1) **`L18` (đoạn văn ≤ 6 câu) là trần thứ năm mà apply-review đụng phải, và là trần RẺ NHẤT** — chưa từng ghi ở đây (trước giờ mới có L07, L12, L13, các trần M01 và R01). Thêm 4 câu theo review làm 3 đoạn vượt 6 câu (mục 4 một đoạn 7 câu, mục 5 hai đoạn 7–8 câu); sửa bằng cách **tách đoạn**, tốn 0 từ của trần L07/L13 — khác hẳn L07/L12 luôn bắt phải cắt chữ. Khi review dày, tách đoạn trước, cắt chữ sau. (2) **`fix` của junior lại chứa glossary term chưa dạy** (mẫu `shell`/`tcp`/`response` đã ghi ở `env-and-config`/`ip-and-ports`/`pipes-and-filters`): "say the body is JSON" → `W L15` vì `json` được introduce ở `foundation.l1.json-and-encoding`, sau bài này; giải bằng cách gọi tên header trong inline code (`` set the `Content-Type` header ``) — vừa hết L15 vừa ngắn hơn. (3) **`fix` của technical cho mục 9 vượt trần L12 (≤ 25 từ/dòng)**: câu đề nghị cho dòng 5 dài 32 từ; phải nén còn 24 mà vẫn giữ cả hai thứ reviewer đòi (giữ "may" thay vì "do", và giữ kết luận "một verb sai đánh lừa cả hai"). Cùng họ với "`fix` là đề xuất nội dung, không phải câu chữ" (`ip-and-ports`, `terminal-basics`). Ngân sách: 1397 → 1520 từ (trần 1600), mục 4 giữ đúng 300/300 sau khi cắt 42 từ ở chỗ review không nhắc, mục 2 giữ 114/120. **Bốn issue nằm trong `.quiz.json`** (minor q1 của technical, minor q1/q3/q5 của junior) → `open_questions`, **phải chạy `/quiz foundation.l1.http-methods apply-review`** nếu không bài không bao giờ `approved` (R03).
- (2026-09-08, `/lesson foundation.l1.http-status-codes generate`) **Dạng biến cách của một danh từ glossary cũng dính L15 — mở rộng họ lỗi `shell`/`tcp`/`response`/`slow-response`.** Câu "what that number **commits** the server to" ở mục 1 bị `W L15: dùng thuật ngữ "commit"`, vì A.2 thêm dạng số nhiều đơn giản (+s) và không phân biệt danh từ với động từ. Quy tắc chung: **tránh dùng động từ tiếng Anh thông thường trùng danh từ glossary** (`commit`, `merge`, `branch`, `index`, `cache`, `join`, `conflict`) kể cả khi nghĩa hoàn toàn khác; ở bài này còn phải viết `409` là "clashes with the current state" thay vì gọi tên reason phrase (`conflict` là term của `foundation.l2.git-conflicts`), và gọi `Cookie: role=guest` bằng inline code (`cookie` là term của bài sau) — cùng cách đã dùng ở `pipes-and-filters`. **`L18` (đoạn ≤ 6 câu) lần đầu chặn ở chế độ `generate`, không phải `apply-review`**: mục "Tình huống" liệt kê bảy địa chỉ thành 8 câu ngắn; gộp ba câu status code liền nhau thành một câu là đủ, không mất từ nào (91/120 từ). Kết quả: `tools/validate` 0 lỗi E, chỉ 2 W hợp lệ (`backend.l1.errors-and-problem-details` chưa outline). `awk`/`python` vẫn bị chặn quyền → `word_count` ước lượng bằng `wc -w` trừ code block/frontmatter/H2 (~1390).
- (2026-09-08, `/lesson foundation.l1.dns apply-review`) Áp 4 major + 3 minor của technical, 6 major + 2 minor của junior; 5 claim `rephrased` (2, 3, 4, 5, 8) lấy `suggested_text` sau khi lọc vocab. Bốn điểm mới đáng nhớ: (1) **`claims[].source_hint` ≤ 200 là trần schema thứ tư** (sau `claims` ≤ 25, `text` ≤ 300, `self_check.notes` ≤ 500) và nó bị vượt ngay lần đầu khi ghép bằng chứng thực thi của reviewer vào `source_hint`; ba trần kia đã ghi ở `files-and-permissions`, thêm trần này vào cùng danh sách cần đưa vào prompt 02. (2) **Tên sản phẩm có thể bỏ khỏi văn xuôi mà vẫn giữ `version_key`** — junior (major) không hiểu "Docker"; bài prereq `ip-and-ports` cũng có `versions_used: [docker]` mà không hề gọi tên trong văn xuôi, nên chọn "run by the lab itself" và giữ Docker trong `source_hint` của claim 6/7/11/12/15. Rẻ hơn và không mất bằng chứng. (3) **Reviewer đòi sửa `content/versions.yaml`** (thêm `standards: dns → RFC 1034, dns_impl → RFC 1035`, và `version_key` cho 10 claim) — cùng họ với minor `kind: version` ở `files-and-permissions`: CLAUDE.md cấm sửa `versions.yaml` khi không được yêu cầu → `open_questions`, để chủ quyết. Đây là **major** đầu tiên thuộc loại này; `tools/gen` nên coi là "đã xử lý", không đốt vòng lặp. (4) **Sửa được câu tin sai lấy nguyên văn từ `misconceptions` của outline**: junior (major) báo tiêu đề "One domain name equals one server" dùng hai từ bài không bao giờ dùng (`domain name`, `server`); đổi thành "One name equals one machine" — outline quy định *nội dung* niềm tin sai, không quy định câu chữ, và phần sửa bên dưới vẫn nói đúng điều đó. Bảy issue nằm trong `.quiz.json` (major q4 + minor q2/q3/q5/q6 của technical, minor q1/q6 của junior) → `open_questions`, **phải chạy `/quiz foundation.l1.dns apply-review`**. Chi tiết vặt: `python`/`awk` bị chặn quyền trong phiên này nên `self_check.word_count` được tính bằng tay theo delta so với bản trước (1287 → ~1430); `tools/validate` không báo L07/L13 nên vẫn trong ngưỡng.
- (2026-09-08, `/lesson foundation.l1.tcp-vs-udp generate`) **Lời gọi `generate` gặp `.en.md` đã có sẵn nhưng thiếu `.meta.json` — dấu hiệu một lượt generate trước bị cắt giữa chừng, và cách rẻ nhất là *kiểm tra rồi giữ*, không sinh lại.** File `.en.md` (06:51 cùng ngày) đã đủ 9 mục, code block + output khớp nguyên văn `scripts/network/tcp-connect.sh` và `outputs/stage-0/scripts/network/tcp-connect.txt` ở `stage-0`, `tools/validate` không báo E nào; chỉ thiếu sidecar. Đã đối chiếu lại cả 4 outline item, `known_vocab`, trần L05 (100/120), L07 (255/300), L13 (1372/1600) rồi viết `.meta.json` 23 claim, thay vì đốt một lượt Opus viết lại bài. **Quy tắc chung cho `tools/gen`:** ở mode `generate`, nếu `.en.md` tồn tại mà `.meta.json` không, coi là lượt trước bị cắt — xác minh bài rồi bổ sung sidecar; sinh lại chỉ khi `validate` báo E trong `.en.md`. Hai chi tiết đáng nhớ: (1) `udp`, `packet`, `connection`, `handshake` **không** phải term trong `glossary.yaml` (chỉ `tcp` có), nên dùng thoải mái trong văn xuôi mà không dính L15 và không cần bold — khác trực giác vì bài dạy cả bốn khái niệm. (2) `node` chạy được trong phiên này (G1 cho phép `npx/npm/node`) trong khi `python`/`awk`/`sed` vẫn bị chặn → đếm từ theo A.1 bằng một script node 20 dòng trong scratchpad thay vì ước lượng tay như các bài trước; nên dùng cách này cho mọi bài sau để `word_count` là số thật chứ không phải delta.
- (2026-09-08, `/lesson foundation.l1.shell-scripts apply-review`) **Blocker đầu tiên thuộc loại "không có nguồn nào để xác minh", không phải "bài viết sai" — và nó KHÔNG sửa được trong `.en.md`/`.meta.json`.** Technical báo blocker vì 4 claim `syntax` cốt lõi (1 `#!`, 6 `${1:-word}`, 9 `$?`, 12 `$1`/`$2`/`$0`/`$@`) đều `unverified`: `versions.yaml` không pin khóa `bash`, `refs/` không mirror Bash/POSIX manual, và phiên reviewer bị chặn `bash -c`. Quy tắc prompt 02 ("`unverified` + `number`/`syntax` → viết lại ở mức nguyên lý cho claim biến mất") **không dùng được** ở đây: outline item 1 ("first line names the interpreter") và item 3 ("$1, $2 are arguments") đòi đúng những câu đó — bỏ đi là phá hợp đồng outline. Chính reviewer cũng đề xuất hai cách đều là việc của chủ: (a) thêm khóa `bash` + docs root vào `versions.yaml`, hoặc (b) chạy lại `review-technical` trong phiên được phép `bash -c` (như phiên của `pipes-and-filters`). → `open_questions`, `tools/gen` nên coi là "đã xử lý"; bài sẽ dừng ở `reviewed`, không bao giờ `approved` theo D1 chừng nào chủ chưa chọn (a) hoặc (b). Bốn điểm mới đáng nhớ: (1) **Đổi `kind` của claim là cách hợp lệ để gỡ một claim khỏi tập chặn D1 — khi câu trong bài thật sự đổi.** Claim 8 `rephrased` bỏ dải `0`–`255` theo `suggested_text`, nên nội dung còn lại chỉ là ngữ nghĩa thành công/thất bại → `kind: number` đổi thành `behavior`. Không phải mẹo lách: câu trong bài không còn con số nào. (2) **Chuyển `section` của một claim là cách rẻ nhất để cứu trần L07** khi mục 4 chạm trần mà review đòi thêm chữ. Mục 4 đang 298/300 sau khi áp ~50 từ bắt buộc; dời cơ chế `$?` (claim 9) xuống mục 5 — nơi `$?` mới thật sự xuất hiện trong code (dòng 18) — giải phóng 26 từ ở mục 4 và chỉ tốn 14 từ ở mục 5 (mục 5 không có trần riêng). Cùng họ với "nới `lines=`" và "đổi nhãn Mermaid": tìm chỗ **không bị đếm bởi trần đang chặn** trước khi cắt chữ. (3) **Nhãn node Mermaid nên tránh `[`, `]`, `|` ngay cả trong chuỗi có nháy kép.** Technical đề nghị nguyên văn `B2["[ -f /.dockerenv ] || exec …: …"]`; viết lại thành `B2["line 5: no /.dockerenv here, so exec puts the run inside the lab box"]` — vẫn đóng issue (dòng 5 hiện diện trong sơ đồ) mà không đặt cược vào parser. Sơ đồ lên đúng 8 node = trần A.5. (4) **`suggested_text`/`fix` của reviewer có thể sai về chính repo ví dụ**: junior gọi `lab-run.sh` là "the neighbouring script", nhưng `"$(dirname "$0")/../lab-run.sh"` trỏ lên **thư mục cha** (`scripts/`), không cùng thư mục với `scripts/terminal/backup-db.sh`; đã viết "in the folder above it". Cùng họ với "lọc `suggested_text` qua vocab" (`ip-and-ports`) và "hợp đồng section thắng `fix`" (`terminal-basics`). Bốn issue nằm trong `.quiz.json` (minor q2 + q5 technical, minor q1 + q2 junior) → **phải chạy `/quiz foundation.l1.shell-scripts apply-review`**. Ngân sách: 1364 → 1526/1600 (số thật, đếm bằng script node theo A.1, không ước lượng); mục 2 107/120, mục 4 298/300, dòng tóm tắt 5 phải nén còn 24 từ sau khi bỏ "only" theo technical minor (trần L12). `tools/validate foundation.l1.shell-scripts`: 0 lỗi E.
- (2026-09-08, `/lesson foundation.l1.shell-scripts generate`) **`L17` bắt cả biến thể `recently`, và một bài về `$?` gần như chắc chắn dính.** Tài liệu Bash định nghĩa `$?` là "the most recently executed foreground command", nên cách diễn đạt tự nhiên nhất trong văn xuôi lại đúng chuỗi cấm; bản generate dính 2 lần (mục 4 và mục 6) và đó là **lỗi E duy nhất** của bài. Sửa: "the command that finished just before it" — ngân sách từ đổi −1 ở mục 4, +1 ở mục 6, tổng 0 nên `word_count` giữ 1415. Hai điểm phụ: (1) chuỗi cấm **không** bị quét trong `.meta.json`, nên `claims[].text` giữ nguyên văn tài liệu ("most recently executed…") cho reviewer đối chiếu — cố ý, không phải sót; (2) vì vậy đừng dựa vào `validate` để bắt lỗi từ ngữ trong meta. Cùng họ với `shell`/`tcp`/`response` đã ghi ở `env-and-config`/`ip-and-ports`/`pipes-and-filters`: ràng buộc chuỗi/vocab chặn đúng những từ mà chính chủ đề của bài kéo mình dùng. Bài này cũng lần đầu **trích trọn một script 18 dòng** (≤ 25) kèm khối `text output=true` — cả hai khớp nguyên văn `stage-0` (`tools/extract-code`, `outputs/stage-0/scripts/terminal/backup-db.txt`), và việc trích trọn file làm phần giải thích dòng 5 (`[ -f /.dockerenv ] || exec …`, món nợ `pipes-and-filters` để lại) không tốn thêm block thứ hai.
- (2026-09-08, `/lesson foundation.l1.tcp-vs-udp apply-review`) Áp 1 blocker + 3 major + 8 minor của technical và 7 major + 6 minor của junior; 6 claim `rephrased` (1, 9, 11, 15, 19, 23) lấy `suggested_text`, không claim nào `wrong`. Bốn điểm mới đáng nhớ: (1) **Blocker đầu tiên thuộc loại "không nguồn nào được phép dùng tài liệu hóa được công cụ", và `fix` của nó có HAI phương án — một thuộc prompt 08, một thuộc `/lesson`; chọn phương án trong tầm và ghi phương án kia vào `open_questions`.** Claim 12 (`-w 3` = ba giây) không kiểm được: `refs/` không có `nc(1)`, `versions.yaml` không pin netcat, và reviewer không chạy được Docker. Phương án 1 (chạy `nc -h` + `time` rồi commit transcript có giờ vào repo ví dụ) là **sửa repo ví dụ = prompt 08**, không phải việc của `/lesson`; phương án 2 (bỏ đơn vị khỏi văn xuôi + để Try it bảo người đọc tự bấm giờ) làm được ngay. Đã áp phương án 2: `-w 3` chỉ còn "stops it waiting for ever" (kiểm được bằng chính transcript: lần thử 3 KẾT THÚC và script chạy tiếp), con số "3 seconds" chỉ còn nằm trong khối output nguyên văn. Đồng thời sửa luôn mục 2 ("three seconds" → "a few seconds") dù **không reviewer nào nhắc** — cùng một con số không kiểm được thì không thể chỉ sửa một chỗ. (2) **Mở rộng một claim có sẵn là cách rẻ nhất để lách trần 25 claim khi review đòi thêm claim mới.** Review đòi 3 claim mới (đóng kết nối hai chiều, Caddy giữ cả 8443, hệ điều hành cấp port cho bên gọi) trong khi bài đã có 23; ngoài ra junior (major) đòi giải thích "exit code"/`$?` — vốn cũng là một claim nữa. Giải: gộp claim 12 vào 15 (cùng nói về lần thử thứ ba) và **nới claim 17** (`&&`) để ôm luôn câu về mã thoát — cả hai đều cùng nguồn Bash/transcript. 23 − 1 + 3 = **đúng 25**, chạm trần chứ không vượt. Giữ nguyên `n` của các claim cũ để verdict vòng trước còn truy được (mẫu đã ghi ở `dns`). (3) **Junior đòi BỎ một thuật ngữ mà outline bắt buộc phải có → giới thiệu nó, đừng bỏ.** Junior (major) báo dòng tóm tắt 5 nói "HTTP travels over a TCP connection" mà cả bài không hề nói HTTP là gì, `fix` đề nghị viết lại dòng 5 không có chữ HTTP; nhưng outline item 4 chính là "HTTP rides on TCP". Outline là hợp đồng (đã chốt ở `dns`, `pipes-and-filters`) → giữ HTTP, thêm một mệnh đề ở mục 5 định nghĩa nó bằng lời thường ("The way a browser asks for a page, called HTTP") và lặp lại mệnh đề đó trong dòng 5. Cùng họ với "`fix` là đề xuất nội dung, không phải câu chữ" (`ip-and-ports`, `terminal-basics`). (4) **Thêm một mũi tên vào khối Mermaid đóng được 1 minor technical + 1 major junior với giá 0 từ, nhưng KHÔNG đóng hết** — technical nói đóng kết nối là việc của từng chiều, junior nói mũi tên 4 không được văn xuôi kể lại; thêm mũi tên 5 ("and I with mine") sửa được cái sai, còn cái thiếu vẫn cần một câu thật trong mục 4. Trần L07 (150–300 từ) lại là ràng buộc chặt nhất: 5 chỗ thêm theo review (câu đóng kết nối, câu port 51000, Caddy/8443, đảo chiều "ai hỏi lại", "message 2") tốn ~80 từ nên phải cắt ~45 từ ở chỗ review không nhắc, dừng ở **291/300**; tổng 1372 → 1497/1600, mục 2 101/120. `node` chạy được nên `word_count` đếm thật theo A.1 (script trong scratchpad), đúng như đã ghi ở lượt `generate`. `tools/validate foundation.l1.tcp-vs-udp`: **0 lỗi E**. Hai issue không sửa được ở đây → `open_questions`: 1 minor junior nằm trong `.quiz.json` (q1/q6 chép nguyên câu của bài) → **phải chạy `/quiz foundation.l1.tcp-vs-udp apply-review`**, và 1 minor junior đòi bold mọi mục "Core concepts" (prompt 02 dành bold riêng cho `vocab` — từ chối có chủ ý, giống `files-and-permissions`). Minor `kind: version` đòi thêm `standards: tcp/udp/ip` vào `versions.yaml` + mirror RFC 9293/768/791 vào `refs/rfc/` → việc của chủ, cùng họ với yêu cầu `standards: dns`.
- (2026-09-08, `/lesson foundation.l1.shell-scripts apply-review` vòng 2) Áp 1 major + 4 minor của technical và 5 major + 9 minor trong bài của junior; claim 23 `rephrased` lấy `suggested_text`, không claim nào `wrong`. Bốn điểm mới đáng nhớ: (1) **`L06` (mục "Core concepts" 3–6 bullet) là trần format thứ SÁU và chưa từng được ghi ở đâu** — prompt 02 mục "Hard constraints" không nhắc số bullet, nên minor của junior đòi thêm một bullet `$?` (bài đang có đúng 6) làm `validate` báo `E L06: mục 3: 7 bullet, phải 3–6`. Giải rẻ nhất: **gộp vào bullet có sẵn** ("exit code — … ; `$?` holds the code of whichever command finished just before it") — vẫn cho junior gặp `$?` trong mục 3 như yêu cầu, lại tốn ít từ hơn một bullet mới. Danh sách trần cần đưa vào prompt 02 nay là: L05, L06, L07, L12, L13, L18 + M01 (`claims` ≤ 25, `text` ≤ 300, `source_hint` ≤ 200, `notes` ≤ 500) + R01. (2) **Verdict `unverified` của vòng trước có thể tự đóng ở vòng sau nhờ một mirror KHÔNG liên quan — đừng vội bỏ nội dung ở lần `unverified` đầu.** Blocker vòng 1 chặn 4 claim `syntax` (1, 6, 9, 12); vòng này technical đóng claim 1 bằng `refs/dotnet-docs` (trang `preprocessor-directives` của C# giải thích đúng `#!` + `/usr/bin/env`), claim 6 bằng `refs/docker-docs` (Dockerfile reference gọi `${variable:-word}` là "standard bash modifier"), claim 9 bằng transcript thực thi của chính vòng trước. Chỉ còn **claim 12** (`$1`/`$2`/`$0`/`$@`) chặn D1, và fix vẫn là việc của chủ: (a) thêm khóa `bash` + docs root gnu.org vào `versions.yaml`, hoặc (b) chạy lại `review-technical` khi lab box đang chạy (một lần chạy có 2 tham số đóng luôn claim 21). → `open_questions`, `tools/gen` coi là "đã xử lý". (3) **Dời `section` của claim lần thứ ba cứu trần L07** (đã ghi ở vòng 1 với claim 9): mục 4 phải nhận thêm ~31 từ bắt buộc (câu về dòng 5 theo junior major, viết lại `/usr/bin/env` không dùng "command search path"), nên dời claim 11 (mã thoát của script = mã lệnh cuối) và claim 22 (build/deployment step) xuống mục 5 — nơi câu của junior major thứ 5 phải đứng để dòng tóm tắt 5 có chỗ dựa. Mục 4 dừng ở 295/300, tổng 1582/1600 (đếm thật bằng script node theo A.1). (4) **Vòng apply-review trước bỏ sót 2 minor mà vẫn báo "all applied"** — technical vòng này mở đầu hai issue bằng "Not applied from round 2 and still wrong" ("at every space" lỏng hơn claim 7; "never for the run as a whole" mâu thuẫn với chính mục 7 của bài, nơi `echo $?` gõ ngay sau script ĐÚNG là trả lời cho cả lần chạy). Bài học: sau khi áp xong, đối chiếu lại từng `quote` của review với thân bài, đừng tin ghi chú `self_check` của lượt trước. Thêm: sửa nhãn node Mermaid (bỏ số dòng, ghi rõ `exec` **khởi động lại** file từ dòng 1) + một nhãn cạnh đóng cùng lúc 1 minor `diagram` của technical và 1 major + 1 minor của junior với giá **0 từ** — mẫu đã ghi ở `dns`. Một minor junior nằm trong `.quiz.json` (q4: ba đáp án đúng chép gần nguyên văn ba câu mục 5) → **phải chạy `/quiz foundation.l1.shell-scripts apply-review`**; mục 5 vừa đổi câu chữ về dòng 11-12 và `$( … )` nên đằng nào cũng phải sửa đề. `tools/validate foundation.l1.shell-scripts`: 0 lỗi E.
- (2026-09-08, `/lesson foundation.l1.http-status-codes apply-review`) Áp 2 major + 6 minor của technical và 2 major + 8 minor của junior; 2 claim `rephrased` (1, 8), không claim nào `wrong`, không `unverified` với `kind ∈ {number, syntax}` (claim 21 `unverified` là `behavior`). Bốn điểm mới đáng nhớ: (1) **`suggested_text` có thể đòi một sự thật mà chính repo ví dụ vi phạm — lấy phần bài nói, ghi phần còn lại vào `open_questions`.** Claim 8 `rephrased` thêm "server trả 401 PHẢI gửi `WWW-Authenticate`" (đúng RFC 9110 §15.5.2), nhưng `/admin` ở `stage-0` trả 401 bằng `respond` trơn, không có header đó (Caddyfile:62-68). Dán nguyên `suggested_text` vào claim = ledger khẳng định một luật mà khối output ngay mục 5 phá; viết luật đó vào bài còn tệ hơn. Đã lấy nửa `400` nguyên văn, giữ nửa `401` đúng như bài nói, ghi lý do vào `open_questions`. Cùng họ với "lọc `suggested_text` qua vocab" (`ip-and-ports`) và "hợp đồng section thắng `fix`" (`terminal-basics`): **claim phải bám thân bài, không bám câu chữ reviewer**. (2) **Chuyển một câu từ mục 4 sang mục 5 vừa cứu trần L07 vừa đóng đúng issue** — mẫu "dời `section` của claim" đã ghi ở `shell-scripts`, lần này dời cả câu: câu "reason phrase" rời mục 4 (đang 314/300) xuống cạnh dòng `HTTP/1.1 302 Found` trong khối output mục 5, đóng luôn technical minor `kind: version` (reviewer đòi nói rõ đây là chuyện của thông điệp HTTP/1.1 in ra) mà không phải khẳng định "HTTP/2, /3 không có reason phrase" — điều `refs/` không xác minh được. (3) **Trần L13 (≤1600) là ràng buộc thật lần đầu ở pilot này, và nó chặt hơn L07.** Review đòi thêm ~90 từ; mục 4 quay về 299/300 nhờ cắt 2 chỗ review không nhắc, nhưng tổng chạm **đúng 1600** → phải cắt thêm 6 từ nữa ở mục 5/8 để dừng ở **1594**. Khi review đụng cả 4 mục có chữ, kiểm tổng *trước* khi tinh chỉnh từng mục. (4) **`stack` trong `known_vocab` biến một cụm vô hại thành lỗi nghĩa**: junior (major) đọc "the stage-0 stack" theo nghĩa vùng nhớ đã học ở `memory-stack-heap` → đổi thành "the stage-0 lab site". Nghịch đảo của họ lỗi L15: ở đó term chưa dạy làm hỏng bài, ở đây term ĐÃ dạy làm hỏng bài. Ba yêu cầu sửa nguồn sự thật → `open_questions` (owner-only, cùng họ `standards: dns/tcp`): `http_messaging` → RFC 9112 trong `versions.yaml`, `vocab: [proxy, reason phrase]` trong `track.yaml` + `glossary.yaml`, và outline item 4 viết lại cho đúng 503 (reviewer nói rõ **không** sửa bài). 1 minor junior nằm trong `.quiz.json` (đáp án đúng của q2/q3 chép nguyên câu bài) → **phải chạy `/quiz foundation.l1.http-status-codes apply-review`**. `tools/validate foundation.l1.http-status-codes`: **0 lỗi E**; `word_count` đếm thật theo A.1 bằng script `node` trong scratchpad.
- (2026-09-09, `/lesson foundation.l1.ssh-and-remote apply-review`) Áp 2 blocker + 4 major (bài) + 5 minor của technical và 7 major + 6 minor (bài) của junior; 3 claim `rephrased` (3, 5, 14) lấy `suggested_text`, 1 claim `wrong` (3). Bốn điểm mới đáng nhớ: (1) **Blocker "không có nguồn được phép" lần này CÓ phương án trong tầm `/lesson`, khác `shell-scripts`/`tcp-vs-udp`.** Blocker 2 chặn 4 claim OpenSSH (9 `syntax` `-i`/`-p`/`user@host`, 10 từ chối khóa riêng ai cũng đọc được, 11 ba tùy chọn `-o`, 12 host key đổi) vì `versions.yaml` không pin khóa `openssh` và `refs/` không mirror `ssh(1)`/`ssh_config(5)`. Reviewer cho hai đường: (a) thêm `openssh` + docs root vào `versions.yaml` — sửa nguồn sự thật, việc của chủ; (b) bỏ phần ngữ nghĩa tùy chọn, chỉ mô tả điều output đã ghi lại chứng minh. Chọn (b) vì outline **không** đòi giải thích từng cờ (khác `shell-scripts`, nơi outline item 1/3 đòi đúng `#!` và `$1`) — và (b) đóng luôn 1 major `kind: scope` (`depth_notes`: "Không nói về ssh config") + 2 major về `-o` và "identity khi rebuild" + 2 major gap của junior về host key. **Một blocker về nguồn có thể đồng thời là blocker về phạm vi; khi thế, cắt là lời giải rẻ nhất, không phải thêm nguồn.** (2) **Thay 4 claim không kiểm được bằng MỘT claim phạm vi repo là cách giữ được câu chữ dạy học mà không khẳng định cú pháp.** Claim 9 mới ("script kết nối bằng `secrets/lab_key` với user `dev` tới lab box ở địa chỉ của chính máy chủ, chạy ba lệnh") kiểm được bằng `scripts/terminal/ssh-into-lab.sh` + `outputs/stage-0/.../ssh-into-lab.txt`, nên văn xuôi vẫn nói được "dòng đó nêu ba thứ — khóa riêng để ký, user để làm, máy để tới" mà không nói cờ nào nghĩa gì. Ledger 18 → 15 claim. (3) **`request` là thuật ngữ glossary (chưa dạy ở GĐ0 track terminal) và câu tự nhiên nhất khi tả xác thực SSH dính ngay** — `suggested_text` của claim 3 chứa "its own login request", dán thẳng vào bài là `W L15`. Viết lại thành "the name it asks to log in as". Cùng họ với `recently`/`shell`/`tcp`/`stack` đã ghi: **lọc `suggested_text` qua `known_vocab` trước khi dán, kể cả khi reviewer đúng về nội dung.** (4) **`self_check.word_count` của lượt trước sai 93 từ (1330 ghi, 1423 thật) — đừng ước lượng, và ở máy Windows này `python`/`awk` bị chặn.** Cách đếm đúng A.1 rẻ nhất: tạo tạm `tools/bin/wc_tmp.dart` gọi `countWords` trong `tools/src/markdown_util.dart` (đúng cài đặt mà `validate` dùng), `dart run`, rồi `mv` file tạm ra scratchpad (`rm` bị chặn trong repo). Nên bổ sung một cờ `--word-count` vào `tools/validate` thay vì lặp lại mẹo này. Ngân sách: mục 4 phải cắt 7 từ về 296/300 sau khi thêm câu vân tay kết nối; dòng tóm tắt 5 nén còn 23 từ (trần L12 = 25); tổng 1423/1600. Ba issue quiz (q1 `answer` phụ thuộc bước challenge đã bị xóa, q4 option c + giải thích, q4/q5 chép nguyên câu bài) → **phải chạy `/quiz foundation.l1.ssh-and-remote apply-review`**. `tools/validate foundation.l1.ssh-and-remote`: **0 lỗi E**.
- (2026-09-14, `/lesson foundation.l1.json-and-encoding generate`) **Sample C# không có file output đã capture** (`capture-output` chỉ chụp `scripts/`), nên không được đặt khối `text output=true` sau block C#. Mặc định: chạy thật `dotnet run --project samples/DonHang.Samples -- <sample>` trên máy chủ ở `stage-0`, trích từng dòng output **inline** trong văn xuôi, ghi claim với `source_hint` là lệnh đó. Thêm: `versions.yaml` chưa có khóa cho JSON (RFC 8259) → claims JSON là `fact`, ghi `open_questions` cho chủ.
- (2026-09-14, `/lesson foundation.l1.ssh-and-remote generate` — lượt đầu của `tools/gen` chạy lại sau khi lần 09-09 dừng vì rate limit) **Mở rộng quy tắc `tcp-vs-udp` (09-08): ở mode `generate`, nếu CẢ `.en.md` lẫn `.meta.json` đã tồn tại, nguồn sự thật không đổi kể từ lúc viết, và `validate` không báo E nào trong hai file đó → giữ nguyên, không sinh lại.** `process_lesson` luôn gọi `generate` trước (`tools/gen:215`), kể cả với bài đã qua 2 vòng apply-review; sinh lại sẽ vứt bỏ công sức review và bắt reviewer bắt đầu lại từ đầu. Đã kiểm: `track.yaml`/`glossary.yaml`/`versions.yaml` sửa lần cuối 09-06, `stage-0^{commit}` vẫn `ff911b0`; bài 09-09 23:38. Lỗi E duy nhất là **R01 ở `.review.json`** (họ lỗ hổng thứ 6, lần thứ ba): `technical.claim_verdicts[0].evidence` 632/600 → cắt 37 ký tự thừa lời (`rfc6265, rfc9110…` → `(6265, 9110…)`, `repository` → `repo`…), giữ nguyên mọi dữ kiện. `.review.json` (09-10) mới hơn bài và còn 3 major technical + 4 major junior chưa áp → việc của vòng `apply-review` kế tiếp trong `tools/gen`, không phải của `generate`. Nên thêm vào `tools/gen` một nhánh bỏ qua `generate` khi cả hai file đã có và validate sạch.
- (2026-09-14, `/quiz foundation.l1.ssh-and-remote generate`) Cùng quy tắc mục trên, áp cho quiz: `.quiz.json` (09-09 23:41) mới hơn `.en.md` (23:38), `track.yaml` không đổi từ 09-06, `validate` 0 lỗi E, mọi `misconception` khớp outline/mục 6, Bloom 1/3/2/0 và `section_ref` 3–6 đúng prompt 03 → **giữ nguyên, không sinh lại** (sinh lại vứt công 2 vòng apply-review). 2 minor `kind: quiz` (q2, q5) trong `.review.json` là việc của `/quiz … apply-review`, không phải `generate`.
- (2026-09-14, `/quiz foundation.l1.tls-and-https generate`) **Mặt ngược của quy tắc mục trên: `.en.md` (09-14 20:49, vừa qua `/lesson … generate`) mới hơn `.quiz.json` (09-09) → không giữ nguyên, nhưng cũng không viết từ trắng.** Bài mới đổi sơ đồ bắt tay (key material của trình duyệt ở bước 1, bước 4 "proof" có văn xuôi riêng) và thêm câu "a copied certificate cannot finish the exchange". Đối chiếu từng câu cũ với bài mới: giữ q2, q3 (đã áp minor q3 của junior), sửa giải thích q1d (nói key material gửi *sau* certificate — sai với bước 1 mới) và q4d (lý do về bản sao private key — bài không nói), q5 thêm bước 4, q6 bỏ `request` (term chưa học, chưa có trong `known_vocab`) và thêm phương án mang misconception thứ hai; thêm q7 cho nội dung mới. Bloom 1/4/2/0 (7 câu), độ khó TB 2.14, `section_ref` 3–6. `.review.json` cũ không còn khớp bài — `tools/gen` phải chạy review lại cả bài lẫn quiz.
- (2026-09-14, `/lesson foundation.l1.ssh-and-remote apply-review` vòng 3) Áp 6/7 major + 8 minor (bài) của technical, 6 major + 10 minor (bài) của junior; không claim `wrong`/`rephrased`. Hai điểm mới: (1) **Technical cho "cắt hoặc thêm claim", junior major đòi dạy đúng thứ đó → chọn thêm.** Mệnh đề host key ở mục 8 (technical: cắt, hoặc thêm claim + một câu ở mục 5) trùng junior major (mục 5 nhắc "kiểm tra máy trả lời" mà chưa từng dạy) và technical minor mục 4 (điều kiện "không ai ở giữa đọc được"); một câu ở mục 4 + claim 23 (`behavior`, `unverified` không chặn D1) đóng cả ba. (2) **Dời trọn một đoạn khỏi mục có trần là cách rẻ nhất khi review dồn vào mục đó**: mục 4 nhận ~5 câu mới; đoạn scp/rsync + lịch sử (≈35 từ) chuyển sang cuối mục 5, nơi đoạn "không có lối tắt" vốn đã nói về chép file — mục 4 còn 292/300. Tổng 1590/1600 sau khi cắt ~95 từ ở chỗ review không nhắc. Ledger 20 → 24 claim. Quiz (q2, q5, q6) → `/quiz … apply-review`.
- (2026-09-14, `/quiz foundation.l1.ssh-and-remote apply-review`) Áp 3 mục: q5 stem đánh dấu lệnh đã lược (`...` + "ba tùy chọn `-o` được lược đi"); q2 option c gộp technical ("chữ ký *của bạn*") + junior (lời mới, theo file box lưu cho `dev`); q6 giải thích b nhận lối tắt mount `secrets/`. **Mở rộng một chỗ ngoài danh sách:** câu `correct` của q2 ("so that machine never learns it") vướng đúng major §7 (box được cho thấy cả file private) → đổi thành "never needs it". Quy tắc: **một issue về sự thật của repo (ở đây mount `secrets/`) áp cho mọi câu quiz khẳng định điều ngược lại, không chỉ câu reviewer gọi tên** — nếu không, vòng review sau sẽ bắt lại đúng lỗi đó ở q2. Không đổi key, Bloom, `section_ref`.
- (2026-09-14, `/lesson foundation.l1.tls-and-https apply-review`) Áp 1 blocker + 2 major + 7 minor (bài) của technical, 7 major + 3 minor (bài) của junior; 8 claim `rephrased`. Hai điểm mới: (1) **Hai reviewer mâu thuẫn về câu tần suất lấy từ outline** (item 4 "one of the most common production incidents"): junior chỉ đòi bỏ thuật ngữ nhưng giữ "often", technical đòi bỏ hẳn tần suất vì không nguồn nào đếm sự cố → chọn technical (đúng trước, rồi mới đủ outline): claim 24 `opinion` thành `behavior` (hết hạn là một ngày cố định), lời outline ghi `open_questions` cho chủ. (2) **Blocker `syntax` về cờ không có nguồn (`-servername`) đóng bằng cách bỏ nghĩa cờ, giữ phần transcript chứng minh được** — cùng đường (b) như `ssh-and-remote`; claim 18 đổi `kind` sang `behavior` vì câu còn lại chỉ tả điều output cho thấy. 1595/1600, mục 4 299/300. Quiz (q1, q3, q5, q6, q7) → `/quiz … apply-review`.
- (2026-09-14, `/quiz foundation.l1.tls-and-https apply-review`) Áp 5 mục: q7 giải thích c lấy nguyên `fix` technical (certificate *này* ghi tên, không phải "certificate không bao giờ ghi địa chỉ"); q5 option d đổi thứ tự "key material, then certificate"; q1 option a và q6 option b bỏ lời chép bài (q1 tả ba thứ theo việc trình duyệt làm với chúng, q6 nêu `donhang.local` + "thông điệp đầu tiên"); q3 (junior major) giải thích b dựa vào câu mới của bài (danh sách máy lab cũng không có authority của Caddy). **Theo quy tắc ssh-and-remote, căn thêm các câu quiz khẳng định ngược với bài vừa sửa:** q3 a/correct/c đổi "judge/không xét" → "không dừng vì bên ký" (bài cố ý tránh nói `s_client` không kiểm tra); q1 d + giải thích, q4 giải thích a "the key" → "the keys" (technical minor "không có một key duy nhất"); q6 `correct` thêm "as the lab is set up" (technical major §9, ECH); q5 option a thêm chữ ký theo bước 3 mới của sơ đồ; q1 `correct` bỏ câu "ba thứ ấy là đúng thứ ba phép kiểm tra đọc" (public key dùng cho chữ ký lần trao đổi, không phải một trong ba phép). Không đổi key, Bloom, `section_ref`, độ khó.
- (2026-09-14, `/lesson foundation.l1.ssh-and-remote apply-review` vòng 4) Áp 2 major + 4 minor (bài) của technical, 8 major + 8 minor (bài) của junior. Hai mặc định mới: (1) **Chạm trần 25 claim mà review đòi tách một claim và thêm một claim → gộp hai claim cùng nguồn repo (22 vào 8) và để trống `n=22`, thêm `n=25`, `n=26`.** Schema chỉ đòi `n` ≥ 1, không đòi liên tục; giữ `n` cũ để verdict vòng trước còn truy được (mẫu `dns`). (2) **Junior đòi giá trị cụ thể cho một kết quả Try it mà không capture nào ghi (đường dẫn `pwd` trên box), Docker không chạy → bỏ bước không kiểm được thay vì bịa giá trị**; kết quả mong đợi dựa vào `ls /` (claim 12). Trần L13 lại chặt nhất: +~90 từ theo review, cắt ~85 từ ở chỗ review không nhắc, dừng ở 1595/1600, mục 4 294/300. Quiz (q2, q3, q4) → `/quiz … apply-review`.
- (2026-09-14, `/quiz foundation.l1.ssh-and-remote apply-review` vòng 4) Áp 4 mục: q4 option a lấy `fix` technical (trùng ý junior major: "`ssh` đọc trên laptop và không bao giờ gửi qua kết nối" thay "file ở lại trên laptop"); q3 giải thích đổi đuôi thành "không phải vì shell của chính bạn đọc thư mục"; q2 giải thích bỏ câu đồng nghiệp, thay bằng câu bài chứng minh được (box chỉ cần nửa `.pub` cho `dev`). Theo quy tắc mục 144, căn thêm giải thích q1 ("private key that never leaves your disk" → "never sent across the connection") — cùng chỗ mơ hồ với q4a khi box được cho thấy `secrets/`. Không đổi key, Bloom, `section_ref`, độ khó.
- (2026-09-14, `/lesson foundation.l1.tls-and-https apply-review` vòng 2) Áp 3 claim `rephrased` (1, 4, 19), 1 major + 3 minor (bài) của technical, 6 major + 7 minor (bài) của junior. Ba mặc định mới: (1) **Junior major "thiếu bước" có thể đòi một sự thật mà nguồn chỉ phủ một phần → viết kèm rào "is expected to" và ghi phạm vi nguồn vào `open_questions`**: "authority chỉ ký cho tên sau khi kiểm người xin kiểm soát tên" chỉ có nguồn là mục ACME challenges của Caddy (cert công khai), không nguồn pin nào nói cho mọi authority trong danh sách → claim 26 `behavior` (không chặn D1). (2) **Một câu đúng (đã `verified`) nhưng làm junior rối (máy thứ ba, "tries to") → cắt câu, dồn phần cần thiết của claim vào câu còn lại** ("Nothing in the lab puts that authority on your browser's list"); rẻ hơn thêm câu giải thích và nhả ~25 từ cho các major khác. (3) **`outputs/unstable.regex` chỉ che trong bản capture** (header của file nói vậy) → junior thấy mâu thuẫn "ngày bị che" vs Try it "so ngày với hôm nay": sửa câu mục 5 thành "bản in này che, lần chạy của bạn in ngày thật" thay vì bỏ bước Try it. Hết chỗ claim (25) → gộp 10 vào 11 (cùng nguồn RFC 8446 CertificateVerify), để trống `n=10`. 1595/1600, mục 4 294/300. Quiz (q5 major, q6 major, vị trí đáp án) → `/quiz … apply-review`.
- (2026-09-14, `/quiz foundation.l1.tls-and-https apply-review` vòng 2) Áp 3 mục: q5 option a bỏ "then" (technical major, nguyên văn `fix`); q6 lấy `fix` technical (cửa hàng online, "normally") thay vì `fix` junior (Đơn Hàng giả định đặt trên mạng) — cùng đóng lỗi "lab trỏ về 127.0.0.1", nhưng giả định Đơn Hàng chạy công khai là điều repo không có; thêm "normally" vào giải thích d cho khớp mục 6. **Vị trí đáp án (junior minor): hoán đổi đáp án đúng với một phương án sai** (q1 a↔c, q2 a↔d, q3 a↔b, q7 a↔d; misconception và giải thích đi theo phương án) — không viết lại chữ, không đổi Bloom, `section_ref`, độ khó. Theo quy tắc mục 144: giải thích q3 (phương án lab box) còn dựa vào câu "Caddy thử cài authority" mà bài đã bỏ → viết lại bằng điều bài còn nói (danh sách của lab box cũng không có authority).
- (2026-09-14, `/lesson foundation.l1.time-and-timezones generate`) **Mở rộng mục 139 sang file `db/queries/*.sql`:** `outputs/stage-0/scripts/sql/run-query.txt` chỉ chụp query mặc định (`select-basics`), nên một bài trích query khác không có khối `text output=true` hợp lệ. Mặc định: chạy thật file đó trong lab (`docker exec donhang-lab psql … --file /repo/db/queries/<name>.sql` ở `stage-0^{commit}`), trích kết quả **inline** trong văn xuôi + Try it, ghi claim có `source_hint` là lần chạy đó; đề xuất script capture riêng (prompt 08) vào `open_questions`, không tự thêm.
- (2026-09-14, `/lesson foundation.l1.time-and-timezones apply-review`) **Junior major "từ chưa biết" mà `fix` đòi đổi `prereqs`/thứ tự bài (sửa `track.yaml`, việc của chủ) → nếu từ đó KHÔNG phải term glossary, giải thích bằng lời thường ngay trong bài và ghi yêu cầu đổi prereq vào `open_questions`.** `database`/`table`/`column`/`row`/`SELECT` không có trong `glossary.yaml` nên không dính L15; một mệnh đề ở mục 2 ("the program that keeps Đơn Hàng's data… one row per order and one column per fact") + một mệnh đề mỗi từ khóa SQL ở mục 5 đóng 5 major mà không chờ chủ. Trần L05 chạm đúng 120/120, L13 1597/1600; mục DST dời 4 → 6 (claim 9) để giữ L07 282/300. Quiz (q1, q6) → `/quiz … apply-review`.
- (2026-09-15, `/lesson foundation.l1.oop-encapsulation generate`) **Try it kiểu "sửa một chữ trong repo ví dụ rồi chạy lại" được xác minh bằng chính lần sửa đó, không suy luận.** Mở rộng mục 139 sang `dotnet test`: sửa tạm working tree `examples/don-hang` (ở `stage-0^{commit}`), chạy lệnh, ghi kết quả thành claim `behavior` có `source_hint` là lần chạy, rồi hoàn nguyên và kiểm `git status` sạch. Không commit gì vào repo ví dụ.
- (2026-09-15, `/quiz foundation.l1.oop-encapsulation apply-review`) Áp 3 mục: q5 giải thích d lấy nguyên `fix` technical (tổng tiền không ai tự đặt được, bỏ "không thể lệch khỏi các dòng" — int tràn số); q4 a/c và q6 a viết lại theo `fix` junior (bỏ lời chép Try it/mục 5). Theo mục 144, căn thêm: giải thích q4 d, q5 b, q6 c và **chữ của phương án q5 b** thêm "setter chỉ lưu giá trị" — sau khi bài thừa nhận setter có thể kiểm quy tắc, "getter + setter public" trơn làm đáp án q5 mơ hồ; q3 đề "các dòng" → "các dòng đơn hàng" (cạnh "dòng 6" code, junior minor mục 5 của bài). Không đổi key, Bloom, `section_ref`, độ khó. `validate` 0 E.
- (2026-09-15, `/lesson foundation.l1.oop-encapsulation apply-review` vòng 2) **Junior đòi nới `lines=` khi block đã chạm trần 25 dòng → không nới (mất `MarkPaid`), dùng phương án thay thế trong `fix`: một câu nói dòng 1–10 khai báo gì, ghi vào claim có sẵn.** Mặt ngược của mẹo "nới `lines=`" (`terminal-basics`, `pipes-and-filters`): chỉ rẻ khi còn chỗ dưới trần. Thêm: junior hỏi `OrderExposed` có phải code thật không → câu "chưa có ứng dụng ở tag này" (STAGE.md) đặt ở đầu mục 5 thay vì mục 2 vì L05 đang 114/120. 1560/1600, mục 4 269/300. Quiz q5 → `/quiz … apply-review`.
- (2026-09-15, `/quiz foundation.l1.naming apply-review`) Áp 2 mục `kind: quiz` trùng nhau của hai reviewer: q3 option c và giải thích d của q2 chuyển sang thể giả định ("any line that called `Calc` … would show") vì mục 5 nói không gì trong sample gọi `Calc`. Theo mục 144, căn thêm giải thích `correct` và d của q3 (cùng thể giả định; "stays where it is written" theo câu mới ở mục 6) và **giải thích q1** — `/lesson` vòng 2 đã đổi cạnh `no: switch to it` sang "Keep the name", nên câu "thấy team dùng từ khác thì quay lại ô đầu" sai với sơ đồ dù không reviewer nào nhắc (quiz 21:09 cũ hơn bài 21:45). Quy tắc: **khi `/lesson apply-review` sửa sơ đồ/luồng, soát câu `order` và giải thích của nó dù review không gắn cờ.** Giữ chữ "ordinary" ở q3 vì bài đã định nghĩa nó (junior minor mục 6 đã đóng ở bài).
- (2026-09-15, `/quiz foundation.l1.oop-encapsulation apply-review` vòng 2) Áp 1 mục (junior minor q5): thêm vào đề, en + vi, "Assume the team also adds a method for adding lines" — đặt giữa mục tiêu và câu hỏi, đuôi đổi "achieves that" → "achieves that goal" cho khỏi trỏ nhầm; không kéo dài phương án a (đúng `fix`). Issue nói giải thích không nhắc chuyện thêm dòng → giải thích `correct` "the lines out of reach" → "reachable only through the class's own methods". Theo `open_questions` + mục 144: giải thích q4 d mở bằng câu gần nguyên văn Suggested answer của Try it ("The property looks almost the same as before") → "Only one word differs from before". Không đổi key, Bloom, `section_ref`, độ khó. `validate` 0 E.
- (2026-09-15, `/lesson foundation.l1.tables-keys-relations apply-review` vòng 2) Technical `pass` (5 minor), junior 3 major + 7 minor trong bài; claim 2, 3 `rephrased`. Hai mặc định mới: (1) **Script node đếm từ phải thay inline code bằng `' X '` (có khoảng trắng hai bên), đúng như `countWords`** — dấu câu dính sau inline code thành token riêng; bản thay bằng `'X'` đếm thiếu 8 từ ở mục 4 (312 so với 320 của `validate`). (2) **Chú thích thuộc tính trong `erDiagram` (`integer order_id PK, FK "with product_id, one primary key"`) đóng issue "sao bảng có hai PK" với giá 0 từ** — cùng họ đổi nhãn Mermaid ở `dns`; A.5 không bị ảnh hưởng. Tránh "left empty" cho NULL (chuỗi rỗng không phải NULL) → "leaves it with no value". Mục 4 299/300 sau khi cắt ~25 từ review không nhắc; tổng 1390. Quiz (q2, q4, q5 + căn q3 theo §3 mới) → `/quiz … apply-review`.
- (2026-09-15, `/quiz foundation.l1.tables-keys-relations apply-review`) Áp 3 minor junior: q2 a tả hệ quả ("đơn có ba sản phẩm → ba dòng `order_items`") thay lời ghép mục 4 + tóm tắt, giải thích `correct` căn theo; q4 a đổi sang ca cụ thể (sản phẩm 2 lên 500,000, đơn 1 vẫn 450,000/đơn vị); q5 đề nói rõ `customers.id` (khóa chính) giữ số điện thoại thay bộ đếm, bộ phận hỗ trợ sửa `id`. Căn q3 `correct` theo §3 mới ("Trong Đơn Hàng … bảng nó trỏ tới"). **Mặc định mới:** "giữ số giống nhau hai ngôn ngữ" (prompt 03 §8) hiểu là cùng giá trị; phần `vi` viết nghìn bằng dấu chấm (`500.000 đồng`) theo prompt 06 §7, số trong code giữ nguyên. Không đổi key, Bloom, `section_ref`. `tools/validate`: 0 lỗi E.
- (2026-09-15, `/lesson foundation.l1.oop-polymorphism generate`) **Claim "đoạn này không biên dịch được" (mã lỗi CS…) được xác minh bằng build nháp NGOÀI repo, không suy luận.** Mở rộng mục 153: chép nguyên văn file ví dụ ở `stage-0` vào một project console tạm trong scratchpad (tạo bằng Write, vì Bash không được ghi ra ngoài repo), thêm vài dòng thử, `dotnet build` bằng đường dẫn Windows (`C:/…`; đường `/c/…` bị MSBuild đọc thành switch), ghi mã lỗi thật vào `source_hint` (CS0534, CS1503). Code thử chỉ để kiểm, không vào bài; bài chỉ tả bằng lời.
- (2026-09-15, `/lesson foundation.l1.small-functions generate`) **Cặp "trước/sau" của repo ví dụ không phải lúc nào cũng tương đương hành vi — đối chiếu từng khối trước khi viết "hai bản làm y hệt".** `PlaceOrderSplit.FirstProblemWith` đổi vòng lặp kiểm từng dòng của `PlaceOrderLong` thành hai lời gọi `Any`, nên với dòng (1, 0) + (0, 450_000) hai bản trả thông điệp khác nhau (xác minh bằng sửa tạm `Program.cs` theo mục 153); test `BothVersionsAgree` chỉ phủ một đơn hợp lệ. Mặc định: dạy đúng sự thật ("dời nguyên văn là an toàn; viết lại trên đường dời là thay đổi thứ hai"), giữ câu tuyệt đối của outline ở mức ý kiến có điều kiện, ghi `open_questions` (repo muốn bản tách thuần → prompt 08).
- (2026-09-15, `/lesson foundation.l1.oop-polymorphism apply-review`) **Outline item đúng nhưng thiếu điều kiện (không sai hẳn) → bài nói kèm điều kiện, `open_questions` báo chủ, không `blocked`.** Item 1 ("gọi method qua base type → chạy method của kiểu thật") sai với method không `virtual` (mặc định C#); technical major đòi giới hạn định nghĩa + tóm tắt dòng 1 vào *abstract method*. Prompt 02 bước 1 ("sai → dừng") dành cho item sai với phạm vi bài; ở đây mọi method trong bài đều abstract nên câu có điều kiện vẫn thiết lập đúng item — thêm claim 21, không sửa `track.yaml`. Junior `fix` dùng "branches" cho sơ đồ → `branch` là glossary term → viết "arrows".
- (2026-09-16, `/quiz foundation.l1.oop-polymorphism apply-review`) Áp 6 mục: q5 đề theo `fix` technical major ("theo quy tắc của bài về khi nào một `switch` là đủ, nên chọn cách nào"), giải thích b ("vẫn chạy được… bài dành nó cho…"), `correct` bỏ "đọc nhanh hơn" → "giữ quy tắc ở một chỗ"; q6 giải thích bỏ "cách thứ hai" → "cách khác" (en + vi); q3 c bỏ lời chép "plain `ShippingFee`". **`fix` junior xung đột với trần độ dài prompt 03 mục 6/8:** q5 a rút còn "A single `switch` in one method" (31 ký tự, ~30% phương án khác) → vi phạm "within 60%"; giữ ý (không lặp điều kiện của đề) bằng cách tả *hình dạng* lựa chọn ("trong method in hóa đơn, viết một lần, mỗi cách thanh toán một dòng"), dài ngang d. q6 b: `fix` junior dùng "a second kind of type" trùng lỗi technical vừa bắt → viết "another kind of type, one that unrelated classes can all be called through". Theo mục 144, căn theo bài mới: `misconception` q6 a → tên mới ở mục 6 ("Deriving from a base type…"; `inheritance` không thuộc vocab), q6 đề/a/giải thích a "direct base class" → "chỉ ghi được một class sau `:`", "base class" → "base type"; q4 giải thích "below it" → "that derives from it". Không đổi key, Bloom, `section_ref`, độ khó. `validate` 0 E.
- (2026-09-16, `/lesson foundation.l1.small-functions apply-review`) **Đếm từ A.1 thật của `validate` tính dấu câu dính sau inline code thành một từ riêng** (`countWords` thay inline code bằng `' X '`, nên `` `Place`, `` → `X` + `,`). Script tự viết thay bằng `'X'` đếm thiếu ~2% (bài này: tự đếm 1586, `validate` báo 1614 → `E L13` bất ngờ). Khi tự đếm, thay inline code và `[[id]]` bằng `' X '` (có dấu cách) như `tools/src/markdown_util.dart:137`. 1 minor junior thuộc `.quiz.json` (q5) → phải chạy `/quiz foundation.l1.small-functions apply-review`.
- (2026-09-16, `/quiz foundation.l1.small-functions apply-review`) Áp 1 mục (junior minor q5): option c (en + vi) bỏ lời chép Try it ("checks every quantity before any price") → "the checks on the lines now run in a different sequence, so for some bad orders the two versions return different messages". Không lấy nguyên `fix` ("the checks were reordered on the way"): hai check đầu (`customerId`, `lines.Count`) giữ thứ tự, và "on the way" lại chép mục 5 ("rewriting them on the way"). Giải thích `correct`/b đã khớp (b: "that order changed"), không đổi. q4 a "first line" (junior minor style mục 4 có nhắc) để nguyên: bài giờ định nghĩa "first line, the one with its name and parentheses". Không đổi key, Bloom, `section_ref`. `validate`: 0 lỗi E.
- (2026-09-16, `/lesson foundation.l1.sql-select apply-review`) **Junior hỏi "chạy lại `scripts/up.sh` có an toàn không" → trả lời bằng claim kiểm được, và thêm khóa `docker` vào `versions_used` dù bài không dạy Docker.** Nguồn: `dev-secrets.sh` chỉ tạo file khi thiếu + `compose_up.md` trong `refs/docker-docs` (chỉ tạo lại container khi cấu hình/image đổi, giữ volume) → claim 24 `behavior`, `version_key: docker`. `fix` junior "lab box là nơi PostgreSQL và psql chạy" sai với repo (PostgreSQL ở box `db`) → chỉ nói psql chạy ở lab box, repo hiện ở `/repo` (`./:/repo:ro`). 1398/1600, mục 4 296/300.
- (2026-09-16, `/lesson foundation.l1.oop-polymorphism apply-review` vòng 2) **`suggested_text` của claim `rephrased` tự nó vượt trần `claims[].text` ≤ 300 (305) → bỏ tiền tố phiên bản "In C# 14, "**, vì `version_key: dotnet` đã mang đúng thông tin đó; không đổi chữ nào khác của reviewer. Thêm: junior major "sao không dồn `switch` vào một method dùng chung" trả lời bằng ý kiến có điều kiện (bước đầu đủ khi chỉ có một câu hỏi; `switch` thứ hai theo cùng các kind thì class trả công) và **căn luôn câu điều kiện ở mục 5** ("same `switch` in a second place" → "a second `switch` over the same kinds") dù review không nhắc, vì câu cũ mâu thuẫn với bước method dùng chung. Mục 4 299/300 nhờ dời câu "diamond không phải code bạn viết" vào nhãn Mermaid. Quiz (q4, q5, q6) → `/quiz … apply-review`.
- (2026-09-16, `/quiz foundation.l1.sql-select apply-review`) **`.review.json` không có issue `kind: quiz` nào → không ghi `.quiz.json`, không bump gì.** Thay vào đó rà quiz theo mục 144 với bài đã sửa (01:53, sau quiz 01:18): không câu nào khẳng định thứ tự khác thứ tự chèn (technical major §9 — q2/q5/q6 đều nói "không được hứa"), không dùng `Linux`/`database`/"moves itself", q1 khớp "`SELECT` chỉ quyết định cái được in", 2 `misconception` khớp nguyên văn mục 6, Bloom 1/3/2/0 khớp hàng GĐ0 ±1. `validate` 0 E. Mặc định: `apply-review` với 0 mục quiz = kiểm tra căn chỉnh, sửa chỉ khi lệch.
- (2026-09-16, `/quiz foundation.l1.oop-polymorphism apply-review` vòng 2) Áp 2 mục `kind: quiz` + 2 mục căn theo `open_questions`: q4 giải thích `correct` thêm "trực tiếp" (technical minor, nguyên văn `fix`); q5 đề lấy `fix` junior ("trade-off between a `switch` and one class per kind"). Theo mục 144, căn q5 giải thích b/d theo mục 4/5 mới: chỗ thứ hai cần nhãn → dời `switch` vào một method dùng chung; class chỉ đáng công khi *một câu hỏi thứ hai về các loại* cần `switch` riêng (giải thích cũ "chỗ thứ hai cần cùng `switch` → class" mâu thuẫn bài). q6 giải thích a dùng ca class thanh toán của mục 6 để cho thấy dữ kiện "một class sau `:`" là lý do cần cách kia. **q4 c giữ nguyên**: câu mới ở mục 4 đã nói đúng bước đó, sửa thêm chỉ làm đáp án sát chữ bài hơn. Không đổi key, Bloom, `section_ref`, độ khó. `validate` 0 E.
- (2026-09-16, `/lesson foundation.l1.small-functions apply-review` vòng 2) **Junior hỏi "`NamingAfter` là gì, có sẵn hay mới tách" → trỏ về bài prereq đã trích nguyên thân nó ("the function quoted in the naming lesson"), vài từ thay cho một câu giới thiệu class/file.** Trước khi giải thích lại một symbol từ đầu, xem khối trích của prereq. Junior major "`Place` bản tách vẫn cần 'and' để tả" → trả lời bằng ý kiến có điều kiện (thân chỉ gồm lời gọi có tên hướng về một kết quả thì qua phép thử), gộp vào claim 18. `suggested_text` claim 17 dài 330/300 → bỏ đúng mệnh đề cuối (claim 2 đã phủ). Nhãn `opt` Mermaid đổi thành "only when a problem came back" (0 từ). 1595/1600, mục 4 294/300; claims chạm trần 25. Quiz (q3, q5 giải thích a, căn q4) → `/quiz … apply-review`.
- (2026-09-16, `/quiz foundation.l1.small-functions apply-review` vòng 2) Áp 3 mục (en + vi): q3 giải thích a bỏ "`TotalVnd` là một vòng lặp" (junior minor; bài không cho thấy thân nó) → "một vòng lặp trông như một việc"; q5 giải thích a thêm điều kiện của tóm tắt dòng 2 (technical minor: "không viết lại các bước… miễn là nơi gọi kiểm tra thứ trả về"), đồng thời **bỏ luôn "phần lớn việc tách là như vậy"** vì mục 4 mới nói tổng/giảm giá được *thay* bằng lời gọi chứ không dời; q4 giải thích d căn theo mệnh đề mới ở mục 4 ("trừ khi giá trị bị dời vào field static, việc đó chỉ giấu nó"). Không đổi key, Bloom, `section_ref`, độ khó. `validate` 0 E.
- (2026-09-16, `/lesson foundation.l1.oop-interface-vs-abstract apply-review`) **Junior hỏi "object khác vào bằng cách nào" khi bài chưa được dạy DI (`depth_notes` chặn) → nói *hình dạng* ("thôi tạo bằng `new`, nhận vào, ví dụ qua parameter") ngay trong câu hỏi của mục 2, còn "ai tạo" để cho bài DI trong Connections.** Sơ đồ + mục 4 đổi `holds a variable` → `receives a parameter` (0 từ). Claim 18 sau khi thay `production` vượt 300 → nén, giữ cả mệnh đề cuối vì thân bài còn nói. 1590/1600, mục 4 282/300, claims chạm 25. Quiz (q1, q3, q4, q6) → `/quiz … apply-review`.
- (2026-09-16, `/quiz foundation.l1.oop-interface-vs-abstract apply-review`) **Câu outline trong `misconceptions` dùng từ người học chưa biết (`'do abstraction'`) → trường `misconception` của quiz lấy câu tin sai ở mục 6 của bài** ("two interchangeable ways to write the same thing"); Q06 chấp nhận khớp outline *hoặc* bullet mục 6, nên không cần sửa `track.yaml`. Áp junior minor q3 c (nêu lý do — field riêng mỗi notifier, interface không khai báo được — thay lời chép bài), bỏ `production` ở giải thích q1 d, bỏ "do abstraction" ở q1 c/q3 a/q6 b. Theo mục 144: "variable" → "parameter" ở q1/q2/q4 (mục 2 + sơ đồ mới), giải thích q4 d dựa vào định nghĩa `implements` mới (kế thừa class đã implement), giải thích q4 `correct` thêm "class tạo được bằng `new`" (technical major mục 5), giải thích q3 `correct` nói rõ cái gì hỏng (junior major mục 6). Không đổi key, Bloom, `section_ref`, độ khó. `validate` 0 E.
- (2026-09-16, `/quiz foundation.l1.code-smells-basic apply-review`) Áp 4 mục `kind: quiz` (en + vi): q5 giải thích `correct` lấy nguyên `fix` technical ("a new limit or big-city price must go into both" — luật khách thân thiết có thể là một guard clause, không phải gõ hai lần); q5 option c lấy `fix` junior ("have to be edited in two places"; vi vốn đã "phải sửa ở hai chỗ"); q6 option b lấy `fix` technical ("another city with and without express"); q6 option a theo `fix` junior: năm đơn Hà Nội dưới `5000` g, tổng `100000`–`1900000`, có/không giao nhanh — chi tiết ngang b nhưng vẫn chỉ tới một `return` (giải thích a "all five"; `correct` thêm "both sides of every `? :`" theo câu mới ở mục 4). Theo mục 144, căn thêm theo câu chữ mới của bài: q3 giải thích `correct`/a/e bỏ "neither is a wrong price" / "today's prices are the expected ones" / "the prices are right today" → "by itself, shows that a price is wrong" (technical + junior major mục 4); q2 option a + `correct` và q4 giải thích a bỏ "nothing shows which edit" → "the check cannot show" (claim 20 rephrased; mục 6 "no check can tell you"). Không đổi key, Bloom, `section_ref`, độ khó. `validate` 0 E.
- (2026-09-21, `/lesson foundation.l2.debugger-and-logging apply-review`) Áp 2 major + 4 minor của technical và 3 major + 8 minor của junior; 2 claim `rephrased` (14, 15) lấy `suggested_text`, không claim nào `wrong`, không `unverified`. Ba điểm mới đáng nhớ: (1) **`L04` (mục 1: số bullet = số `prereqs`, tập id phải trùng) là trần thứ sáu mà apply-review đụng phải** — junior major đòi thêm bullet `[[foundation.l2.debugging-method]]` vì `hypothesis`/`narrowed` học ở đó; không thêm được nếu không sửa `prereqs` trong `track.yaml` (việc của chủ). Giải theo mẫu `time-and-timezones`: hai từ đó **không** phải term glossary nên định nghĩa thẳng bằng lời trong mục 4 ("your hypothesis, the one guess about the cause you are checking now"; "the guess has not been narrowed yet: it still covers more of the run than one stop can settle"), thêm nửa câu vào mục Liên hệ chỉ nơi giới thiệu, ghi yêu cầu đổi `prereqs` vào `open_questions`. (2) **Nới `lines=` đóng cùng lúc 3 issue** (mẫu `pipes-and-filters`/`terminal-basics` lặp lại lần thứ ba): khối `WrongTotal.cs` 9-15 → 7-23 (17 dòng, ≤ 25) đưa `Run`, `orderOne` và hai dòng `expected/got` vào tầm mắt — đóng junior minor "order 1 là gì", junior minor "`Run` chưa từng thấy" và đỡ cho junior major "hai con số nào" ở mục 2, tốn 0 từ của trần L13. (3) **Reviewer bắt được lỗi số học nội bộ mâu thuẫn giữa hai mục của cùng bài**: mục 5 nói prints để lại "print three lines for every order" trong khi mục 7 của chính bài tính ra 4 dòng cho đơn 3 món; đúng là `n + 1` dòng cho `n` món → viết lại thành "one line per order plus one for each item added" và thêm claim 20 `number`. Bài học: khi một con số xuất hiện ở hai mục, kiểm chéo trước khi đóng vòng. Ngân sách: mục 4 phải cắt 25 từ ở chỗ review không nhắc để về 299/300 sau khi thêm 4 câu theo review; mục 2 116/120; tổng 1367 → 1456/1600. Claim 18 → 21 (thêm 19 nội dung stack trace, 20 số dòng log, 21 phải step mới tới `return`). **4 issue nằm trong `.quiz.json`** (technical minor q6, junior minor q1/q5/q6) → `open_questions`, **phải chạy `/quiz foundation.l2.debugger-and-logging apply-review`** nếu không bài không bao giờ `approved` (R03). `tools/validate foundation.l2.debugger-and-logging`: **0 lỗi E**; `word_count` đếm thật theo A.1 bằng script node trong scratchpad.
- (2026-09-22, `/lesson management.l1.scrum-from-junior-seat generate`) **Khối `markdown file=` trích file văn xuôi không được chứa dòng `## `**: `validate` tách mục H2 trên toàn file, không bỏ qua fenced block → bốn heading của `docs/team/sprint-example.md` bị đếm thành mục của bài, kéo theo 6 lỗi giả (L02 sai thứ tự mục, L08 đếm sai số dòng khối, L09/L10/L11/L12 rỗng). Không sửa tool (không đổi ngữ nghĩa quy tắc, CLAUDE.md §8); chọn lại đoạn trích **không có heading**: bảng sprint backlog (lines=12-18) và ba câu daily + hai dòng lý giải (lines=22-27), phần còn lại của file (độ dài, thành phần đội, mục tiêu sprint, sprint review, retrospective) kể bằng văn xuôi và ghi thành claim. Quy tắc này áp cho mọi bài sau trích `docs/*/*.md`. 1547/1600 từ, mục 2 95/120, mục 4 237/300, 19 claim, `validate` 0 E 0 W.
- (2026-09-22, `/lesson management.l1.scrum-from-junior-seat apply-review`) Áp 2 major + 12 minor của technical (1 thuộc quiz) và 8 major + 8 minor của junior (3 thuộc quiz); 7 claim `rephrased` lấy `suggested_text`. Bốn điểm mới đáng nhớ: (1) **Outline có thể chứa một khẳng định định lượng không nguồn** — outline item 4 viết "Most teams run a modified Scrum", technical (minor `opinion-as-fact`) chỉ ra không câu nào ở scrumguides.org đỡ cho lượng từ "most". Giải: **giữ phần thực chất của outline item** (biết bản tham chiếu thì phân biệt được adaptation lành với adaptation hỏng, claim 19) và **bỏ lượng từ** ở mục 5 + dòng tóm tắt 5; ghi `open_questions`. Khác các lần trước ("outline là hợp đồng, scope thắng"): ở đây hợp đồng chỉ mất một tính từ chứ không mất ý. (2) **Junior đòi giấu tên nguồn, technical đòi gọi tên nguồn — thỏa mãn cả hai bằng cách buộc tên vào thứ người đọc đã đọc**: junior major nói "the reference" từ trên trời rơi xuống (không kiểm được), technical minor đòi nêu tên và đặt `versions_used: [scrum]`. Giải: thêm một câu đầu mục 4 ("…above are what the word Scrum names; the reference description of them is a document called the Scrum Guide") rồi mục 5 viết "the Scrum Guide summarised in Core concepts" — junior có thứ để đối chiếu, technical có tên nguồn; `versions_used: [scrum]` khớp `standards.scrum` (khóa trong `standards` dùng **phẳng** trong `versions_used`, giống `http`). Thêm claim 21 `kind: history` cho chính cái tên (prompt 00: nêu tên tài liệu kinh điển = claim history). (3) **Claim `unverified` không thuộc `number`/`syntax` vẫn nên bỏ khi junior cũng không đọc được từ đó** — claim 15 (Kanban) `unverified` vì không nguồn nào được phép phủ Kanban, đồng thời junior major báo "Kanban"/"continuous flow" là hai tên không định nghĩa ở câu cuối mục 5. Bỏ hẳn câu → mất luôn claim 15; **giữ khoảng trống ở n=15** (không đánh số lại) để verdict của reviewer còn truy được — schema chỉ đòi `n` nguyên ≥ 1, `validate` không đòi liên tục. (4) **Ngân sách từ là ràng buộc nặng nhất của vòng này**: review đòi thêm ≈ 156 từ (1547 → 1703 nếu chỉ thêm); phải cắt ≈ 110 từ ở chỗ review không nhắc, làm hai đợt, mới về 1592 (script node) / 1596 (validate). **Validate đếm nhiều hơn script A.1 tự viết đúng 4 từ vì ba dòng HTML `<details>`/`<summary>`/`</details>` của mục 7 — lấy số của validate cho `self_check.word_count`.** Mục 4 phình 237 → 292/300 vì gánh 5 fix cùng lúc (định nghĩa tên Scrum, daily thuộc developers + 15 phút + mọi ngày làm việc, tiêu chuẩn "xong" gồm gì, review là working session chứ không phải trình diễn). Bốn issue nằm trong `.quiz.json` (technical minor q6, junior minor q3 ×2 + q6) → `open_questions`, **phải chạy `/quiz management.l1.scrum-from-junior-seat apply-review`** nếu không bài không bao giờ `approved` (R03). `tools/validate management.l1.scrum-from-junior-seat`: **0 lỗi E**.
- (2026-09-22, `/quiz management.l1.scrum-from-junior-seat apply-review`) Áp đúng 4 issue `kind: quiz` (technical minor q6; junior minor q3 option a, q3 nhãn misconception, q6 options a+b), đóng nốt phần `open_questions` mà `/lesson` không đụng tới được. Hai chi tiết đáng nhớ: (1) **`fix` của reviewer lại chứa từ bài không hề có** — technical đề nghị option b là "an estimate in points", trong khi bài chỉ nói team "sizes each piece of work with a number … the Guide names no unit"; chép nguyên văn sẽ dựng lên một đơn vị không tồn tại trong bài. Viết lại theo *ý* ("The number beside each piece of work is in a unit the team chose for itself."), đúng quy tắc đã ghi ở `ip-and-ports`: `fix`/`suggested_text` là đề xuất nội dung, phải lọc qua bài + vocab trước khi dán. (2) **Nhãn `misconception` phải theo câu chữ của BÀI, không phải của outline** — outline ghi "The daily standup is where I prove I worked yesterday.", bài (mục 6) ghi "The daily is where I prove I worked yesterday."; app hiện nhãn này cho người học nên `standup` là từ chưa gặp ở đâu trong bài. Prompt 03 cho phép cả hai nguồn ("appears in the outline or the lesson") → khi hai nguồn lệch nhau, **chọn bản của bài**. Đồng thời sửa `explanation.correct` của q6 (en+vi) cho khớp key mới; không đổi key, Bloom, `section_ref`, độ khó. `tools/validate management.l1.scrum-from-junior-seat`: **0 lỗi E**.
- (2026-09-22, `/lesson management.l1.user-story-and-ac generate`) **Mở rộng mục 176: khối `markdown file=` cũng không được chứa `**bold**`.** `docs/team/story-example.md` có ba đoạn đáng trích, nhưng câu story (lines 5-6) chứa `**khách hàng**`, `**hủy đơn hàng chưa thanh toán của mình**`, `**không phải gọi tổng đài…**` — mà ràng buộc "bold chỉ dành cho `vocab` ở mục 3 và câu tin sai ở mục 6" không loại trừ fenced block (cùng họ với `## ` ở mục 176: validate quét cả file; `stripForTermMatch` chỉ dùng cho L15). Chọn hai khối **không heading, không bold**: tiêu chí chấp nhận (lines=10-19) và Definition of Done (lines=23-27); câu story được **kể lại bằng tiếng Anh trong văn xuôi** (claim 4) thay vì trích — mất chữ gốc nhưng giữ đủ ba phần ai/cái gì/vì sao. Quy tắc cho mọi bài sau trích `docs/*/*.md`: trước khi chọn `lines=`, loại cả dòng `## ` lẫn dòng có `**`. Ba chi tiết khác: (1) **outline item 2 chứa `estimated` mà `depth_notes` hoãn story point sang GĐ1** → viết "nobody can say how big it is" / "size the work", nêu tên cột `Ước lượng` của sprint note mà không giải thích đơn vị; (2) tránh `code review`/`pull request` (term của `management.l1.code-review-basics`, bài sau) → dòng Definition of Done kể lại thành "at least one other person has read and approved the code"; (3) claim 18 nói thẳng Scrum Guide 2020 **không** dùng hai chữ `user story`/`acceptance criteria`, để reviewer kiểm được thay vì bài ngầm gán chúng cho Scrum — và theo mục 177 (bỏ lượng từ không nguồn), câu kết mục 5 viết "the Guide leaves it to a team to decide how the work itself gets written down" thay cho "most teams". 1503/1600 từ (script A.1 khớp `countWords`), mục 2 107/120, mục 4 255/300, 21 claim, `tools/validate management.l1.user-story-and-ac`: **0 lỗi E**.
- (2026-09-22, `/lesson management.l1.user-story-and-ac apply-review`) Áp 2 major + 7 minor (bài) của technical và 7 major + 5 minor (bài) của junior; 7 claim `rephrased` (6, 7, 13, 16, 18, 20, 21) lấy `suggested_text`, không claim nào `wrong`. Bốn điểm mới đáng nhớ: (1) **Thêm một bullet vào mục 3 là cách chuyển ngân sách từ ra khỏi trần L07 mà vẫn đóng được major "từ chưa định nghĩa".** Junior (major) đòi giải thích `test` ngay tại chỗ dùng đầu ở mục 4 (~20 từ), nhưng mục 4 sau khi áp review đã gần trần 300; đặt định nghĩa thành bullet thứ 5 của "Core concepts" ("an automatic test — a small program that puts the system into a state and reports whether a statement about it held") thì chữ rơi vào mục 3 (không có trần riêng; L06 cho 3–6 bullet nên 4 → 5 vẫn hợp lệ), mục 4 giữ nguyên câu cũ và thuật ngữ được định nghĩa **trước** khi dùng. Cùng họ với "nới `lines=`" / "đổi nhãn Mermaid": tìm chỗ không bị cái trần đang chặn đếm. (2) **Đếm chữ của một chuỗi tiếng Việt trong văn xuôi tiếng Anh là một claim, và gần như chắc sai.** Technical (major) bắt "Six words and a number" tả `Nút "Hủy đơn" trên màn hình đơn hàng` — 8 token khoảng trắng, hoặc 5 từ vựng tiếng Việt, không cách nào ra 6 (số 6 thật ra đếm bản dịch tiếng Anh). Bỏ hẳn con số ("A title and a number") đóng luôn câu hỏi kết mục 2 ("the six words leave out" → "the title leaves out"). Quy tắc: **đừng đếm chữ của chuỗi không phải tiếng Anh**; nếu vẫn muốn, phải là claim ghi rõ cách đếm. (3) **Xóa hẳn một câu là cách rẻ nhất khi nó vừa là `skipped_step` của junior vừa nặng ~18 từ.** Junior báo câu "The junior in the first note was blocked at the daily by a question this page answers" (mục 2) treo một câu hỏi mãi ba mục sau mới trả lời; mục 5 vốn đã kể lại đầy đủ chuyện đó → xóa câu ở mục 2 và **dời `section` của claim 3 từ 2 sang 5** — đóng issue, nhả 18 từ cho các major khác, không mất dữ kiện nào. (4) **Gloss hai status code cho junior kéo theo một `version_key` mới.** Junior (minor) không biết 403/409 (`status-code` có trong `known_vocab`, hai con số thì không); viết "returns 403, a refusal" và "409 …, the code for a request that does not fit the current state of the order" là khẳng định ngữ nghĩa RFC → thêm claim 22 `version_key: http` và thêm `http` vào `versions_used` (khóa `standards.http` đã có sẵn, không phải sửa nguồn sự thật). Lưu ý vocab: chữ `conflict` (term của `foundation.l2.git-conflicts`) chỉ được nằm trong `claims[].text`, văn xuôi phải nói vòng. Thêm claim 23 (Sprint Backlog, `scrum`) cho gloss "sprint backlog" mà junior (major) đòi; ledger 21 → 23. Ngân sách: 1503 → **1592/1600** sau khi cắt ~40 từ ở chỗ review không nhắc (danh sách DoD xem trước ở mục 4 — mục 5 liệt kê đủ ngay sau đó; các đuôi tu từ); mục 2 118/120, mục 4 260/300. Lần này `word_count` lấy **đúng số của `countWords`** bằng `tools/bin/wc_tmp.dart` tạm (mẫu `ssh-and-remote` mục 138) rồi `mv` ra scratchpad — script node tự viết cho cùng 1592, không lệch 4 từ như mục 177 (chênh lệch ở đó là do script hôm ấy lược ba dòng `<details>`/`<summary>`). **Sáu issue nằm trong `.quiz.json`** (technical minor q1; junior major q2, minor q5+q1, minor q6) **cộng hai chỗ quiz phải chạy theo bài vừa sửa** (giải thích q6 chép "a button alone would satisfy one of the five"; q2 option d "at the sprint review" — mục 144) → `open_questions`, **phải chạy `/quiz management.l1.user-story-and-ac apply-review`** nếu không bài không bao giờ `approved` (R03). `tools/validate management.l1.user-story-and-ac`: **0 lỗi E**.
- (2026-09-22, `/lesson management.l1.user-story-and-ac apply-review` vòng 2) Áp 1 major + 6 minor (bài) của technical và 3 major + 7 minor (bài) của junior; claim 17 và 21 lấy `suggested_text`. Ba điểm mới đáng nhớ: (1) **Major của technical lần này là một suy luận vượt quá chính tiêu chí bài đang trích** — bài viết "tiêu chí 3 trả lời điều junior bị vướng (đơn `shipped` có hủy được không)", nhưng tiêu chí 3 chỉ nói **nút không hiện**; chính bài, hai câu sau, lại nói tiêu chí 4–5 rời khỏi màn hình, tức request vẫn gửi được. Sửa: thu hẹp câu về "nửa màn hình" và **mở rộng claim 10** thêm một câu phủ định ("không tiêu chí nào nói đơn `shipped` có được hủy bằng chính request hay không") thay vì thêm claim mới. Bài học: khi bài nói một tiêu chí "đóng" một câu hỏi, đối chiếu đúng phạm vi tiêu chí đó, đừng lấy chủ đề chung làm cầu. (2) **Một minor `accuracy` có thể được đóng bằng cách XÓA câu, rẻ hơn mở rộng nó** — technical báo gloss "sprint backlog" ở mục 2 hẹp hơn claim 23 (Scrum Guide có ba phần), và chính fix đề xuất hai lối: viết đủ ba phần (+6 từ) hoặc bỏ gloss để bài prereq gánh định nghĩa (−13 từ). Chọn lối thứ hai và **bỏ luôn claim 23** — verdict `rephrased` của nó không còn áp dụng; ngân sách từ có thêm 13 từ để trả cho major của junior ở cùng mục. Nguyên tắc: với minor "bài hẹp hơn claim", xóa câu là một lời giải hợp lệ, không phải né tránh. (3) **Hai reviewer kéo ngược nhau trong cùng một đoạn văn** — junior (minor, `length`) đòi cắt đoạn "tên Definition of Done đến từ đâu" xuống hai câu, trong khi technical đòi thêm ba sửa chính xác vào đúng đoạn đó (`order` trong danh sách refinement, `never`→`not`, "cannot name"→"has nothing to gain from naming"). Giải: gộp câu (5 câu → 3 câu, +6 từ thay vì +14), ghi phần chưa áp vào `open_questions` thay vì im lặng. Ngân sách: 1592 → 1597/1600, mục 2 114/120, mục 4 265/300; cắt ~35 từ ở chỗ review không nhắc. `claims[].text` ≤ 300 chặn một lần (claim 20: 310), `self_check.notes` ≤ 500 chặn một lần (561) — đúng hai trần đã cảnh báo từ `files-and-permissions`. 2 issue còn lại nằm trong `.quiz.json` (technical minor q5, junior minor q1) cộng với các issue quiz của vòng 1 → **phải chạy `/quiz management.l1.user-story-and-ac apply-review`**, nếu không bài không bao giờ `approved` (R03). `tools/validate management.l1.user-story-and-ac`: 0 lỗi E.
- (2026-09-22, `/lesson management.l1.code-review-basics apply-review` vòng 4) Áp 2 major + 1 minor (bài) của technical (`fix-required`) và 6 minor (bài) của junior (`pass`). Ba điểm đáng nhớ: (1) **Hai claim `rephrased` (13, 19) lấy nội dung `suggested_text` nhưng không chép câu chữ** — bản gợi ý của claim 19 trích nguyên một câu GitHub Docs và nêu tên dịch vụ, trong khi prompt 00 cấm quote/URL và bài cố tình chỉ nói "dịch vụ lưu trữ repo"; chỉ lấy *điều kiện* ("trên dịch vụ lưu trữ, PR bám theo nhánh nguồn chứ không phải một tập commit cố định"). Cùng họ với "lọc `suggested_text` qua vocab" (`ip-and-ports`): **`suggested_text` là đề xuất nội dung, phải qua ràng buộc cứng trước khi dán.** (2) **Major "khẳng định không có trong ledger" giải bằng THÊM claim, không phải bỏ câu** — technical cho hai lối với so sánh "junior đi review học nhanh hơn": bỏ, hoặc ghi vào claims; thêm claim 20 (`opinion`, outline item 4) rẻ hơn và giữ đúng outline item 4, kèm hạ superlative ở dòng tóm tắt 5 ("fastest way" → "sooner than only being reviewed") cho khớp claim. (3) **Mục 4 vào vòng này chỉ còn 1 từ dư (299/300)** — để trả cho 3 câu review thêm vào đúng mục đó, bỏ hẳn câu về "question" ở mục 4 và **chuyển claim 14 về mục 3** nơi Core concepts đã định nghĩa question; đồng thời thay `—` bằng ngoặc/dấu phẩy ở các đoạn review không nhắc — A.1 đếm `—` đứng riêng là một từ, nên mỗi cặp dash tiết kiệm 2 từ, thủ thuật rẻ nhất khi kẹt trần L07/L13. Tổng 1596/1600, mục 2 118/120, 20 claim, `self_check.notes` ≤ 500 chặn một lần (597). 5 issue nằm trong `.quiz.json` (3 của các vòng trước + 2 vòng này: đáp án đúng luôn là (a) ở q1/q5/q6; q4 (c) dịch gần nguyên văn comment đã trích ở mục 5) và technical chưa chạy bước 6 → **phải chạy `/quiz management.l1.code-review-basics apply-review`** rồi review lại quiz, nếu không bài không bao giờ `approved` (R03). `tools/validate management.l1.code-review-basics`: **0 lỗi E**.
- (2026-09-22, `/quiz management.l1.code-review-basics apply-review`) Áp 2 issue `kind: quiz` của junior vòng 4 và xác nhận 3 issue vòng trước đã nằm sẵn trong bank (không có chữ "push" nào trong `.quiz.json`, q2 (c) đã viết lại theo fix của r3, q4 a/b/e đã đổi chỗ) → **kiểm trước khi sửa, không sinh lại bank** (cùng họ với mục 141). Hai điểm đáng nhớ: (1) **"Đáp án đúng luôn là (a)" sửa bằng cách hoán vị NỘI DUNG giữa các id, không phải đảo thứ tự mảng** — app hiện option theo đúng thứ tự liệt kê, nên giữ id chạy a–d và chuyển text sang id khác (q5: đáp án a→c, q6: a→b), đồng thời chuyển **khóa giải thích** và trường `misconception` theo text (không theo id cũ). Sau khi hoán vị, đáp án đúng chỉ còn ở (a) tại q1. (2) **Viết lại một option "dịch gần nguyên văn đoạn trích" phải đổi cả chỗ được hỏi, không chỉ đổi chữ** — q4 (c) mới hỏi về một dòng khác của thay đổi hủy đơn (`Cancel` khi đơn đã ở `cancelled`) nên vẫn là "câu hỏi tốt" (khóa giữ [a, c]) mà không nhận ra được từ code block mục 5. **Mặc định mới: `/quiz … apply-review` được sửa `open_questions` của `.meta.json`** khi mục đó mô tả đúng các issue quiz vừa áp — để nguyên thì `.meta.json` nói sai sự thật và W M06 mãi kể việc đã xong; chỉ sửa đúng mục đó, không đụng `claims`/`self_check`. Còn mở: **bước 6 prompt 04 chưa từng chạy cho bài này** → chưa có verdict quiz, phải chạy trên bank hiện tại trước khi bài được `approved`. Tự kiểm: Bloom 1/3/2/0 (stage 0, lệch ≤ ±1), `section_ref` 3/6/4/5/5/6, 4 loại câu, khó trung bình 2.17 ≤ 2.5, mọi option ≤ 220 ký tự. `tools/validate management.l1.code-review-basics`: **0 lỗi E** (2 W: L11 chip "sắp có", M06 open_questions).
- (2026-09-23, `/outline track=design module=solid stage=1 tag=stage-0`) **Track `design` bắt đầu.** 7 bài: `why-design-matters`, `coupling-and-cohesion`, `solid-srp` (id hứa trước, đúng), `solid-ocp`, `solid-lsp`, `solid-isp`, `solid-dip`. **Quyết tái dùng sample có sẵn (tự quyết, không hỏi lại):** `Samples/Clean/{PlaceOrderLong,PlaceOrderSplit}.cs` (GĐ0) minh họa đúng why-design-matters/coupling-cohesion/SRP; `Samples/Oop/{ShippingFee,INotifier,NotifierBase}.cs` (GĐ0) minh họa đúng OCP/LSP/ISP/DIP — dùng lại nguyên văn với góc nhìn mới, không tạo file trùng lặp. Chỉ 4 phản ví dụ THỰC SỰ chưa có (vi phạm OCP/LSP/ISP, một caller phụ thuộc `INotifier` cho DIP) vào `repo_changes_needed`, đặt ở thư mục mới `Samples/Design/` (song song `Clean/`, `Oop/`) để không đụng file cũ đã chốt (CLAUDE.md quy tắc 7). **Mẫu chung cho các module design/GoF-patterns sau:** luôn kiểm `samples/DonHang.Samples/Samples/*` trước khi viết `repo_changes_needed` — code minh họa design principle thường đã có sẵn từ lúc dạy OOP cơ bản ở GĐ0, chỉ thiếu phản ví dụ (bad case) là thứ genuinely mới. `tools/merge-outline`: 0 lỗi E.
- (2026-09-23, `/outline track=backend module=validation-errors-logging stage=1 tag=stage-0`) 5 bài: `errors-and-problem-details`, `validating-input`, `choosing-an-error-status`, `structured-logging`, `exception-handling-middleware` (2 id hứa trước từ GĐ0 dùng đúng). Lesson cuối **hiện thực đúng** middleware đã hứa "tái dùng được" ở `repo_changes_needed` của module `web-server` — không viết middleware minh họa mới, tie-back có chủ đích giữ Program.cs không phình ra hai bản. Đối chiếu `db/schema.sql` trước khi viết lý do validate: `customer_id` đã có FK, `quantity` đã có `CHECK` — DB tự chặn hai lỗi đó rồi, lý do thật để validate ở tầng ứng dụng là (a) trả `400` sạch thay vì lỗi DB thô, và (b) "ít nhất một item" là ràng buộc liên-dòng không cột nào chặn được; không bịa lý do khác. `repo_changes_needed` chọn đúng API thật của ASP.NET Core (`AddProblemDetails()`, `Results.Problem`, `ILogger` message-template) thay vì thư viện ngoài, để tránh claim sai lệch nếu bài sau (`/lesson`) tra cứu không khớp thực tế .NET 10. `tools/merge-outline`: 0 lỗi E.
- (2026-09-23, `/outline track=backend module=orm-efcore stage=1 tag=stage-0`) 6 bài: `efcore-mapping`, `efcore-relationships-and-keys`, `migrations`, `querying-with-linq`, `saving-changes`, `efcore-n-plus-one` (2 id hứa trước từ GĐ0 dùng đúng: `efcore-mapping`, `efcore-n-plus-one`). **Quyết kiến trúc tự quyết (không hỏi lại):** `db/schema.sql` (đã có từ GĐ0, dùng để bootstrap Postgres container của lab) **không đổi** cách khởi tạo; EF Core Migrations trở thành nguồn sự thật cho **thay đổi mới** kể từ module này — migration đầu tiên là "baseline" khớp đúng `db/schema.sql`, không áp lại vào DB dev đã seed sẵn (chỉ dùng cho môi trường mới/CI, verify bằng cách áp vào DB rỗng rồi diff). Endpoint GET `/api/v1/products` và POST `/api/v1/orders` (đã xây ở `rest-api`, đang dùng list trong bộ nhớ) được viết lại dùng `DbContext` thật, giữ nguyên hợp đồng response/status code — không cần bài mới, chỉ `repo_changes_needed` sửa file cũ. Tránh trùng thuật ngữ với DDD tactical (GĐ3) dạy lại "Entity" theo nghĩa khác: **không** đưa "entity" vào `vocab` module này (chỉ dùng như code thường, không bold), để dành từ đó cho DDD. `tools/merge-outline`: 0 lỗi E.
- (2026-09-23, `/outline track=backend module=rest-api stage=1 tag=stage-0`) 5 bài: `rest-resources`, `dtos-and-serialization`, `get-and-status-codes`, `creating-a-resource`, `rest-for-writes`. Dùng đúng 2 id "hứa trước" từ GĐ0 (`backend.l1.rest-resources`, `backend.l1.dtos-and-serialization`). **Quyết phạm vi không cần hỏi lại:** chỉ xây code thật cho GET `/api/v1/products(+/{id})` và POST `/api/v1/orders` (khớp đúng docs/00 §2 GĐ1 "Flutter client danh sách sản phẩm và tạo đơn"); PUT/PATCH/DELETE chỉ dạy ngữ nghĩa qua Caddyfile có sẵn từ GĐ0, không hứa endpoint thật — tránh vẽ thêm việc ngoài phạm vi app V1. Dữ liệu ở các endpoint mới là **danh sách trong bộ nhớ**, không phải DB thật — `repo_changes_needed` ghi rõ để module `orm-efcore` (tiếp theo) thay bằng EF Core mà không đổi hợp đồng endpoint. `tools/merge-outline`: 0 lỗi E lần đầu (không đụng bug L07 maxLength như module trước).
- (2026-09-23, `/outline track=backend module=web-server stage=1 tag=stage-0`) **Bắt đầu Giai đoạn 1.** `tools/known-vocab --before-module` trả `[]` cho module chưa có `track.yaml` (tool tra theo `modulesByKey`, không tự suy theo `path.yaml`) → tính `known_vocab` bằng tay = toàn bộ `content/glossary.yaml` hiện có (40 term của GĐ0), vì `backend/web-server` là module đầu tiên của GĐ1. Chủ giao toàn quyền quyết 3 `open_questions` của draft ("bạn tự quyết đi"), quyết theo brainstorm v0.3 (docs/00-chuong-trinh.md §2 "GĐ 1 — Một API + một app… chạy bằng Docker Compose") và DECISIONS D8 (Caddy đã có TLS `donhang.local:8443`, xu hướng giữ vai trò reverse-proxy xuyên suốt các GĐ): (1) **Caddy tiếp tục đứng trước Kestrel** làm reverse-proxy/TLS-termination ở stage 1 (không expose Kestrel trực tiếp) — khớp vai trò Caddy đã thiết lập từ GĐ0 và để lại chỗ cho devops/deploy-basics, devops/docker dạy reverse proxy thật; `/repo-stage 1` cấu hình `Caddyfile` proxy `/api/v1/*` sang service `api` trong `docker-compose.yml`. (2) **Giữ nguyên `DonHang.Api/Program.cs`** ở gốc repo (ngang `samples/`), đúng tên cố định `prompts/00-system.md`. (3) **Middleware demo là middleware thật, tái dùng được** (không phải code minh họa rồi bỏ) — để `validation-errors-logging` và `auth-basics` mở rộng cùng một `Program.cs` thay vì viết lại. Merge draft bằng `tools/merge-outline backend content/tracks/backend/outline-web-server.draft.yaml`. Áp dụng cho mọi outline GĐ1 sau: khi `known-vocab --before-module` trả `[]` vì module chưa tồn tại, tính tay từ `glossary.yaml` + vị trí module trong `path.yaml`; `open_questions` của draft outline được tự quyết theo đúng cơ chế DECISIONS §0 (chọn mặc định hợp lý, ghi lại, đi tiếp) trừ khi ảnh hưởng không đảo ngược được.
